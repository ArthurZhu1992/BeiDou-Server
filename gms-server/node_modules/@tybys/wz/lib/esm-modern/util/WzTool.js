import { WzMapleVersion } from "../WzMapleVersion.js";
import { WZ_MSEAIV, WZ_GMSIV } from "./MapleCryptoConstants.js";
export function rotateLeft(x, n) {
    return ((((x) << (n)) >>> 0) | ((x) >>> (32 - (n)))) >>> 0;
}
export function getIvByMapleVersion(ver) {
    switch (ver) {
        case WzMapleVersion.EMS:
            return WZ_MSEAIV;
        case WzMapleVersion.GMS:
            return WZ_GMSIV;
        case WzMapleVersion.CUSTOM: {
            return new Uint8Array(4);
        }
        case WzMapleVersion.GENERATE:
            return new Uint8Array(4);
        case WzMapleVersion.BMS:
        case WzMapleVersion.CLASSIC:
        default:
            return new Uint8Array(4);
    }
}
