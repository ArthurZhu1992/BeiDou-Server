import { __awaiter, __extends, __generator } from "tslib";
import * as bufferMethods from "./buffer.js";
import { Reader, checkRange } from "./Reader.js";
import { fs } from "./node.js";
import { EndianType } from "./EndianType.js";
import { FileDescriptor } from "./FileDescriptor.js";
var onceSupported = false;
try {
    var options = Object.defineProperty({}, 'once', {
        get: function () {
            onceSupported = true;
        }
    });
    window.addEventListener('test', null, options);
}
catch (err) { }
var listenerOpts = onceSupported ? { once: true } : false;
function fsRead(reader, buf, bufStart, len, pos) {
    return new Promise(function (resolve, reject) {
        var file = reader._file;
        if (typeof file === 'number') {
            try {
                resolve(fs.readSync(file, buf, bufStart, len, pos));
            }
            catch (err) {
                reject(err);
            }
        }
        else {
            var fr_1 = new FileReader();
            var onError_1;
            // eslint-disable-next-line prefer-const
            var onLoad_1;
            // eslint-disable-next-line prefer-const
            onError_1 = function () {
                if (!onceSupported) {
                    fr_1.removeEventListener('error', onError_1);
                    fr_1.removeEventListener('load', onLoad_1);
                }
                reject(fr_1.error);
            };
            // eslint-disable-next-line prefer-const
            onLoad_1 = function () {
                if (!onceSupported) {
                    fr_1.removeEventListener('error', onError_1);
                    fr_1.removeEventListener('load', onLoad_1);
                }
                var readBuffer = new Uint8Array(fr_1.result);
                var copied = bufferMethods.copy(readBuffer, buf, bufStart, 0, readBuffer.length);
                resolve(copied);
            };
            fr_1.addEventListener('error', onError_1, listenerOpts);
            fr_1.addEventListener('load', onLoad_1, listenerOpts);
            fr_1.readAsArrayBuffer(file.slice(pos, pos + len));
        }
    });
}
var methods = bufferMethods.methods;
function readNumber(reader, method) {
    var promise = new Promise(function (resolve, reject) {
        checkRange(reader.pos, reader.size);
        if (method === 'readBigInt64BE' || method === 'readBigInt64LE' || method === 'readBigUInt64BE' || method === 'readBigUInt64LE') {
            bufferMethods.validateBigInt();
        }
        var buf = new Uint8Array(methods[method]);
        if (reader.type === 0 /* FILE */) {
            fsRead(reader, buf, 0, methods[method], reader.pos).then(function (readLength) {
                reader.pos += readLength;
                resolve(bufferMethods[method](buf, 0));
            }).catch(reject);
        }
        else {
            var readLength = bufferMethods.copy(reader._buffer, buf, 0, reader.pos, reader.pos + methods[method]);
            reader.pos += readLength;
            resolve(bufferMethods[method](buf, 0));
        }
    });
    return promise;
}
/**
 * @public
 */
var AsyncBinaryReader = /*#__PURE__*/ (function (_super) {
    __extends(AsyncBinaryReader, _super);
    function AsyncBinaryReader(fileOrBuffer, endian) {
        if (endian === void 0) { endian = EndianType.BigEndian; }
        var _this = this;
        if (fileOrBuffer instanceof Uint8Array) {
            _this = _super.call(this, fileOrBuffer.length) || this;
            Object.defineProperty(_this, 'type', { configurable: true, enumerable: true, writable: false, value: 1 /* BUFFER */ });
            _this._path = '';
            _this._file = null;
            _this._buffer = fileOrBuffer;
        }
        else if (typeof fileOrBuffer === 'string') {
            _this = _super.call(this, fs.statSync(fileOrBuffer).size) || this;
            Object.defineProperty(_this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            _this._file = fs.openSync(fileOrBuffer, 'r');
            _this._path = fileOrBuffer;
            _this._buffer = null;
        }
        else if (fileOrBuffer instanceof FileDescriptor) {
            _this = _super.call(this, fileOrBuffer.size) || this;
            Object.defineProperty(_this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            _this._file = fileOrBuffer.fd;
            _this._path = fileOrBuffer.path;
            _this._buffer = null;
        }
        else {
            _this = _super.call(this, fileOrBuffer.size) || this;
            Object.defineProperty(_this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            _this._file = fileOrBuffer;
            _this._path = fileOrBuffer.webkitRelativePath || '';
            _this._buffer = null;
        }
        Object.defineProperty(_this, 'endian', {
            configurable: true,
            enumerable: true,
            get: function () { return endian; },
            set: function (ed) {
                if (ed !== EndianType.BigEndian && ed !== EndianType.LittleEndian) {
                    throw new TypeError('endian type error');
                }
                endian = ed;
            }
        });
        _this._opened = true;
        return _this;
    }
    AsyncBinaryReader.prototype.close = function () {
        if (this._opened) {
            if (this.type === 0 /* FILE */) {
                if (typeof this._file === 'number') {
                    fs.closeSync(this._file);
                }
                this._file = null;
            }
            else {
                this._buffer = null;
            }
            this._opened = false;
        }
    };
    AsyncBinaryReader.prototype.read = function (len) {
        var _this = this;
        if (len === void 0) { len = 1; }
        var promise = new Promise(function (resolve, reject) {
            checkRange(_this.pos, _this._size);
            if (len === 0) {
                resolve(new Uint8Array(0));
                return;
            }
            if (_this.pos + len > _this._size) {
                len = _this._size - _this.pos;
            }
            if (len === 0) {
                resolve(new Uint8Array(0));
                return;
            }
            var buf = new Uint8Array(len);
            if (_this.type === 0 /* FILE */) {
                fsRead(_this, buf, 0, len, _this.pos).then(function (readLength) {
                    _this.pos += readLength;
                    resolve(buf);
                }).catch(reject);
            }
            else {
                var readLength = bufferMethods.copy(_this._buffer, buf, 0, _this.pos, _this.pos + len);
                _this.pos += readLength;
                resolve(buf);
            }
        });
        return promise;
    };
    AsyncBinaryReader.prototype.readToBuffer = function (buf, bufStart, len) {
        var _this = this;
        if (bufStart === void 0) { bufStart = 0; }
        if (len === void 0) { len = 1; }
        var promise = new Promise(function (resolve, reject) {
            checkRange(_this.pos, _this._size);
            if (len === 0) {
                resolve(0);
                return;
            }
            if (_this.pos + len > _this._size) {
                len = _this._size - _this.pos;
            }
            if (len === 0) {
                resolve(0);
                return;
            }
            if (_this.type === 0 /* FILE */) {
                fsRead(_this, buf, bufStart, len, _this.pos).then(function (readLength) {
                    _this.pos += readLength;
                    resolve(readLength);
                }).catch(reject);
            }
            else {
                var readLength = bufferMethods.copy(_this._buffer, buf, bufStart, _this.pos, _this.pos + len);
                _this.pos += readLength;
                resolve(readLength);
            }
        });
        return promise;
    };
    AsyncBinaryReader.prototype.readString = function (encoding, length) {
        if (encoding === void 0) { encoding = 'ascii'; }
        if (length === void 0) { length = -1; }
        return __awaiter(this, void 0, void 0, function () {
            var l, buf, readLength, r, _a, _b;
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0:
                        checkRange(this.pos, this._size);
                        if (length === 0) {
                            return [2 /*return*/, ''];
                        }
                        if (!(length === -1)) return [3 /*break*/, 10];
                        l = 0;
                        buf = new Uint8Array(1);
                        _c.label = 1;
                    case 1:
                        readLength = void 0;
                        if (!(this.type === 0 /* FILE */)) return [3 /*break*/, 3];
                        return [4 /*yield*/, fsRead(this, buf, 0, 1, this.pos + l)];
                    case 2:
                        readLength = _c.sent();
                        return [3 /*break*/, 4];
                    case 3:
                        readLength = bufferMethods.copy(this._buffer, buf, 0, this.pos + l, this.pos + l + 1);
                        _c.label = 4;
                    case 4:
                        if (readLength === 0)
                            return [3 /*break*/, 6];
                        l += readLength;
                        _c.label = 5;
                    case 5:
                        if (buf[0] !== 0) return [3 /*break*/, 1];
                        _c.label = 6;
                    case 6:
                        r = new Uint8Array(l - 1);
                        if (!(this.type === 0 /* FILE */)) return [3 /*break*/, 8];
                        return [4 /*yield*/, fsRead(this, r, 0, l - 1, this.pos)];
                    case 7:
                        _c.sent();
                        return [3 /*break*/, 9];
                    case 8:
                        bufferMethods.copy(this._buffer, r, 0, this.pos, this.pos + l - 1);
                        _c.label = 9;
                    case 9:
                        this.pos += l;
                        return [2 /*return*/, bufferMethods.bufferToString(r, encoding)];
                    case 10:
                        _b = (_a = bufferMethods).bufferToString;
                        return [4 /*yield*/, this.read(length)];
                    case 11: return [2 /*return*/, _b.apply(_a, [_c.sent(), encoding])];
                }
            });
        });
    };
    AsyncBinaryReader.prototype.readBoolean = function () {
        return this.readUInt8().then(function (u8) { return (u8 !== 0); });
    };
    AsyncBinaryReader.prototype.readInt8 = function () {
        return readNumber(this, 'readInt8');
    };
    AsyncBinaryReader.prototype.readUInt8 = function () {
        return readNumber(this, 'readUInt8');
    };
    AsyncBinaryReader.prototype.readInt16LE = function () {
        return readNumber(this, 'readInt16LE');
    };
    AsyncBinaryReader.prototype.readUInt16LE = function () {
        return readNumber(this, 'readUInt16LE');
    };
    AsyncBinaryReader.prototype.readInt16BE = function () {
        return readNumber(this, 'readInt16BE');
    };
    AsyncBinaryReader.prototype.readUInt16BE = function () {
        return readNumber(this, 'readUInt16BE');
    };
    AsyncBinaryReader.prototype.readInt32LE = function () {
        return readNumber(this, 'readInt32LE');
    };
    AsyncBinaryReader.prototype.readUInt32LE = function () {
        return readNumber(this, 'readUInt32LE');
    };
    AsyncBinaryReader.prototype.readInt32BE = function () {
        return readNumber(this, 'readInt32BE');
    };
    AsyncBinaryReader.prototype.readUInt32BE = function () {
        return readNumber(this, 'readUInt32BE');
    };
    AsyncBinaryReader.prototype.readBigInt64LE = function () {
        return readNumber(this, 'readBigInt64LE');
    };
    AsyncBinaryReader.prototype.readBigUInt64LE = function () {
        return readNumber(this, 'readBigUInt64LE');
    };
    AsyncBinaryReader.prototype.readBigInt64BE = function () {
        return readNumber(this, 'readBigInt64BE');
    };
    AsyncBinaryReader.prototype.readBigUInt64BE = function () {
        return readNumber(this, 'readBigUInt64BE');
    };
    AsyncBinaryReader.prototype.readFloatLE = function () {
        return readNumber(this, 'readFloatLE');
    };
    AsyncBinaryReader.prototype.readFloatBE = function () {
        return readNumber(this, 'readFloatBE');
    };
    AsyncBinaryReader.prototype.readDoubleLE = function () {
        return readNumber(this, 'readDoubleLE');
    };
    AsyncBinaryReader.prototype.readDoubleBE = function () {
        return readNumber(this, 'readDoubleBE');
    };
    AsyncBinaryReader.prototype.readInt16 = function () {
        return this.endian === EndianType.BigEndian ? this.readInt16BE() : this.readInt16LE();
    };
    AsyncBinaryReader.prototype.readUInt16 = function () {
        return this.endian === EndianType.BigEndian ? this.readUInt16BE() : this.readUInt16LE();
    };
    AsyncBinaryReader.prototype.readInt32 = function () {
        return this.endian === EndianType.BigEndian ? this.readInt32BE() : this.readInt32LE();
    };
    AsyncBinaryReader.prototype.readUInt32 = function () {
        return this.endian === EndianType.BigEndian ? this.readUInt32BE() : this.readUInt32LE();
    };
    AsyncBinaryReader.prototype.readBigInt64 = function () {
        return this.endian === EndianType.BigEndian ? this.readBigInt64BE() : this.readBigInt64LE();
    };
    AsyncBinaryReader.prototype.readBigUInt64 = function () {
        return this.endian === EndianType.BigEndian ? this.readBigUInt64BE() : this.readBigUInt64LE();
    };
    AsyncBinaryReader.prototype.readFloat = function () {
        return this.endian === EndianType.BigEndian ? this.readFloatBE() : this.readFloatLE();
    };
    AsyncBinaryReader.prototype.readDouble = function () {
        return this.endian === EndianType.BigEndian ? this.readDoubleBE() : this.readDoubleLE();
    };
    return AsyncBinaryReader;
}(Reader));
export { AsyncBinaryReader };
