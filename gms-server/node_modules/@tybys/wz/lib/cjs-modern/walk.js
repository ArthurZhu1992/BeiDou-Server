"use strict";
/* eslint-disable @typescript-eslint/strict-boolean-expressions */
Object.defineProperty(exports, "__esModule", { value: true });
exports.walkPropertyContainer = exports.walkDirectory = exports.walkWzFileAsync = void 0;
const WzUOLProperty_1 = require("./properties/WzUOLProperty.js");
const WzFile_1 = require("./WzFile.js");
const WzImage_1 = require("./WzImage.js");
const init_1 = require("./init.js");
const WzFileParseStatus_1 = require("./WzFileParseStatus.js");
/**
 * @public
 * @returns `true` if stop manually
 */
async function walkWzFileAsync(filepath, mapleVersion, callback) {
    await (0, init_1.init)();
    const wz = new WzFile_1.WzFile(filepath, mapleVersion);
    const r = await wz.parseWzFile();
    if (r !== WzFileParseStatus_1.WzFileParseStatus.SUCCESS) {
        wz.dispose();
        throw new Error((0, WzFileParseStatus_1.getErrorDescription)(r));
    }
    const stop = await walkDirectory(wz.wzDirectory, callback);
    wz.dispose();
    return stop;
}
exports.walkWzFileAsync = walkWzFileAsync;
/**
 * @public
 * @returns `true` if stop manually
 */
async function walkDirectory(dir, callback) {
    let stop = !!(await Promise.resolve(callback(dir)));
    if (stop) {
        dir.dispose();
        return stop;
    }
    for (const subdir of dir.wzDirectories) {
        stop = await walkDirectory(subdir, callback);
        if (stop) {
            dir.dispose();
            return stop;
        }
    }
    for (const img of dir.wzImages) {
        stop = await walkPropertyContainer(img, callback);
        if (stop) {
            dir.dispose();
            return stop;
        }
    }
    dir.dispose();
    return stop;
}
exports.walkDirectory = walkDirectory;
/**
 * @public
 * @returns `true` if stop manually
 */
async function walkPropertyContainer(container, callback) {
    if (container instanceof WzImage_1.WzImage) {
        await container.parseImage();
    }
    let stop = !!(await Promise.resolve(callback(container)));
    if (!stop && !(container instanceof WzUOLProperty_1.WzUOLProperty) && container.wzProperties != null) {
        for (const prop of container.wzProperties) {
            stop = await walkPropertyContainer(prop, callback);
            if (stop)
                break;
        }
    }
    if (container instanceof WzImage_1.WzImage) {
        container.dispose();
    }
    return stop;
}
exports.walkPropertyContainer = walkPropertyContainer;
//# sourceMappingURL=walk.js.map