"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AsyncBinaryReader = void 0;
const bufferMethods = require("./buffer.js");
const Reader_1 = require("./Reader.js");
const node_1 = require("./node.js");
const EndianType_1 = require("./EndianType.js");
const FileDescriptor_1 = require("./FileDescriptor.js");
let onceSupported = false;
try {
    const options = Object.defineProperty({}, 'once', {
        get: function () {
            onceSupported = true;
        }
    });
    window.addEventListener('test', null, options);
}
catch (err) { }
const listenerOpts = onceSupported ? { once: true } : false;
function fsRead(reader, buf, bufStart, len, pos) {
    return new Promise((resolve, reject) => {
        const file = reader._file;
        if (typeof file === 'number') {
            try {
                resolve(node_1.fs.readSync(file, buf, bufStart, len, pos));
            }
            catch (err) {
                reject(err);
            }
        }
        else {
            const fr = new FileReader();
            let onError;
            // eslint-disable-next-line prefer-const
            let onLoad;
            // eslint-disable-next-line prefer-const
            onError = () => {
                if (!onceSupported) {
                    fr.removeEventListener('error', onError);
                    fr.removeEventListener('load', onLoad);
                }
                reject(fr.error);
            };
            // eslint-disable-next-line prefer-const
            onLoad = () => {
                if (!onceSupported) {
                    fr.removeEventListener('error', onError);
                    fr.removeEventListener('load', onLoad);
                }
                const readBuffer = new Uint8Array(fr.result);
                const copied = bufferMethods.copy(readBuffer, buf, bufStart, 0, readBuffer.length);
                resolve(copied);
            };
            fr.addEventListener('error', onError, listenerOpts);
            fr.addEventListener('load', onLoad, listenerOpts);
            fr.readAsArrayBuffer(file.slice(pos, pos + len));
        }
    });
}
const methods = bufferMethods.methods;
function readNumber(reader, method) {
    const promise = new Promise((resolve, reject) => {
        (0, Reader_1.checkRange)(reader.pos, reader.size);
        if (method === 'readBigInt64BE' || method === 'readBigInt64LE' || method === 'readBigUInt64BE' || method === 'readBigUInt64LE') {
            bufferMethods.validateBigInt();
        }
        const buf = new Uint8Array(methods[method]);
        if (reader.type === 0 /* FILE */) {
            fsRead(reader, buf, 0, methods[method], reader.pos).then(readLength => {
                reader.pos += readLength;
                resolve(bufferMethods[method](buf, 0));
            }).catch(reject);
        }
        else {
            const readLength = bufferMethods.copy(reader._buffer, buf, 0, reader.pos, reader.pos + methods[method]);
            reader.pos += readLength;
            resolve(bufferMethods[method](buf, 0));
        }
    });
    return promise;
}
/**
 * @public
 */
class AsyncBinaryReader extends Reader_1.Reader {
    constructor(fileOrBuffer, endian = EndianType_1.EndianType.BigEndian) {
        if (fileOrBuffer instanceof Uint8Array) {
            super(fileOrBuffer.length);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 1 /* BUFFER */ });
            this._path = '';
            this._file = null;
            this._buffer = fileOrBuffer;
        }
        else if (typeof fileOrBuffer === 'string') {
            super(node_1.fs.statSync(fileOrBuffer).size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = node_1.fs.openSync(fileOrBuffer, 'r');
            this._path = fileOrBuffer;
            this._buffer = null;
        }
        else if (fileOrBuffer instanceof FileDescriptor_1.FileDescriptor) {
            super(fileOrBuffer.size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = fileOrBuffer.fd;
            this._path = fileOrBuffer.path;
            this._buffer = null;
        }
        else {
            super(fileOrBuffer.size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = fileOrBuffer;
            this._path = fileOrBuffer.webkitRelativePath || '';
            this._buffer = null;
        }
        Object.defineProperty(this, 'endian', {
            configurable: true,
            enumerable: true,
            get() { return endian; },
            set(ed) {
                if (ed !== EndianType_1.EndianType.BigEndian && ed !== EndianType_1.EndianType.LittleEndian) {
                    throw new TypeError('endian type error');
                }
                endian = ed;
            }
        });
        this._opened = true;
    }
    close() {
        if (this._opened) {
            if (this.type === 0 /* FILE */) {
                if (typeof this._file === 'number') {
                    node_1.fs.closeSync(this._file);
                }
                this._file = null;
            }
            else {
                this._buffer = null;
            }
            this._opened = false;
        }
    }
    read(len = 1) {
        const promise = new Promise((resolve, reject) => {
            (0, Reader_1.checkRange)(this.pos, this._size);
            if (len === 0) {
                resolve(new Uint8Array(0));
                return;
            }
            if (this.pos + len > this._size) {
                len = this._size - this.pos;
            }
            if (len === 0) {
                resolve(new Uint8Array(0));
                return;
            }
            const buf = new Uint8Array(len);
            if (this.type === 0 /* FILE */) {
                fsRead(this, buf, 0, len, this.pos).then(readLength => {
                    this.pos += readLength;
                    resolve(buf);
                }).catch(reject);
            }
            else {
                const readLength = bufferMethods.copy(this._buffer, buf, 0, this.pos, this.pos + len);
                this.pos += readLength;
                resolve(buf);
            }
        });
        return promise;
    }
    readToBuffer(buf, bufStart = 0, len = 1) {
        const promise = new Promise((resolve, reject) => {
            (0, Reader_1.checkRange)(this.pos, this._size);
            if (len === 0) {
                resolve(0);
                return;
            }
            if (this.pos + len > this._size) {
                len = this._size - this.pos;
            }
            if (len === 0) {
                resolve(0);
                return;
            }
            if (this.type === 0 /* FILE */) {
                fsRead(this, buf, bufStart, len, this.pos).then(readLength => {
                    this.pos += readLength;
                    resolve(readLength);
                }).catch(reject);
            }
            else {
                const readLength = bufferMethods.copy(this._buffer, buf, bufStart, this.pos, this.pos + len);
                this.pos += readLength;
                resolve(readLength);
            }
        });
        return promise;
    }
    async readString(encoding = 'ascii', length = -1) {
        (0, Reader_1.checkRange)(this.pos, this._size);
        if (length === 0) {
            return '';
        }
        if (length === -1) {
            let l = 0;
            const buf = new Uint8Array(1);
            do {
                let readLength;
                if (this.type === 0 /* FILE */) {
                    readLength = await fsRead(this, buf, 0, 1, this.pos + l);
                }
                else {
                    readLength = bufferMethods.copy(this._buffer, buf, 0, this.pos + l, this.pos + l + 1);
                }
                if (readLength === 0)
                    break;
                l += readLength;
            } while (buf[0] !== 0);
            const r = new Uint8Array(l - 1);
            if (this.type === 0 /* FILE */) {
                await fsRead(this, r, 0, l - 1, this.pos);
            }
            else {
                bufferMethods.copy(this._buffer, r, 0, this.pos, this.pos + l - 1);
            }
            this.pos += l;
            return bufferMethods.bufferToString(r, encoding);
        }
        return bufferMethods.bufferToString(await this.read(length), encoding);
    }
    readBoolean() {
        return this.readUInt8().then(u8 => (u8 !== 0));
    }
    readInt8() {
        return readNumber(this, 'readInt8');
    }
    readUInt8() {
        return readNumber(this, 'readUInt8');
    }
    readInt16LE() {
        return readNumber(this, 'readInt16LE');
    }
    readUInt16LE() {
        return readNumber(this, 'readUInt16LE');
    }
    readInt16BE() {
        return readNumber(this, 'readInt16BE');
    }
    readUInt16BE() {
        return readNumber(this, 'readUInt16BE');
    }
    readInt32LE() {
        return readNumber(this, 'readInt32LE');
    }
    readUInt32LE() {
        return readNumber(this, 'readUInt32LE');
    }
    readInt32BE() {
        return readNumber(this, 'readInt32BE');
    }
    readUInt32BE() {
        return readNumber(this, 'readUInt32BE');
    }
    readBigInt64LE() {
        return readNumber(this, 'readBigInt64LE');
    }
    readBigUInt64LE() {
        return readNumber(this, 'readBigUInt64LE');
    }
    readBigInt64BE() {
        return readNumber(this, 'readBigInt64BE');
    }
    readBigUInt64BE() {
        return readNumber(this, 'readBigUInt64BE');
    }
    readFloatLE() {
        return readNumber(this, 'readFloatLE');
    }
    readFloatBE() {
        return readNumber(this, 'readFloatBE');
    }
    readDoubleLE() {
        return readNumber(this, 'readDoubleLE');
    }
    readDoubleBE() {
        return readNumber(this, 'readDoubleBE');
    }
    readInt16() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readInt16BE() : this.readInt16LE();
    }
    readUInt16() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readUInt16BE() : this.readUInt16LE();
    }
    readInt32() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readInt32BE() : this.readInt32LE();
    }
    readUInt32() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readUInt32BE() : this.readUInt32LE();
    }
    readBigInt64() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readBigInt64BE() : this.readBigInt64LE();
    }
    readBigUInt64() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readBigUInt64BE() : this.readBigUInt64LE();
    }
    readFloat() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readFloatBE() : this.readFloatLE();
    }
    readDouble() {
        return this.endian === EndianType_1.EndianType.BigEndian ? this.readDoubleBE() : this.readDoubleLE();
    }
}
exports.AsyncBinaryReader = AsyncBinaryReader;
//# sourceMappingURL=AsyncBinaryReader.js.map