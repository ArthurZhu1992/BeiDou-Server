(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else if(typeof exports === 'object')
		exports["wz"] = factory();
	else
		root["wz"] = factory();
})((function (_this) {
  if (typeof globalThis !== "undefined") return globalThis;
  if (typeof __webpack_public_path__ === 'undefined') {
    if (typeof global !== "undefined") return global;
  }
  if (typeof window !== "undefined") return window;
  if (typeof self !== "undefined") return self;
  var g;
  g = (function () { return this; })();
  try {
    g = g || new Function("return this")();
  } catch (_) {}
  return g || _this || {};
})(this), function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 38);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "e", function() { return fs; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "g", function() { return path; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return _Buffer; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "f", function() { return os; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "j", function() { return zlib; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return crypto; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return asciiTextDecoder; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "i", function() { return utf16leTextDecoder; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return Jimp; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "h", function() { return tybysWindowsFileVersionInfo; });
/* harmony import */ var _tybys_native_require__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(23);
/* harmony import */ var _wz_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(37);
/* harmony import */ var _wz_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_wz_js__WEBPACK_IMPORTED_MODULE_1__);


const _require = Object(_tybys_native_require__WEBPACK_IMPORTED_MODULE_0__[/* tryGetRequireFunction */ "a"])();
function throwError() {
    throw new Error('Node.js API is not supported');
}
const fs = (function () {
    try {
        return _require('fs');
    }
    catch (_) {
        return {
            existsSync() { throwError(); },
            mkdirSync() { throwError(); },
            writeFileSync() { throwError(); },
            openSync() { throwError(); },
            writeSync() { throwError(); },
            closeSync() { throwError(); }
        };
    }
})();
const path = (function () {
    try {
        return _require('path');
    }
    catch (_) {
        return {
            // eslint-disable-next-line @typescript-eslint/prefer-includes
            sep: typeof navigator !== 'undefined' ? ((navigator.appVersion.indexOf('Win') !== -1) ? '\\' : '/') : '/',
            dirname() { throwError(); },
            join() { throwError(); }
        };
    }
})();
const _Buffer = (function () {
    try {
        return _require('buffer').Buffer;
    }
    catch (_) {
        return null;
    }
})();
const os = (function () {
    try {
        return _require('os');
    }
    catch (_) {
        const platform = function () {
            if (window.navigator.userAgent.indexOf('Windows') !== -1) {
                return 'win32';
            }
            if (window.navigator.userAgent.indexOf('Linux') !== -1) {
                return 'linux';
            }
            if (window.navigator.userAgent.indexOf('Macintosh') !== -1) {
                return 'darwin';
            }
            return 'unknown';
        };
        return {
            EOL: window.navigator.userAgent.toLowerCase().indexOf('win') !== -1 ? '\r\n' : '\n',
            platform
        };
    }
})();
const zlib = (function () {
    try {
        return _require('zlib');
    }
    catch (_) {
        return null;
    }
})();
const crypto = (function () {
    try {
        return _require('crypto');
    }
    catch (_) {
        return {
            createCipheriv(algo, key, _iv) {
                if (algo === 'aes-256-ecb') {
                    return Object(_wz_js__WEBPACK_IMPORTED_MODULE_1__["aesCreate"])(key);
                }
                throwError();
            }
        };
    }
})();
const asciiTextDecoder = (function () {
    try {
        return new TextDecoder('ascii');
    }
    catch (_) {
        return {
            decode(buf) {
                return _Buffer.from(buf).toString('ascii');
            }
        };
    }
})();
const utf16leTextDecoder = (function () {
    try {
        return new TextDecoder('utf-16le');
    }
    catch (_) {
        return {
            decode(buf) {
                return _Buffer.from(buf).toString('utf16le');
            }
        };
    }
})();
const Jimp = (function () {
    try {
        return _require('jimp');
    }
    catch (_) {
        return null;
    }
})();
const tybysWindowsFileVersionInfo = (function () {
    try {
        if (os.platform() !== 'win32')
            throwError();
        return _require('@tybys/windows-file-version-info');
    }
    catch (_) {
        const FileVersionInfo = function () { throwError(); };
        FileVersionInfo.getVersionInfo = function () {
            throwError();
        };
        return { FileVersionInfo };
    }
})();


/***/ }),
/* 1 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzPropertyType; });
/**
 * @public
 */
var WzPropertyType;
(function (WzPropertyType) {
    // #region Regular
    WzPropertyType[WzPropertyType["Null"] = 0] = "Null";
    WzPropertyType[WzPropertyType["Short"] = 1] = "Short";
    WzPropertyType[WzPropertyType["Int"] = 2] = "Int";
    WzPropertyType[WzPropertyType["Long"] = 3] = "Long";
    WzPropertyType[WzPropertyType["Float"] = 4] = "Float";
    WzPropertyType[WzPropertyType["Double"] = 5] = "Double";
    WzPropertyType[WzPropertyType["String"] = 6] = "String";
    // #endregion
    // #region Extended
    WzPropertyType[WzPropertyType["SubProperty"] = 7] = "SubProperty";
    WzPropertyType[WzPropertyType["Canvas"] = 8] = "Canvas";
    WzPropertyType[WzPropertyType["Vector"] = 9] = "Vector";
    WzPropertyType[WzPropertyType["Convex"] = 10] = "Convex";
    WzPropertyType[WzPropertyType["Sound"] = 11] = "Sound";
    WzPropertyType[WzPropertyType["UOL"] = 12] = "UOL";
    WzPropertyType[WzPropertyType["Lua"] = 13] = "Lua";
    // #endregion
    // #region Png
    WzPropertyType[WzPropertyType["PNG"] = 14] = "PNG";
    // #endregion
})(WzPropertyType || (WzPropertyType = {}));


/***/ }),
/* 2 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzImageProperty; });
/* harmony import */ var _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(5);
/* harmony import */ var _WzImage_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(4);
/* harmony import */ var _WzObject_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(12);
/* harmony import */ var _WzObjectType_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(8);




/**
 * @public
 */
class WzImageProperty extends _WzObject_js__WEBPACK_IMPORTED_MODULE_2__[/* WzObject */ "a"] {
    /**
     * @virtual
     */
    get wzProperties() {
        return null;
    }
    /**
     * @virtual
     */
    getFromPath(_path) {
        return null;
    }
    /**
     * @virtual
     */
    at(_name) {
        return null;
    }
    /**
     * @virtual
     */
    set(_name, _value) {
        throw new _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_0__[/* NotImplementedError */ "a"]('[WzImageProperty#set]');
    }
    get objectType() {
        return _WzObjectType_js__WEBPACK_IMPORTED_MODULE_3__[/* WzObjectType */ "a"].Property;
    }
    get parentImage() {
        let parent = this.parent;
        while (parent != null) {
            if (parent instanceof _WzImage_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImage */ "a"])
                return parent;
            else
                parent = parent.parent;
        }
        return null;
    }
    get wzFileParent() {
        if (this.parentImage != null) {
            return this.parentImage.wzFileParent;
        }
        return null;
    }
    static async parseLuaProperty(_offset, reader, parent, _parentImg) {
        // 28 71 4F EF 1B 65 F9 1F A7 48 8D 11 73 E7 F0 27 55 09 DD 3C 07 32 D7 38 21 57 84 70 C1 79 9A 3F 49 F7 79 03 41 F4 9D B9 1B 5F CF 26 80 3D EC 25 5F 9C
        // [compressed int] [bytes]
        const length = await reader.readWzInt();
        const rawEncBytes = await reader.read(length);
        const WzLuaProperty = __webpack_require__(24).WzLuaProperty;
        const lua = new WzLuaProperty('Script', rawEncBytes);
        lua.parent = parent;
        return lua;
    }
    static async parsePropertyList(offset, reader, parent, parentImg) {
        const entryCount = await reader.readWzInt();
        const properties = new Set( /* entryCount */);
        for (let i = 0; i < entryCount; i++) {
            const name = await reader.readStringBlock(offset);
            const ptype = await reader.readUInt8();
            switch (ptype) {
                case 0: {
                    const WzNullProperty = __webpack_require__(25).WzNullProperty;
                    const p = new WzNullProperty(name);
                    p.parent = parent;
                    properties.add(p);
                    break;
                }
                case 11:
                case 2: {
                    const WzShortProperty = __webpack_require__(26).WzShortProperty;
                    const p = new WzShortProperty(name, await reader.readInt16LE());
                    p.parent = parent;
                    properties.add(p);
                    break;
                }
                case 3:
                case 19: {
                    const WzIntProperty = __webpack_require__(16).WzIntProperty;
                    const p = new WzIntProperty(name, await reader.readWzInt());
                    p.parent = parent;
                    properties.add(p);
                    break;
                }
                case 20: {
                    const WzLongProperty = __webpack_require__(27).WzLongProperty;
                    const p = new WzLongProperty(name, await reader.readWzLong());
                    p.parent = parent;
                    properties.add(p);
                    break;
                }
                case 4: {
                    const WzFloatProperty = __webpack_require__(28).WzFloatProperty;
                    const type = await reader.readUInt8();
                    let p;
                    if (type === 0x80) {
                        p = new WzFloatProperty(name, await reader.readFloatLE());
                        p.parent = parent;
                        properties.add(p);
                    }
                    else if (type === 0) {
                        p = new WzFloatProperty(name, 0);
                        p.parent = parent;
                        properties.add(p);
                    }
                    break;
                }
                case 5: {
                    const WzDoubleProperty = __webpack_require__(29).WzDoubleProperty;
                    const p = new WzDoubleProperty(name, await reader.readDoubleLE());
                    p.parent = parent;
                    properties.add(p);
                    break;
                }
                case 8: {
                    const WzStringProperty = __webpack_require__(30).WzStringProperty;
                    const p = new WzStringProperty(name, await reader.readStringBlock(offset));
                    p.parent = parent;
                    properties.add(p);
                    break;
                }
                case 9: {
                    const eob = (await reader.readUInt32LE()) + reader.pos;
                    const exProp = await WzImageProperty.parseExtendedProp(reader, offset, eob, name, parent, parentImg);
                    properties.add(exProp);
                    if (reader.pos !== eob) {
                        reader.pos = eob;
                    }
                    break;
                }
                default:
                    throw new Error(`Unknown property type at ParsePropertyList, ptype = ${ptype}`);
            }
        }
        return properties;
    }
    static async parseExtendedProp(reader, offset, endOfBlock, name, parent, imgParent) {
        const type = await reader.readUInt8();
        switch (type) {
            case 0x01:
            case _WzImage_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImage */ "a"].WzImageHeaderByte_WithOffset:
                return await WzImageProperty.extractMore(reader, offset, endOfBlock, name, await reader.readWzStringAtOffset(offset + await reader.readInt32LE()), parent, imgParent);
            case 0x00:
            case _WzImage_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImage */ "a"].WzImageHeaderByte_WithoutOffset:
                return await WzImageProperty.extractMore(reader, offset, endOfBlock, name, '', parent, imgParent);
            default:
                throw new Error('Invalid byte read at ParseExtendedProp');
        }
    }
    static async extractMore(reader, offset, _eob, name, iname, parent, imgParent) {
        if (iname === '') {
            iname = await reader.readWzString();
        }
        switch (iname) {
            case 'Property': {
                const WzSubProperty = __webpack_require__(31).WzSubProperty;
                const subProp = new WzSubProperty(name);
                subProp.parent = parent;
                reader.pos += 2; // Reserved?
                subProp.addProperties(await WzImageProperty.parsePropertyList(offset, reader, subProp, imgParent));
                return subProp;
            }
            case 'Canvas': {
                const WzCanvasProperty = __webpack_require__(32).WzCanvasProperty;
                const canvasProp = new WzCanvasProperty(name);
                canvasProp.parent = parent;
                reader.pos++;
                const b = await reader.readUInt8();
                if (b === 1) {
                    reader.pos += 2;
                    canvasProp.addProperties(await WzImageProperty.parsePropertyList(offset, reader, canvasProp, imgParent));
                }
                const WzPngProperty = __webpack_require__(36).WzPngProperty;
                canvasProp.pngProperty = await WzPngProperty.create(reader /* , imgParent.parseEverything */);
                canvasProp.pngProperty.parent = canvasProp;
                return canvasProp;
            }
            case 'Shape2D#Vector2D': {
                const WzVectorProperty = __webpack_require__(33).WzVectorProperty;
                const WzIntProperty = __webpack_require__(16).WzIntProperty;
                const vecProp = new WzVectorProperty(name);
                vecProp.parent = parent;
                vecProp.x = new WzIntProperty('x', await reader.readWzInt());
                vecProp.x.parent = vecProp;
                vecProp.y = new WzIntProperty('y', await reader.readWzInt());
                vecProp.y.parent = vecProp;
                return vecProp;
            }
            case 'Shape2D#Convex2D': {
                const WzConvexProperty = __webpack_require__(34).WzConvexProperty;
                const convexProp = new WzConvexProperty(name);
                convexProp.parent = parent;
                const convexEntryCount = await reader.readWzInt();
                // convexProp.wzProperties.capacity = convexEntryCount
                for (let i = 0; i < convexEntryCount; i++) {
                    convexProp.addProperty(await WzImageProperty.parseExtendedProp(reader, offset, 0, name, convexProp, imgParent));
                }
                return convexProp;
            }
            case 'Sound_DX8': {
                const WzBinaryProperty = __webpack_require__(35).WzBinaryProperty;
                const soundProp = await WzBinaryProperty.create(name, reader, imgParent.parseEverything);
                soundProp.parent = parent;
                return soundProp;
            }
            case 'UOL': {
                const WzUOLProperty = __webpack_require__(17).WzUOLProperty;
                reader.pos++;
                const t = await reader.readUInt8();
                switch (t) {
                    case 0: {
                        const p = new WzUOLProperty(name, await reader.readWzString());
                        p.parent = parent;
                        return p;
                    }
                    case 1: {
                        const p = new WzUOLProperty(name, await reader.readWzStringAtOffset(offset + await reader.readInt32LE()));
                        p.parent = parent;
                        return p;
                    }
                }
                throw new Error('Unsupported UOL type');
            }
            default:
                throw new Error('Unknown iname: ' + iname);
        }
    }
    getLinkedWzImageProperty() {
        let thisWzImage = this;
        const WzUOLProperty = __webpack_require__(17).WzUOLProperty;
        while ((thisWzImage instanceof WzUOLProperty)) {
            if (thisWzImage.linkValue instanceof WzImageProperty) {
                thisWzImage = thisWzImage.linkValue;
            }
            else {
                return this;
            }
        }
        return thisWzImage;
    }
}


/***/ }),
/* 3 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return ErrorLevel; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return WzError; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return ErrorLogger; });
/* harmony import */ var _util_node_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(0);
/* harmony import */ var _BaseError_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(18);
/* harmony import */ var _NotImplementedError_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(5);



const EOL = _util_node_js__WEBPACK_IMPORTED_MODULE_0__[/* os */ "f"].EOL;
/** @public */
var ErrorLevel;
(function (ErrorLevel) {
    ErrorLevel[ErrorLevel["MissingFeature"] = 0] = "MissingFeature";
    ErrorLevel[ErrorLevel["IncorrectStructure"] = 1] = "IncorrectStructure";
    ErrorLevel[ErrorLevel["Critical"] = 2] = "Critical";
    ErrorLevel[ErrorLevel["Crash"] = 3] = "Crash";
})(ErrorLevel || (ErrorLevel = {}));
/** @public */
class WzError extends _BaseError_js__WEBPACK_IMPORTED_MODULE_1__[/* BaseError */ "a"] {
    constructor(level, message) {
        super(message);
        this.level = level;
    }
}
/** @public */
class ErrorLogger {
    static log(level, message) {
        this._errorList.add(new WzError(level, message));
    }
    static numberOfErrorsPresent() {
        return this._errorList.size;
    }
    static errorsPresent() {
        return this._errorList.size > 0;
    }
    static clearErrors() {
        this._errorList.clear();
    }
    static saveToFile(file) {
        if (typeof window !== 'undefined') {
            throw new _NotImplementedError_js__WEBPACK_IMPORTED_MODULE_2__[/* NotImplementedError */ "a"]('Can not save to file in browser');
        }
        try {
            _util_node_js__WEBPACK_IMPORTED_MODULE_0__[/* fs */ "e"].mkdirSync(_util_node_js__WEBPACK_IMPORTED_MODULE_0__[/* path */ "g"].dirname(file), { recursive: true });
        }
        catch (_) { }
        const fd = _util_node_js__WEBPACK_IMPORTED_MODULE_0__[/* fs */ "e"].openSync(file, 'w');
        _util_node_js__WEBPACK_IMPORTED_MODULE_0__[/* fs */ "e"].writeSync(fd, `Starting error log on ${new Date().toLocaleString()}${EOL}`);
        for (const err of this._errorList) {
            _util_node_js__WEBPACK_IMPORTED_MODULE_0__[/* fs */ "e"].writeSync(fd, `[${ErrorLevel[err.level]}] ${err.message}${EOL}`);
        }
        _util_node_js__WEBPACK_IMPORTED_MODULE_0__[/* fs */ "e"].closeSync(fd);
    }
}
ErrorLogger._errorList = new Set();


/***/ }),
/* 4 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzImage; });
/* harmony import */ var _util_ErrorLogger_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(3);
/* harmony import */ var _util_WzBinaryReader_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(19);
/* harmony import */ var _util_WzTool_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(21);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(2);
/* harmony import */ var _WzObject_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(12);
/* harmony import */ var _WzObjectType_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(8);
/* harmony import */ var _util_node_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(0);







/**
 * @public
 */
class WzImage extends _WzObject_js__WEBPACK_IMPORTED_MODULE_4__[/* WzObject */ "a"] {
    constructor(name, reader, checksum = 0) {
        super();
        this.parent = null;
        this.name = '';
        this.blockSize = 0;
        this.checksum = 0;
        this.offset = 0;
        this.tempFileStart = BigInt(0);
        this.tempFileEnd = BigInt(0);
        this.changed = false;
        this.parseEverything = false;
        this.parsed = false;
        this.properties = new Set();
        this.name = name;
        this.reader = reader;
        this.blockStart = reader.pos;
        this.checksum = checksum;
    }
    get isLuaImage() {
        return this.name.endsWith('.lua');
    }
    get wzProperties() {
        if (!this.parsed)
            throw new Error('Image has not been parsed yet');
        return this.properties;
    }
    get objectType() {
        // if (!this.parsed) this.parseImage()
        return _WzObjectType_js__WEBPACK_IMPORTED_MODULE_5__[/* WzObjectType */ "a"].Image;
    }
    static createFromFile(filepath, version) {
        const wzIv = Object(_util_WzTool_js__WEBPACK_IMPORTED_MODULE_2__[/* getIvByMapleVersion */ "a"])(version);
        const reader = new _util_WzBinaryReader_js__WEBPACK_IMPORTED_MODULE_1__[/* WzBinaryReader */ "a"](filepath, wzIv);
        let img;
        if (typeof filepath === 'string') {
            img = new WzImage(_util_node_js__WEBPACK_IMPORTED_MODULE_6__[/* path */ "g"].basename(filepath), reader);
            img.blockSize = reader.size;
        }
        else {
            img = new WzImage(filepath.name, reader);
            img.blockSize = filepath.size;
        }
        return img;
    }
    addProperty(prop) {
        if (this.reader != null && !this.parsed)
            throw new Error('Image has not been parsed yet');
        prop.parent = this;
        this.properties.add(prop);
    }
    removeProperty(prop) {
        if (!this.parsed)
            throw new Error('Image has not been parsed yet');
        prop.parent = null;
        this.properties.delete(prop);
    }
    addProperties(props) {
        for (const prop of props) {
            this.addProperty(prop);
        }
    }
    clearProperties() {
        for (const prop of this.properties) {
            prop.parent = null;
        }
        this.properties.clear();
    }
    set(name, value) {
        if (value != null) {
            value.name = name;
            this.addProperty(value);
        }
    }
    dispose() {
        if (this._disposed)
            return;
        for (const prop of this.properties) {
            prop.dispose();
        }
        this.properties.clear();
        if (this.parent == null) {
            this.reader.dispose();
            this.reader = null;
        }
        this._disposed = true;
    }
    at(name) {
        if (!this.parsed)
            throw new Error('Image has not been parsed yet');
        const nameLower = name.toLowerCase();
        for (const prop of this.properties) {
            if (prop.name.toLowerCase() === nameLower)
                return prop;
        }
        return null;
    }
    get wzFileParent() {
        if (this.parent != null) {
            return this.parent.wzFileParent;
        }
        return null;
    }
    getFromPath(path) {
        if (!this.parsed)
            throw new Error('Image has not been parsed yet');
        const segments = path.split(/[\\/]/);
        if (segments[0] === '..')
            return null;
        let ret = null;
        for (let x = 0; x < segments.length; x++) {
            let foundChild = false;
            const list = (ret == null ? this.properties : ret.wzProperties);
            if (list != null) {
                const l = list;
                for (const iwp of l) {
                    if (iwp.name === segments[x]) {
                        ret = iwp;
                        foundChild = true;
                        break;
                    }
                }
            }
            if (!foundChild) {
                return null;
            }
        }
        return ret;
    }
    calculateAndSetImageChecksum(bytes) {
        this.checksum = 0;
        for (let i = 0; i < bytes.length; i++) {
            this.checksum += bytes[i];
        }
    }
    async parseImage(forceReadFromData = false) {
        if (!forceReadFromData) { // only check if parsed or changed if its not false read
            if (this.parsed) {
                return true;
            }
            else if (this.changed) {
                this.parsed = true;
                return true;
            }
        }
        const reader = this.reader;
        // const originalPos = reader.pos
        reader.pos = this.offset;
        const b = await reader.readUInt8();
        switch (b) {
            case 0x1: {
                if (this.isLuaImage) {
                    const lua = await _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_3__[/* WzImageProperty */ "a"].parseLuaProperty(this.offset, reader, this, this);
                    this.properties.add(lua);
                    this.parsed = true;
                    return true;
                }
                return false;
            }
            case WzImage.WzImageHeaderByte_WithoutOffset: {
                const prop = await reader.readWzString();
                const val = await reader.readUInt16LE();
                if (prop !== 'Property' || val !== 0) {
                    return false;
                }
                break;
            }
            default: {
                _util_ErrorLogger_js__WEBPACK_IMPORTED_MODULE_0__[/* ErrorLogger */ "b"].log(_util_ErrorLogger_js__WEBPACK_IMPORTED_MODULE_0__[/* ErrorLevel */ "a"].MissingFeature, `[WzImage] New Wz image header found. b = ${b}`);
                return false;
            }
        }
        const images = await _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_3__[/* WzImageProperty */ "a"].parsePropertyList(this.offset, reader, this, this);
        for (const img of images) {
            this.properties.add(img);
        }
        this.parsed = true;
        return true;
    }
}
WzImage.WzImageHeaderByte_WithoutOffset = 0x73;
WzImage.WzImageHeaderByte_WithOffset = 0x1B;


/***/ }),
/* 5 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return NotImplementedError; });
/* harmony import */ var _BaseError_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(18);

/**
 * @public
 */
class NotImplementedError extends _BaseError_js__WEBPACK_IMPORTED_MODULE_0__[/* BaseError */ "a"] {
    constructor(where) {
        super(`${typeof where === 'string' ? (where + ' ') : ''}Not implemented`);
    }
}


/***/ }),
/* 6 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzExtended; });
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(2);

/**
 * @public
 */
class WzExtended extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_0__[/* WzImageProperty */ "a"] {
}


/***/ }),
/* 7 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return Canvas; });
/* harmony import */ var _node_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(0);

/**
 * @public
 */
class Canvas {
    constructor(width, height) {
        if (typeof window === 'undefined') {
            this._canvas = new _node_js__WEBPACK_IMPORTED_MODULE_0__[/* Jimp */ "a"](width, height);
        }
        else {
            this._canvas = window.document.createElement('canvas');
            this._canvas.width = width;
            this._canvas.height = height;
        }
    }
    static rgbaToInt(r, g, b, a) {
        if (typeof window === 'undefined') {
            return _node_js__WEBPACK_IMPORTED_MODULE_0__[/* Jimp */ "a"].rgbaToInt(r, g, b, a);
        }
        if (typeof r !== 'number' || typeof g !== 'number' || typeof b !== 'number' || typeof a !== 'number') {
            throw new TypeError('r, g, b and a must be numbers');
        }
        if (r < 0 || r > 255) {
            throw new RangeError('r must be between 0 and 255');
        }
        if (g < 0 || g > 255) {
            throw new RangeError('g must be between 0 and 255');
        }
        if (b < 0 || b > 255) {
            throw new RangeError('b must be between 0 and 255');
        }
        if (a < 0 || a > 255) {
            throw new RangeError('a must be between 0 and 255');
        }
        r = Math.round(r);
        b = Math.round(b);
        g = Math.round(g);
        a = Math.round(a);
        const i = r * Math.pow(256, 3) + g * Math.pow(256, 2) + b * Math.pow(256, 1) + a * Math.pow(256, 0);
        return i;
    }
    setPixelColor(rgba, x, y) {
        if (typeof window === 'undefined') {
            this._canvas.setPixelColor(rgba, x, y);
            return this;
        }
        const context = this._canvas.getContext('2d');
        const imageData = context.createImageData(1, 1);
        imageData.data[0] = rgba >>> 24;
        imageData.data[1] = (rgba >>> 16) & 0xff;
        imageData.data[2] = (rgba >>> 8) & 0xff;
        imageData.data[3] = rgba & 0xff;
        context.putImageData(imageData, x, y);
        return this;
    }
    getWidth() {
        if (typeof window === 'undefined') {
            return this._canvas.getWidth();
        }
        return this._canvas.width;
    }
    getCanvas() {
        return this._canvas;
    }
    getBufferAsync(mime) {
        if (typeof window === 'undefined') {
            return this._canvas.getBufferAsync(mime);
        }
        else {
            return new Promise((resolve, reject) => {
                toBlob(this._canvas, mime).then(blob => {
                    if (blob == null) {
                        reject(new Error('getBufferAsync() failed'));
                        return;
                    }
                    const fr = new FileReader();
                    fr.addEventListener('error', () => { reject(fr.error); });
                    fr.addEventListener('load', () => {
                        const readBuffer = new Uint8Array(fr.result);
                        resolve(readBuffer);
                    });
                    fr.readAsArrayBuffer(blob);
                }).catch(reject);
            });
        }
    }
    async writeAsync(file) {
        if (typeof window === 'undefined') {
            await this._canvas.writeAsync(file);
            return this;
        }
        const blob = await toBlob(this._canvas);
        if (blob == null) {
            throw new Error('Write image failed');
        }
        const a = window.document.createElement('a');
        a.download = file;
        a.href = URL.createObjectURL(blob);
        const event = new MouseEvent('click', {
            view: window,
            bubbles: true,
            cancelable: true
        });
        a.dispatchEvent(event);
        a.remove();
        return this;
    }
    dispose() {
        this._canvas = null;
    }
}
function toBlob(canvas, type = 'image/png') {
    return new Promise((resolve) => {
        canvas.toBlob((blob) => {
            resolve(blob);
        }, type);
    });
}


/***/ }),
/* 8 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzObjectType; });
/**
 * @public
 */
var WzObjectType;
(function (WzObjectType) {
    WzObjectType[WzObjectType["File"] = 0] = "File";
    WzObjectType[WzObjectType["Image"] = 1] = "Image";
    WzObjectType[WzObjectType["Directory"] = 2] = "Directory";
    WzObjectType[WzObjectType["Property"] = 3] = "Property";
    WzObjectType[WzObjectType["List"] = 4] = "List";
})(WzObjectType || (WzObjectType = {}));


/***/ }),
/* 9 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";

// EXPORTS
__webpack_require__.d(__webpack_exports__, "b", function() { return /* reexport */ BinaryReader_BinaryReader; });
__webpack_require__.d(__webpack_exports__, "a", function() { return /* reexport */ AsyncBinaryReader_AsyncBinaryReader; });

// UNUSED EXPORTS: EndianType, FileDescriptor, Reader

// NAMESPACE OBJECT: ./node_modules/@tybys/binreader/lib/esm-modern/buffer.js
var buffer_namespaceObject = {};
__webpack_require__.r(buffer_namespaceObject);
__webpack_require__.d(buffer_namespaceObject, "validateBigInt", function() { return validateBigInt; });
__webpack_require__.d(buffer_namespaceObject, "readBigUInt64LE", function() { return readBigUInt64LE; });
__webpack_require__.d(buffer_namespaceObject, "readBigUInt64BE", function() { return readBigUInt64BE; });
__webpack_require__.d(buffer_namespaceObject, "readBigInt64LE", function() { return readBigInt64LE; });
__webpack_require__.d(buffer_namespaceObject, "readBigInt64BE", function() { return readBigInt64BE; });
__webpack_require__.d(buffer_namespaceObject, "readUInt32LE", function() { return readUInt32LE; });
__webpack_require__.d(buffer_namespaceObject, "readUInt16LE", function() { return readUInt16LE; });
__webpack_require__.d(buffer_namespaceObject, "readUInt8", function() { return readUInt8; });
__webpack_require__.d(buffer_namespaceObject, "readUInt32BE", function() { return readUInt32BE; });
__webpack_require__.d(buffer_namespaceObject, "readUInt16BE", function() { return readUInt16BE; });
__webpack_require__.d(buffer_namespaceObject, "readInt32LE", function() { return readInt32LE; });
__webpack_require__.d(buffer_namespaceObject, "readInt16LE", function() { return readInt16LE; });
__webpack_require__.d(buffer_namespaceObject, "readInt8", function() { return readInt8; });
__webpack_require__.d(buffer_namespaceObject, "readInt32BE", function() { return readInt32BE; });
__webpack_require__.d(buffer_namespaceObject, "readInt16BE", function() { return readInt16BE; });
__webpack_require__.d(buffer_namespaceObject, "readFloatBackwards", function() { return readFloatBackwards; });
__webpack_require__.d(buffer_namespaceObject, "readFloatForwards", function() { return readFloatForwards; });
__webpack_require__.d(buffer_namespaceObject, "readFloatLE", function() { return readFloatLE; });
__webpack_require__.d(buffer_namespaceObject, "readFloatBE", function() { return readFloatBE; });
__webpack_require__.d(buffer_namespaceObject, "readDoubleLE", function() { return readDoubleLE; });
__webpack_require__.d(buffer_namespaceObject, "readDoubleBE", function() { return readDoubleBE; });
__webpack_require__.d(buffer_namespaceObject, "copy", function() { return copy; });
__webpack_require__.d(buffer_namespaceObject, "bufferToString", function() { return bufferToString; });
__webpack_require__.d(buffer_namespaceObject, "methods", function() { return methods; });

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/EndianType.js
/** @public */
var EndianType;
(function (EndianType) {
    EndianType[EndianType["LittleEndian"] = 0] = "LittleEndian";
    EndianType[EndianType["BigEndian"] = 1] = "BigEndian";
})(EndianType || (EndianType = {}));

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/FileDescriptor.js
/** @public */
class FileDescriptor {
    constructor(fd, size, path) {
        this.fd = fd;
        this.size = size;
        this.path = path;
    }
}

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/Reader.js
function checkRange(pos, size) {
    if (pos < 0 || pos > size) {
        throw new RangeError(`Position (${pos}) out of range: [0, ${size}]`);
    }
}
/** @public */
class Reader {
    constructor(size = 0) {
        /**
         * Throws RangeError if position is out of range when set this to `true`
         */
        this.strictRangeChecking = false;
        size = size < 0 ? 0 : size;
        this._size = size;
        let pos = 0;
        Object.defineProperty(this, 'pos', {
            configurable: true,
            enumerable: true,
            get: () => pos,
            set: (v) => {
                if (typeof v !== 'number' || Number.isNaN(v)) {
                    throw new TypeError('Invalid position');
                }
                if (this.strictRangeChecking) {
                    checkRange(v, this._size);
                }
                pos = v;
                // let message: string
                // if (v < 0) {
                //   message = `Position out of range: ${v} < 0`
                //   if (!this.strictRangeChecking) {
                //     console.warn(`${message}, set position to 0`)
                //     pos = 0
                //     return
                //   }
                //   throw new RangeError(message)
                // }
                // if (v > this._size) {
                //   message = `Position out of range: ${v} > ${this._size}`
                //   if (!this.strictRangeChecking) {
                //     console.warn(`${message}, set position to the end`)
                //     pos = this._size
                //     return
                //   }
                //   throw new RangeError(message)
                // }
                // pos = v
            }
        });
    }
    get size() {
        return this._size;
    }
    /**
     * Set current position.
     * @param pos - Target position
     */
    seek(pos) {
        this.pos = pos;
    }
    /**
     * Get current position
     */
    tell() {
        return this.pos;
    }
    /**
     * Destructor.
     * @virtual
     */
    dispose() {
        this.close();
    }
}

// EXTERNAL MODULE: ./node_modules/@tybys/native-require/index.esm.js
var index_esm = __webpack_require__(23);

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/node.js

const _require = Object(index_esm["a" /* tryGetRequireFunction */])();
const fs = (function () {
    try {
        return _require('fs');
    }
    catch (_) {
        const message = 'Node.js API is not supported';
        return {
            readSync() { throw new Error(message); },
            statSync() { throw new Error(message); },
            openSync() { throw new Error(message); },
            closeSync() { },
            read(_fd, ...args) {
                // eslint-disable-next-line @typescript-eslint/no-floating-promises
                Promise.resolve().then(() => {
                    let callback = null;
                    for (let i = 0; i < args.length; i++) {
                        if (typeof args[i] === 'function') {
                            callback = args[i];
                            break;
                        }
                    }
                    if (callback !== null) {
                        callback(new Error(message));
                    }
                });
            },
            promises: {
                read() {
                    return Promise.reject(new Error(message));
                }
            }
        };
    }
})();
const _Buffer = (function () {
    try {
        return _require('buffer').Buffer;
    }
    catch (_) {
        return null;
    }
})();

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/buffer.js
/* eslint-disable @typescript-eslint/prefer-nullish-coalescing */
/* eslint-disable @typescript-eslint/strict-boolean-expressions */

const float32Array = new Float32Array(1);
const uInt8Float32Array = new Uint8Array(float32Array.buffer);
const float64Array = new Float64Array(1);
const uInt8Float64Array = new Uint8Array(float64Array.buffer);
float32Array[0] = -1; // 0xBF800000
const bigEndian = uInt8Float32Array[3] === 0;
function validateBigInt() {
    if (typeof BigInt !== 'function')
        throw new Error('BigInt is not available');
}
function validateNumber(value, name) {
    if (typeof value !== 'number') {
        throw new TypeError(`${name || 'offset'} should be a number`);
    }
}
function boundsError(value, length, type) {
    if (Math.floor(value) !== value) {
        validateNumber(value, type);
        throw new RangeError(`${type || 'offset'} should be an integer`);
    }
    if (length < 0) {
        throw new RangeError('Attempt to access memory outside buffer bounds');
    }
    throw new RangeError(`${type || 'offset'} should be >= ${type ? 1 : 0} and <= ${length}`);
}
function readBigUInt64LE(buf, offset = 0) {
    validateBigInt();
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 7];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 8);
    }
    const lo = first +
        buf[++offset] * 2 ** 8 +
        buf[++offset] * 2 ** 16 +
        buf[++offset] * 2 ** 24;
    const hi = buf[++offset] +
        buf[++offset] * 2 ** 8 +
        buf[++offset] * 2 ** 16 +
        last * 2 ** 24;
    return BigInt(lo) + (BigInt(hi) << BigInt(32));
}
function readBigUInt64BE(buf, offset = 0) {
    validateBigInt();
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 7];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 8);
    }
    const hi = first * 2 ** 24 +
        buf[++offset] * 2 ** 16 +
        buf[++offset] * 2 ** 8 +
        buf[++offset];
    const lo = buf[++offset] * 2 ** 24 +
        buf[++offset] * 2 ** 16 +
        buf[++offset] * 2 ** 8 +
        last;
    return (BigInt(hi) << BigInt(32)) + BigInt(lo);
}
function readBigInt64LE(buf, offset = 0) {
    validateBigInt();
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 7];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 8);
    }
    const val = buf[offset + 4] +
        buf[offset + 5] * 2 ** 8 +
        buf[offset + 6] * 2 ** 16 +
        (last << 24); // Overflow
    return (BigInt(val) << BigInt(32)) +
        BigInt(first +
            buf[++offset] * 2 ** 8 +
            buf[++offset] * 2 ** 16 +
            buf[++offset] * 2 ** 24);
}
function readBigInt64BE(buf, offset = 0) {
    validateBigInt();
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 7];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 8);
    }
    const val = (first << 24) + // Overflow
        buf[++offset] * 2 ** 16 +
        buf[++offset] * 2 ** 8 +
        buf[++offset];
    return (BigInt(val) << BigInt(32)) +
        BigInt(buf[++offset] * 2 ** 24 +
            buf[++offset] * 2 ** 16 +
            buf[++offset] * 2 ** 8 +
            last);
}
function readUInt32LE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 3];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 4);
    }
    return first +
        buf[++offset] * 2 ** 8 +
        buf[++offset] * 2 ** 16 +
        last * 2 ** 24;
}
function readUInt16LE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 1];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 2);
    }
    return first + last * 2 ** 8;
}
function readUInt8(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const val = buf[offset];
    if (val === undefined) {
        boundsError(offset, buf.length - 1);
    }
    return val;
}
function readUInt32BE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 3];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 4);
    }
    return first * 2 ** 24 +
        buf[++offset] * 2 ** 16 +
        buf[++offset] * 2 ** 8 +
        last;
}
function readUInt16BE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 1];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 2);
    }
    return first * 2 ** 8 + last;
}
function readInt32LE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 3];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 4);
    }
    return first +
        buf[++offset] * 2 ** 8 +
        buf[++offset] * 2 ** 16 +
        (last << 24); // Overflow
}
function readInt16LE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 1];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 2);
    }
    const val = first + last * 2 ** 8;
    return val | (val & 2 ** 15) * 0x1fffe;
}
function readInt8(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const val = buf[offset];
    if (val === undefined) {
        boundsError(offset, buf.length - 1);
    }
    return val | (val & 2 ** 7) * 0x1fffffe;
}
function readInt32BE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 3];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 4);
    }
    return (first << 24) + // Overflow
        buf[++offset] * 2 ** 16 +
        buf[++offset] * 2 ** 8 +
        last;
}
function readInt16BE(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 1];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 2);
    }
    const val = first * 2 ** 8 + last;
    return val | (val & 2 ** 15) * 0x1fffe;
}
// Read floats
function readFloatBackwards(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 3];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 4);
    }
    uInt8Float32Array[3] = first;
    uInt8Float32Array[2] = buf[++offset];
    uInt8Float32Array[1] = buf[++offset];
    uInt8Float32Array[0] = last;
    return float32Array[0];
}
function readFloatForwards(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 3];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 4);
    }
    uInt8Float32Array[0] = first;
    uInt8Float32Array[1] = buf[++offset];
    uInt8Float32Array[2] = buf[++offset];
    uInt8Float32Array[3] = last;
    return float32Array[0];
}
function readDoubleBackwards(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 7];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 8);
    }
    uInt8Float64Array[7] = first;
    uInt8Float64Array[6] = buf[++offset];
    uInt8Float64Array[5] = buf[++offset];
    uInt8Float64Array[4] = buf[++offset];
    uInt8Float64Array[3] = buf[++offset];
    uInt8Float64Array[2] = buf[++offset];
    uInt8Float64Array[1] = buf[++offset];
    uInt8Float64Array[0] = last;
    return float64Array[0];
}
function readDoubleForwards(buf, offset = 0) {
    validateNumber(offset, 'offset');
    const first = buf[offset];
    const last = buf[offset + 7];
    if (first === undefined || last === undefined) {
        boundsError(offset, buf.length - 8);
    }
    uInt8Float64Array[0] = first;
    uInt8Float64Array[1] = buf[++offset];
    uInt8Float64Array[2] = buf[++offset];
    uInt8Float64Array[3] = buf[++offset];
    uInt8Float64Array[4] = buf[++offset];
    uInt8Float64Array[5] = buf[++offset];
    uInt8Float64Array[6] = buf[++offset];
    uInt8Float64Array[7] = last;
    return float64Array[0];
}
const readFloatLE = bigEndian ? readFloatBackwards : readFloatForwards;
const readFloatBE = bigEndian ? readFloatForwards : readFloatBackwards;
const readDoubleLE = bigEndian ? readDoubleBackwards : readDoubleForwards;
const readDoubleBE = bigEndian ? readDoubleForwards : readDoubleBackwards;
function copy(source, target, targetStart = 0, sourceStart = 0, sourceEnd = source.length) {
    if (!(source instanceof Uint8Array)) {
        throw new TypeError('source shoule be a Uint8Array');
    }
    if (!(target instanceof Uint8Array)) {
        throw new TypeError('target shoule be a Uint8Array');
    }
    if (targetStart === undefined) {
        targetStart = 0;
    }
    else {
        targetStart = toInteger(targetStart, 0);
        if (targetStart < 0) {
            throw new RangeError('targetStart should be >= 0');
        }
    }
    if (sourceStart === undefined) {
        sourceStart = 0;
    }
    else {
        sourceStart = toInteger(sourceStart, 0);
        if (sourceStart < 0) {
            throw new RangeError('sourceStart should be >= 0');
        }
    }
    if (sourceEnd === undefined) {
        sourceEnd = source.length;
    }
    else {
        sourceEnd = toInteger(sourceEnd, 0);
        if (sourceEnd < 0) {
            throw new RangeError('sourceEnd should be >= 0');
        }
    }
    if (targetStart >= target.length || sourceStart >= sourceEnd) {
        return 0;
    }
    if (sourceStart > source.length) {
        throw new RangeError(`sourceStart should be <= ${source.length}`);
    }
    return _copyActual(source, target, targetStart, sourceStart, sourceEnd);
}
function _copyActual(source, target, targetStart, sourceStart, sourceEnd) {
    if (sourceEnd - sourceStart > target.length - targetStart) {
        sourceEnd = sourceStart + target.length - targetStart;
    }
    let nb = sourceEnd - sourceStart;
    const targetLen = target.length - targetStart;
    const sourceLen = source.length - sourceStart;
    if (nb > targetLen) {
        nb = targetLen;
    }
    if (nb > sourceLen) {
        nb = sourceLen;
    }
    if (sourceStart !== 0 || sourceEnd < source.length) {
        source = new Uint8Array(source.buffer, source.byteOffset + sourceStart, nb);
    }
    target.set(source, targetStart);
    return nb;
}
function toInteger(n, defaultVal) {
    n = +n;
    if (!Number.isNaN(n) &&
        n >= Number.MIN_SAFE_INTEGER &&
        n <= Number.MAX_SAFE_INTEGER) {
        return ((n % 1) === 0 ? n : Math.floor(n));
    }
    return defaultVal;
}
function bufferToString(buf, encoding, start, end) {
    if (_Buffer !== null) {
        return _Buffer.from(buf).toString(encoding, start, end);
    }
    if (arguments.length === 0) {
        const decoder = new TextDecoder('utf-8');
        return decoder.decode(buf.subarray(0, buf.length));
    }
    const len = buf.length;
    if (start <= 0) {
        start = 0;
    }
    else if (start >= len) {
        return '';
    }
    else {
        start |= 0;
    }
    if (end === undefined || end > len) {
        end = len;
    }
    else {
        end |= 0;
    }
    if (end <= start) {
        return '';
    }
    if (encoding === undefined) {
        const decoder = new TextDecoder('utf-8');
        return decoder.decode(buf.subarray(start, end));
    }
    const decoder = new TextDecoder(encoding);
    return decoder.decode(buf.subarray(start, end));
}
const methods = {
    readInt8: 1,
    readUInt8: 1,
    readInt16LE: 2,
    readUInt16LE: 2,
    readInt16BE: 2,
    readUInt16BE: 2,
    readInt32LE: 4,
    readUInt32LE: 4,
    readInt32BE: 4,
    readUInt32BE: 4,
    readBigInt64LE: 8,
    readBigUInt64LE: 8,
    readBigInt64BE: 8,
    readBigUInt64BE: 8,
    readFloatLE: 4,
    readFloatBE: 4,
    readDoubleLE: 8,
    readDoubleBE: 8
};

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/BinaryReader.js





const BinaryReader_methods = methods;
function readNumber(reader, method) {
    checkRange(reader.pos, reader.size);
    if (method === 'readBigInt64BE' || method === 'readBigInt64LE' || method === 'readBigUInt64BE' || method === 'readBigUInt64LE') {
        validateBigInt();
    }
    const buf = new Uint8Array(BinaryReader_methods[method]);
    let readLength;
    if (reader.type === 0 /* FILE */) {
        readLength = fs.readSync(reader._file, buf, 0, BinaryReader_methods[method], reader.pos);
    }
    else {
        readLength = copy(reader._buffer, buf, 0, reader.pos, reader.pos + BinaryReader_methods[method]);
    }
    reader.pos += readLength;
    return buffer_namespaceObject[method](buf, 0);
}
/**
 * @public
 */
class BinaryReader_BinaryReader extends Reader {
    constructor(buffer, endian = EndianType.BigEndian) {
        if (typeof buffer === 'string') {
            super(fs.statSync(buffer).size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = fs.openSync(buffer, 'r');
            this._path = buffer;
            this._buffer = null;
        }
        else if (buffer instanceof Uint8Array) {
            super(buffer.length);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 1 /* BUFFER */ });
            this._file = null;
            this._path = '';
            this._buffer = buffer;
        }
        else if (buffer instanceof FileDescriptor) {
            super(buffer.size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = buffer.fd;
            this._path = buffer.path;
            this._buffer = null;
        }
        else {
            throw new TypeError('[BinaryReader] Contructor parameter type error');
        }
        Object.defineProperty(this, 'endian', {
            configurable: true,
            enumerable: true,
            get() { return endian; },
            set(ed) {
                if (ed !== EndianType.BigEndian && ed !== EndianType.LittleEndian) {
                    throw new TypeError('endian type error');
                }
                endian = ed;
            }
        });
        this._opened = true;
    }
    close() {
        if (this._opened) {
            if (this.type === 0 /* FILE */) {
                if (typeof this._file === 'number') {
                    fs.closeSync(this._file);
                }
                this._file = null;
            }
            else {
                this._buffer = null;
            }
            this._opened = false;
        }
    }
    read(len = 1) {
        checkRange(this.pos, this._size);
        if (len === 0)
            return new Uint8Array(0);
        if (this.pos + len > this._size) {
            len = this._size - this.pos;
        }
        if (len === 0)
            return new Uint8Array(0);
        const buf = new Uint8Array(len);
        let readLength;
        if (this.type === 0 /* FILE */) {
            readLength = fs.readSync(this._file, buf, 0, len, this.pos);
        }
        else {
            readLength = copy(this._buffer, buf, 0, this.pos, this.pos + len);
        }
        this.pos += readLength;
        return buf;
    }
    readToBuffer(buf, bufStart = 0, len = 1) {
        checkRange(this.pos, this._size);
        if (len === 0)
            return 0;
        if (this.pos + len > this._size) {
            len = this._size - this.pos;
        }
        if (len === 0)
            return 0;
        let readLength;
        if (this.type === 0 /* FILE */) {
            readLength = fs.readSync(this._file, buf, bufStart, len, this.pos);
        }
        else {
            readLength = copy(this._buffer, buf, bufStart, this.pos, this.pos + len);
        }
        this.pos += readLength;
        return readLength;
    }
    readString(encoding = 'ascii', length = -1) {
        checkRange(this.pos, this._size);
        if (length === 0)
            return '';
        if (length === -1) {
            let l = 0;
            const buf = new Uint8Array(1);
            do {
                let readLength;
                if (this.type === 0 /* FILE */) {
                    readLength = fs.readSync(this._file, buf, 0, 1, this.pos + l);
                }
                else {
                    readLength = copy(this._buffer, buf, 0, this.pos + l, this.pos + l + 1);
                }
                if (readLength === 0)
                    break;
                l += readLength;
            } while (buf[0] !== 0);
            const r = new Uint8Array(l - 1);
            if (this.type === 0 /* FILE */) {
                fs.readSync(this._file, r, 0, l - 1, this.pos);
            }
            else {
                copy(this._buffer, r, 0, this.pos, this.pos + l - 1);
            }
            this.pos += l;
            return bufferToString(r, encoding);
        }
        return bufferToString(this.read(length), encoding);
    }
    readBoolean() {
        return this.readUInt8() !== 0;
    }
    readInt8() {
        return readNumber(this, 'readInt8');
    }
    readUInt8() {
        return readNumber(this, 'readUInt8');
    }
    readInt16LE() {
        return readNumber(this, 'readInt16LE');
    }
    readUInt16LE() {
        return readNumber(this, 'readUInt16LE');
    }
    readInt16BE() {
        return readNumber(this, 'readInt16BE');
    }
    readUInt16BE() {
        return readNumber(this, 'readUInt16BE');
    }
    readInt32LE() {
        return readNumber(this, 'readInt32LE');
    }
    readUInt32LE() {
        return readNumber(this, 'readUInt32LE');
    }
    readInt32BE() {
        return readNumber(this, 'readInt32BE');
    }
    readUInt32BE() {
        return readNumber(this, 'readUInt32BE');
    }
    readBigInt64LE() {
        return readNumber(this, 'readBigInt64LE');
    }
    readBigUInt64LE() {
        return readNumber(this, 'readBigUInt64LE');
    }
    readBigInt64BE() {
        return readNumber(this, 'readBigInt64BE');
    }
    readBigUInt64BE() {
        return readNumber(this, 'readBigUInt64BE');
    }
    readFloatLE() {
        return readNumber(this, 'readFloatLE');
    }
    readFloatBE() {
        return readNumber(this, 'readFloatBE');
    }
    readDoubleLE() {
        return readNumber(this, 'readDoubleLE');
    }
    readDoubleBE() {
        return readNumber(this, 'readDoubleBE');
    }
    readInt16() {
        return this.endian === EndianType.BigEndian ? this.readInt16BE() : this.readInt16LE();
    }
    readUInt16() {
        return this.endian === EndianType.BigEndian ? this.readUInt16BE() : this.readUInt16LE();
    }
    readInt32() {
        return this.endian === EndianType.BigEndian ? this.readInt32BE() : this.readInt32LE();
    }
    readUInt32() {
        return this.endian === EndianType.BigEndian ? this.readUInt32BE() : this.readUInt32LE();
    }
    readBigInt64() {
        return this.endian === EndianType.BigEndian ? this.readBigInt64BE() : this.readBigInt64LE();
    }
    readBigUInt64() {
        return this.endian === EndianType.BigEndian ? this.readBigUInt64BE() : this.readBigUInt64LE();
    }
    readFloat() {
        return this.endian === EndianType.BigEndian ? this.readFloatBE() : this.readFloatLE();
    }
    readDouble() {
        return this.endian === EndianType.BigEndian ? this.readDoubleBE() : this.readDoubleLE();
    }
}

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/AsyncBinaryReader.js





let onceSupported = false;
try {
    const options = Object.defineProperty({}, 'once', {
        get: function () {
            onceSupported = true;
        }
    });
    window.addEventListener('test', null, options);
}
catch (err) { }
const listenerOpts = onceSupported ? { once: true } : false;
function fsRead(reader, buf, bufStart, len, pos) {
    return new Promise((resolve, reject) => {
        const file = reader._file;
        if (typeof file === 'number') {
            try {
                resolve(fs.readSync(file, buf, bufStart, len, pos));
            }
            catch (err) {
                reject(err);
            }
        }
        else {
            const fr = new FileReader();
            let onError;
            // eslint-disable-next-line prefer-const
            let onLoad;
            // eslint-disable-next-line prefer-const
            onError = () => {
                if (!onceSupported) {
                    fr.removeEventListener('error', onError);
                    fr.removeEventListener('load', onLoad);
                }
                reject(fr.error);
            };
            // eslint-disable-next-line prefer-const
            onLoad = () => {
                if (!onceSupported) {
                    fr.removeEventListener('error', onError);
                    fr.removeEventListener('load', onLoad);
                }
                const readBuffer = new Uint8Array(fr.result);
                const copied = copy(readBuffer, buf, bufStart, 0, readBuffer.length);
                resolve(copied);
            };
            fr.addEventListener('error', onError, listenerOpts);
            fr.addEventListener('load', onLoad, listenerOpts);
            fr.readAsArrayBuffer(file.slice(pos, pos + len));
        }
    });
}
const AsyncBinaryReader_methods = methods;
function AsyncBinaryReader_readNumber(reader, method) {
    const promise = new Promise((resolve, reject) => {
        checkRange(reader.pos, reader.size);
        if (method === 'readBigInt64BE' || method === 'readBigInt64LE' || method === 'readBigUInt64BE' || method === 'readBigUInt64LE') {
            validateBigInt();
        }
        const buf = new Uint8Array(AsyncBinaryReader_methods[method]);
        if (reader.type === 0 /* FILE */) {
            fsRead(reader, buf, 0, AsyncBinaryReader_methods[method], reader.pos).then(readLength => {
                reader.pos += readLength;
                resolve(buffer_namespaceObject[method](buf, 0));
            }).catch(reject);
        }
        else {
            const readLength = copy(reader._buffer, buf, 0, reader.pos, reader.pos + AsyncBinaryReader_methods[method]);
            reader.pos += readLength;
            resolve(buffer_namespaceObject[method](buf, 0));
        }
    });
    return promise;
}
/**
 * @public
 */
class AsyncBinaryReader_AsyncBinaryReader extends Reader {
    constructor(fileOrBuffer, endian = EndianType.BigEndian) {
        if (fileOrBuffer instanceof Uint8Array) {
            super(fileOrBuffer.length);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 1 /* BUFFER */ });
            this._path = '';
            this._file = null;
            this._buffer = fileOrBuffer;
        }
        else if (typeof fileOrBuffer === 'string') {
            super(fs.statSync(fileOrBuffer).size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = fs.openSync(fileOrBuffer, 'r');
            this._path = fileOrBuffer;
            this._buffer = null;
        }
        else if (fileOrBuffer instanceof FileDescriptor) {
            super(fileOrBuffer.size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = fileOrBuffer.fd;
            this._path = fileOrBuffer.path;
            this._buffer = null;
        }
        else {
            super(fileOrBuffer.size);
            Object.defineProperty(this, 'type', { configurable: true, enumerable: true, writable: false, value: 0 /* FILE */ });
            this._file = fileOrBuffer;
            this._path = fileOrBuffer.webkitRelativePath || '';
            this._buffer = null;
        }
        Object.defineProperty(this, 'endian', {
            configurable: true,
            enumerable: true,
            get() { return endian; },
            set(ed) {
                if (ed !== EndianType.BigEndian && ed !== EndianType.LittleEndian) {
                    throw new TypeError('endian type error');
                }
                endian = ed;
            }
        });
        this._opened = true;
    }
    close() {
        if (this._opened) {
            if (this.type === 0 /* FILE */) {
                if (typeof this._file === 'number') {
                    fs.closeSync(this._file);
                }
                this._file = null;
            }
            else {
                this._buffer = null;
            }
            this._opened = false;
        }
    }
    read(len = 1) {
        const promise = new Promise((resolve, reject) => {
            checkRange(this.pos, this._size);
            if (len === 0) {
                resolve(new Uint8Array(0));
                return;
            }
            if (this.pos + len > this._size) {
                len = this._size - this.pos;
            }
            if (len === 0) {
                resolve(new Uint8Array(0));
                return;
            }
            const buf = new Uint8Array(len);
            if (this.type === 0 /* FILE */) {
                fsRead(this, buf, 0, len, this.pos).then(readLength => {
                    this.pos += readLength;
                    resolve(buf);
                }).catch(reject);
            }
            else {
                const readLength = copy(this._buffer, buf, 0, this.pos, this.pos + len);
                this.pos += readLength;
                resolve(buf);
            }
        });
        return promise;
    }
    readToBuffer(buf, bufStart = 0, len = 1) {
        const promise = new Promise((resolve, reject) => {
            checkRange(this.pos, this._size);
            if (len === 0) {
                resolve(0);
                return;
            }
            if (this.pos + len > this._size) {
                len = this._size - this.pos;
            }
            if (len === 0) {
                resolve(0);
                return;
            }
            if (this.type === 0 /* FILE */) {
                fsRead(this, buf, bufStart, len, this.pos).then(readLength => {
                    this.pos += readLength;
                    resolve(readLength);
                }).catch(reject);
            }
            else {
                const readLength = copy(this._buffer, buf, bufStart, this.pos, this.pos + len);
                this.pos += readLength;
                resolve(readLength);
            }
        });
        return promise;
    }
    async readString(encoding = 'ascii', length = -1) {
        checkRange(this.pos, this._size);
        if (length === 0) {
            return '';
        }
        if (length === -1) {
            let l = 0;
            const buf = new Uint8Array(1);
            do {
                let readLength;
                if (this.type === 0 /* FILE */) {
                    readLength = await fsRead(this, buf, 0, 1, this.pos + l);
                }
                else {
                    readLength = copy(this._buffer, buf, 0, this.pos + l, this.pos + l + 1);
                }
                if (readLength === 0)
                    break;
                l += readLength;
            } while (buf[0] !== 0);
            const r = new Uint8Array(l - 1);
            if (this.type === 0 /* FILE */) {
                await fsRead(this, r, 0, l - 1, this.pos);
            }
            else {
                copy(this._buffer, r, 0, this.pos, this.pos + l - 1);
            }
            this.pos += l;
            return bufferToString(r, encoding);
        }
        return bufferToString(await this.read(length), encoding);
    }
    readBoolean() {
        return this.readUInt8().then(u8 => (u8 !== 0));
    }
    readInt8() {
        return AsyncBinaryReader_readNumber(this, 'readInt8');
    }
    readUInt8() {
        return AsyncBinaryReader_readNumber(this, 'readUInt8');
    }
    readInt16LE() {
        return AsyncBinaryReader_readNumber(this, 'readInt16LE');
    }
    readUInt16LE() {
        return AsyncBinaryReader_readNumber(this, 'readUInt16LE');
    }
    readInt16BE() {
        return AsyncBinaryReader_readNumber(this, 'readInt16BE');
    }
    readUInt16BE() {
        return AsyncBinaryReader_readNumber(this, 'readUInt16BE');
    }
    readInt32LE() {
        return AsyncBinaryReader_readNumber(this, 'readInt32LE');
    }
    readUInt32LE() {
        return AsyncBinaryReader_readNumber(this, 'readUInt32LE');
    }
    readInt32BE() {
        return AsyncBinaryReader_readNumber(this, 'readInt32BE');
    }
    readUInt32BE() {
        return AsyncBinaryReader_readNumber(this, 'readUInt32BE');
    }
    readBigInt64LE() {
        return AsyncBinaryReader_readNumber(this, 'readBigInt64LE');
    }
    readBigUInt64LE() {
        return AsyncBinaryReader_readNumber(this, 'readBigUInt64LE');
    }
    readBigInt64BE() {
        return AsyncBinaryReader_readNumber(this, 'readBigInt64BE');
    }
    readBigUInt64BE() {
        return AsyncBinaryReader_readNumber(this, 'readBigUInt64BE');
    }
    readFloatLE() {
        return AsyncBinaryReader_readNumber(this, 'readFloatLE');
    }
    readFloatBE() {
        return AsyncBinaryReader_readNumber(this, 'readFloatBE');
    }
    readDoubleLE() {
        return AsyncBinaryReader_readNumber(this, 'readDoubleLE');
    }
    readDoubleBE() {
        return AsyncBinaryReader_readNumber(this, 'readDoubleBE');
    }
    readInt16() {
        return this.endian === EndianType.BigEndian ? this.readInt16BE() : this.readInt16LE();
    }
    readUInt16() {
        return this.endian === EndianType.BigEndian ? this.readUInt16BE() : this.readUInt16LE();
    }
    readInt32() {
        return this.endian === EndianType.BigEndian ? this.readInt32BE() : this.readInt32LE();
    }
    readUInt32() {
        return this.endian === EndianType.BigEndian ? this.readUInt32BE() : this.readUInt32LE();
    }
    readBigInt64() {
        return this.endian === EndianType.BigEndian ? this.readBigInt64BE() : this.readBigInt64LE();
    }
    readBigUInt64() {
        return this.endian === EndianType.BigEndian ? this.readBigUInt64BE() : this.readBigUInt64LE();
    }
    readFloat() {
        return this.endian === EndianType.BigEndian ? this.readFloatBE() : this.readFloatLE();
    }
    readDouble() {
        return this.endian === EndianType.BigEndian ? this.readDoubleBE() : this.readDoubleLE();
    }
}

// CONCATENATED MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/index.js
/**
 * Binary reader for Node.js and browser.
 *
 * @packageDocumentation
 */







/***/ }),
/* 10 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzMapleVersion; });
/**
 * @public
 */
var WzMapleVersion;
(function (WzMapleVersion) {
    /**
     * Global MapleStory (old)
     */
    WzMapleVersion[WzMapleVersion["GMS"] = 0] = "GMS";
    /**
     * 新楓之谷 / 冒险岛Online / 메이플스토리 / MapleSEA / EMS (old)
     */
    WzMapleVersion[WzMapleVersion["EMS"] = 1] = "EMS";
    /**
     * BMS / GMS / MapleSEA / メイプルストーリー / 메이플스토리
     */
    WzMapleVersion[WzMapleVersion["BMS"] = 2] = "BMS";
    WzMapleVersion[WzMapleVersion["CLASSIC"] = 3] = "CLASSIC";
    /**
     * 生成
     */
    WzMapleVersion[WzMapleVersion["GENERATE"] = 4] = "GENERATE";
    WzMapleVersion[WzMapleVersion["GETFROMZLZ"] = 5] = "GETFROMZLZ";
    /**
     * 自定义编码
     */
    WzMapleVersion[WzMapleVersion["CUSTOM"] = 6] = "CUSTOM";
    WzMapleVersion[WzMapleVersion["UNKNOWN"] = 99] = "UNKNOWN";
})(WzMapleVersion || (WzMapleVersion = {}));


/***/ }),
/* 11 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return MAPLESTORY_USERKEY_DEFAULT; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "f", function() { return userKeyWzLib; });
/* unused harmony export isDefaultMapleStoryUserKey */
/* unused harmony export bShuffle */
/* unused harmony export bDefaultAESKeyValue */
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return WZ_GMSIV; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return WZ_MSEAIV; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return WZ_OffsetConstant; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "e", function() { return getTrimmedUserKey; });
const MAPLESTORY_USERKEY_DEFAULT = new Uint8Array([
    
]);
const userKeyWzLib = (function () {
    const buf = new Uint8Array(16 * 8);
    buf.set(MAPLESTORY_USERKEY_DEFAULT);
    return buf;
})();
function isDefaultMapleStoryUserKey() {
    if (!(userKeyWzLib instanceof Uint8Array))
        return false;
    if (MAPLESTORY_USERKEY_DEFAULT.length !== userKeyWzLib.length)
        return false;
    for (let i = 0; i < MAPLESTORY_USERKEY_DEFAULT.length; i++) {
        if (userKeyWzLib[i] !== MAPLESTORY_USERKEY_DEFAULT[i])
            return false;
    }
    return true;
}
const bShuffle = new Uint8Array([
    
]);
const bDefaultAESKeyValue = new Uint8Array([
    0xC6, 0x50, 0x53, 0xF2, 0xA8, 0x42, 0x9D, 0x7F, 0x77, 0x09, 0x1D, 0x26, 0x42, 0x53, 0x88, 0x7C
]);
const WZ_GMSIV = new Uint8Array([0x4D, 0x23, 0xC7, 0x2B]);
const WZ_MSEAIV = new Uint8Array([0xB9, 0x7D, 0x63, 0xE9]);
// eslint-disable-next-line @typescript-eslint/naming-convention
const WZ_OffsetConstant = 0x581C3F6D;
function getTrimmedUserKey(userKey) {
    const key = new Uint8Array(32);
    for (let i = 0; i < 128; i += 16) {
        key[i / 4] = userKey[i];
    }
    return key;
}


/***/ }),
/* 12 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzObject; });
/* harmony import */ var _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(5);
/* harmony import */ var _util_node_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(0);


/**
 * @public
 */
class WzObject {
    constructor() {
        // public hcTag: any = null
        // public hcTag_spine: any = null
        // public msTag: any = null
        // public msTag_spine: any = null
        // public tag3: any = null
        this._disposed = false;
        // public abstract remove (): void
    }
    /**
     * @virtual
     */
    get wzValue() {
        return null;
    }
    /**
     * @virtual
     */
    getBitmap() {
        throw new _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_0__[/* NotImplementedError */ "a"]('[WzObject#getBitmap]');
    }
    getTopMostWzDirectory() {
        let parent = this.parent;
        if (parent == null) {
            return this;
        }
        while (parent.parent != null) {
            parent = parent.parent;
        }
        return parent;
    }
    get fullPath() {
        let result = this.name;
        let currObj = this;
        while (currObj.parent != null) {
            currObj = currObj.parent;
            result = currObj.name + _util_node_js__WEBPACK_IMPORTED_MODULE_1__[/* path */ "g"].sep + result;
        }
        return result;
    }
}


/***/ }),
/* 13 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzDirectory; });
/* harmony import */ var _WzDirectoryType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(15);
/* harmony import */ var _WzImage_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(4);
/* harmony import */ var _WzObject_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(12);
/* harmony import */ var _WzObjectType_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(8);




/**
 * @public
 */
class WzDirectory extends _WzObject_js__WEBPACK_IMPORTED_MODULE_2__[/* WzObject */ "a"] {
    constructor(reader, name, hash, wzIv, wzFile) {
        super();
        this.images = new Set();
        this.subDirs = new Set();
        this.offset = 0;
        this.blockSize = 0;
        this.checksum = 0;
        this.parent = null;
        this.reader = reader;
        this.name = name;
        this.hash = hash;
        this.wzIv = wzIv;
        this.wzFile = wzFile;
    }
    dispose() {
        if (this._disposed)
            return;
        this._clearAllChildren();
        this._disposed = true;
    }
    _clearAllChildren() {
        for (const img of this.images) {
            img.dispose();
        }
        for (const dir of this.subDirs) {
            dir.dispose();
        }
        this.images.clear();
        this.subDirs.clear();
    }
    get objectType() {
        return _WzObjectType_js__WEBPACK_IMPORTED_MODULE_3__[/* WzObjectType */ "a"].Directory;
    }
    get wzFileParent() {
        return this.wzFile;
    }
    get wzImages() {
        return this.images;
    }
    get wzDirectories() {
        return this.subDirs;
    }
    at(name) {
        const nameLower = name.toLowerCase();
        for (const img of this.images) {
            if (img.name.toLowerCase() === nameLower)
                return img;
        }
        for (const dir of this.subDirs) {
            if (dir.name.toLowerCase() === nameLower)
                return dir;
        }
        return null;
    }
    set(name, value) {
        if (value != null) {
            value.name = name;
            if (value instanceof WzDirectory) {
                this.addDirectory(value);
            }
            else if (value instanceof _WzImage_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImage */ "a"]) {
                this.addImage(value);
            }
            else {
                throw new TypeError('Value must be a Directory or Image');
            }
        }
    }
    addDirectory(dir) {
        this.subDirs.add(dir);
        dir.wzFile = this.wzFile;
        dir.parent = this;
    }
    addImage(img) {
        this.images.add(img);
        img.parent = this;
    }
    clearDirectories() {
        for (const dir of this.subDirs) {
            dir.parent = null;
        }
        this.subDirs.clear();
    }
    clearImages() {
        for (const img of this.images) {
            img.parent = null;
        }
        this.images.clear();
    }
    getImageByName(name) {
        const nameLower = name.toLowerCase();
        for (const img of this.images) {
            if (img.name.toLowerCase() === nameLower)
                return img;
        }
        return null;
    }
    getDirectoryByName(name) {
        const nameLower = name.toLowerCase();
        for (const dir of this.subDirs) {
            if (dir.name.toLowerCase() === nameLower)
                return dir;
        }
        return null;
    }
    setVersionHash(newHash) {
        this.hash = newHash;
        for (const dir of this.subDirs) {
            dir.setVersionHash(newHash);
        }
    }
    async parseImages() {
        for (const img of this.images) {
            if (this.reader.pos !== img.offset) {
                this.reader.pos = img.offset;
            }
            await img.parseImage();
        }
        for (const subdir of this.subDirs) {
            if (this.reader.pos !== subdir.offset) {
                this.reader.pos = subdir.offset;
            }
            await subdir.parseImages();
        }
    }
    async parseDirectory(lazyParse = false) {
        this._clearAllChildren();
        const available = this.reader.available();
        if (available === 0)
            return;
        if (this.reader.pos !== this.offset) {
            this.reader.pos = this.offset;
        }
        const reader = this.reader;
        const entryCount = await reader.readWzInt();
        if (entryCount < 0 || entryCount > 100000)
            throw new Error('Invalid wz version used for decryption, try parsing other version numbers.');
        for (let i = 0; i < entryCount; i++) {
            let type = await reader.readUInt8();
            let fname = '';
            let fsize;
            let checksum;
            let offset;
            // let unk_GMS230 = 0
            let rememberPos = 0;
            switch (type) {
                case _WzDirectoryType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzDirectoryType */ "a"].UnknownType_1: {
                    /* const unknown =  */ await reader.readInt32LE();
                    await reader.readInt16LE();
                    /* const offs =  */ await reader.readWzOffset();
                    continue;
                }
                case _WzDirectoryType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzDirectoryType */ "a"].RetrieveStringFromOffset_2: {
                    const stringOffset = await reader.readInt32LE();
                    rememberPos = reader.pos;
                    reader.pos = reader.header.fstart + stringOffset;
                    type = await reader.readUInt8();
                    fname = await reader.readWzString();
                    break;
                }
                case _WzDirectoryType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzDirectoryType */ "a"].WzDirectory_3:
                case _WzDirectoryType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzDirectoryType */ "a"].WzImage_4: {
                    fname = await reader.readWzString();
                    rememberPos = reader.pos;
                    break;
                }
                default: {
                    throw new Error(`[WzDirectory] Unknown directory. type = ${type}`);
                }
            }
            reader.pos = rememberPos;
            // eslint-disable-next-line prefer-const
            fsize = await reader.readWzInt();
            // eslint-disable-next-line prefer-const
            checksum = await reader.readWzInt();
            // eslint-disable-next-line prefer-const
            offset = await reader.readWzOffset();
            if (type === _WzDirectoryType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzDirectoryType */ "a"].WzDirectory_3) {
                const subDir = new WzDirectory(reader, fname, this.hash, this.wzIv, this.wzFile);
                subDir.blockSize = fsize;
                subDir.checksum = checksum;
                subDir.offset = offset;
                subDir.parent = this;
                this.subDirs.add(subDir);
                if (lazyParse)
                    break;
            }
            else {
                const img = new _WzImage_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImage */ "a"](fname, reader, checksum);
                img.blockSize = fsize;
                img.offset = offset;
                img.parent = this;
                this.images.add(img);
                if (lazyParse)
                    break;
            }
        }
        for (const subdir of this.subDirs) {
            // reader.pos = subdir.offset
            if (subdir.checksum !== 0) {
                await subdir.parseDirectory(false);
            }
        }
    }
}


/***/ }),
/* 14 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzHeader; });
/**
 * @public
 */
class WzHeader {
    constructor() {
        this.ident = '';
        this.fsize = BigInt(0);
        this.fstart = 0;
        this.copyright = '';
    }
    recalculateFileStart() {
        this.fstart = this.ident.length + 8 + 4 + this.copyright.length + 1;
    }
    static getDefault() {
        const header = new WzHeader();
        header.ident = 'PKG1';
        header.copyright = WzHeader.DEFAULT_WZ_HEADER_COPYRIGHT;
        header.fstart = 60;
        header.fsize = BigInt(0);
        return header;
    }
}
WzHeader.DEFAULT_WZ_HEADER_COPYRIGHT = 'Package file v1.0 Copyright 2002 Wizet, ZMS';


/***/ }),
/* 15 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzDirectoryType; });
/**
 * @public
 */
var WzDirectoryType;
(function (WzDirectoryType) {
    WzDirectoryType[WzDirectoryType["UnknownType_1"] = 1] = "UnknownType_1";
    WzDirectoryType[WzDirectoryType["RetrieveStringFromOffset_2"] = 2] = "RetrieveStringFromOffset_2";
    WzDirectoryType[WzDirectoryType["WzDirectory_3"] = 3] = "WzDirectory_3";
    WzDirectoryType[WzDirectoryType["WzImage_4"] = 4] = "WzImage_4";
})(WzDirectoryType || (WzDirectoryType = {}));


/***/ }),
/* 16 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzIntProperty", function() { return WzIntProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(2);


/**
 * @public
 */
class WzIntProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImageProperty */ "a"] {
    constructor(name = '', val = 0) {
        super();
        this.name = name;
        this.val = val;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Int;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    get value() {
        return this.val;
    }
    get wzValue() {
        return this.val;
    }
    setValue(value) {
        this.val = value;
    }
    toString() {
        return this.val.toString();
    }
}


/***/ }),
/* 17 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzUOLProperty", function() { return WzUOLProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(6);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(2);
/* harmony import */ var _WzImage_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(4);
/* harmony import */ var _WzDirectory_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(13);
/* harmony import */ var _util_ErrorLogger_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(3);






/**
 * @public
 */
class WzUOLProperty extends _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__[/* WzExtended */ "a"] {
    constructor(name = '', val = '') {
        super();
        this.name = name;
        this.val = val;
        this.linkVal = null;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].UOL;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    get value() {
        return this.val;
    }
    get wzValue() {
        if (WzUOLProperty.UOLRES) {
            return this.linkValue;
        }
        return this;
    }
    get wzProperties() {
        return this.linkValue instanceof _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__[/* WzImageProperty */ "a"] ? this.linkValue.wzProperties : null;
    }
    at(name) {
        return this.linkValue instanceof _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__[/* WzImageProperty */ "a"] ? this.linkValue.at(name) : this.linkValue instanceof _WzImage_js__WEBPACK_IMPORTED_MODULE_3__[/* WzImage */ "a"] ? this.linkValue.at(name) : null;
    }
    getFromPath(path) {
        return this.linkValue instanceof _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__[/* WzImageProperty */ "a"] ? this.linkValue.getFromPath(path) : this.linkValue instanceof _WzImage_js__WEBPACK_IMPORTED_MODULE_3__[/* WzImage */ "a"] ? this.linkValue.getFromPath(path) : null;
    }
    get linkValue() {
        if (this.linkVal == null) {
            const paths = this.val.split(/[\\/]/);
            this.linkVal = this.parent;
            // @ts-expect-error ignore
            // eslint-disable-next-line @typescript-eslint/no-unused-vars
            const asdf = this.parent.fullPath;
            for (const path of paths) {
                if (path === '..') {
                    this.linkVal = this.linkVal.parent;
                }
                else {
                    if (this.linkVal instanceof _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__[/* WzImageProperty */ "a"]) {
                        this.linkVal = this.linkVal.at(path);
                    }
                    else if (this.linkVal instanceof _WzImage_js__WEBPACK_IMPORTED_MODULE_3__[/* WzImage */ "a"]) {
                        this.linkVal = this.linkVal.at(path);
                    }
                    else if (this.linkVal instanceof _WzDirectory_js__WEBPACK_IMPORTED_MODULE_4__[/* WzDirectory */ "a"]) {
                        if (path.endsWith('.img')) {
                            this.linkVal = this.linkVal.at(path);
                        }
                        else {
                            this.linkVal = this.linkVal.at(path + '.img');
                        }
                    }
                    else {
                        _util_ErrorLogger_js__WEBPACK_IMPORTED_MODULE_5__[/* ErrorLogger */ "b"].log(_util_ErrorLogger_js__WEBPACK_IMPORTED_MODULE_5__[/* ErrorLevel */ "a"].Critical, `UOL got nexon'd at property: ${this.fullPath}`);
                        return null;
                    }
                }
            }
        }
        return this.linkVal;
    }
    setValue(value) {
        this.val = value;
    }
    toString() {
        return this.val;
    }
    async getBitmap() {
        if (this.linkValue != null) {
            return await this.linkValue.getBitmap();
        }
        return null;
    }
}
WzUOLProperty.UOLRES = true;


/***/ }),
/* 18 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return BaseError; });
/** @public */
class BaseError extends Error {
    constructor(message) {
        super(message);
        this.name = typeof new.target.name === 'string'
            ? new.target.name
            : 'Error';
        if (!(this instanceof BaseError)) {
            // ES5 Code
            if (typeof Object.setPrototypeOf === 'function') {
                Object.setPrototypeOf(this, new.target.prototype);
            }
            else {
                // eslint-disable-next-line no-proto
                this.__proto__ = new.target.prototype;
            }
            if (typeof Error.captureStackTrace === 'function') {
                Error.captureStackTrace(this, new.target);
            }
        }
    }
}


/***/ }),
/* 19 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzBinaryReader; });
/* harmony import */ var _WzHeader_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(14);
/* harmony import */ var _tybys_binreader__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(9);
/* harmony import */ var _MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(11);
/* harmony import */ var _WzKeyGenerator_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(20);
/* harmony import */ var _WzTool_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(21);
/* harmony import */ var _node_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(0);






/**
 * @public
 */
class WzBinaryReader extends _tybys_binreader__WEBPACK_IMPORTED_MODULE_1__[/* AsyncBinaryReader */ "a"] {
    constructor(filePath, wzIv) {
        super(filePath);
        this.wzKey = Object(_WzKeyGenerator_js__WEBPACK_IMPORTED_MODULE_3__[/* generateWzKey */ "b"])(wzIv);
        this.hash = 0;
        this.header = _WzHeader_js__WEBPACK_IMPORTED_MODULE_0__[/* WzHeader */ "a"].getDefault();
    }
    get size() {
        return this._size;
    }
    available() {
        return this.size - this.pos;
    }
    async readWzStringAtOffset(offset, readByte = false) {
        const currentOffset = this.pos;
        this.pos = offset;
        if (readByte) {
            await this.read();
        }
        const returnString = await this.readWzString();
        this.pos = currentOffset;
        return returnString;
    }
    async readWzString() {
        const smallLength = await this.readInt8();
        if (smallLength === 0) {
            return '';
        }
        let length;
        const u8arr = [];
        if (smallLength > 0) {
            let mask = 0xAAAA;
            if (smallLength === 127) {
                length = await this.readInt32LE();
            }
            else {
                length = smallLength;
            }
            if (length <= 0) {
                return '';
            }
            for (let i = 0; i < length; i++) {
                let encryptedChar = await this.readUInt16LE();
                encryptedChar = ((encryptedChar ^ mask) >>> 0) & 0xffff;
                encryptedChar = ((encryptedChar ^ (((this.wzKey.at(i * 2 + 1) << 8) >>> 0) + this.wzKey.at(i * 2))) >>> 0) & 0xffff;
                u8arr.push(encryptedChar & 0xff);
                u8arr.push(encryptedChar >>> 8);
                mask++;
            }
            return _node_js__WEBPACK_IMPORTED_MODULE_5__[/* utf16leTextDecoder */ "i"].decode(new Uint8Array(u8arr));
        }
        else { // ASCII
            let mask = 0xAA;
            if (smallLength === -128) {
                length = await this.readInt32LE();
            }
            else {
                length = -smallLength;
            }
            if (length <= 0) {
                return '';
            }
            for (let i = 0; i < length; i++) {
                let encryptedChar = await this.readUInt8();
                encryptedChar = (encryptedChar ^ mask) >>> 0;
                encryptedChar = (encryptedChar ^ this.wzKey.at(i)) >>> 0;
                u8arr.push(encryptedChar);
                mask++;
            }
            return _node_js__WEBPACK_IMPORTED_MODULE_5__[/* asciiTextDecoder */ "c"].decode(new Uint8Array(u8arr));
        }
    }
    async readStringBlock(offset) {
        const type = await this.readUInt8();
        switch (type) {
            case 0:
            case 0x73:
                return await this.readWzString();
            case 1:
            case 0x1B:
                return await this.readWzStringAtOffset(offset + await this.readInt32LE());
            default:
                return '';
        }
    }
    readNullTerminatedString() {
        return super.readString();
    }
    async readWzInt() {
        const sb = await this.readInt8();
        if (sb === -128) {
            return await this.readInt32LE();
        }
        return sb;
    }
    async readWzLong() {
        const sb = await this.readInt8();
        if (sb === -128) {
            return await this.readBigInt64LE();
        }
        return BigInt(sb);
    }
    async readWzOffset() {
        let offset = this.pos;
        offset = ((offset - this.header.fstart) ^ 0xFFFFFFFF) >>> 0;
        offset = ((offset * this.hash) & 0xFFFFFFFF) >>> 0;
        offset -= _MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_2__[/* WZ_OffsetConstant */ "d"];
        offset = Object(_WzTool_js__WEBPACK_IMPORTED_MODULE_4__[/* rotateLeft */ "b"])(offset, offset & 0x1F) & 0xFFFFFFFF;
        const encryptedOffset = await this.readUInt32LE();
        offset = ((offset ^ encryptedOffset) & 0xFFFFFFFF) >>> 0;
        offset = ((offset + this.header.fstart * 2) & 0xFFFFFFFF) >>> 0;
        return offset;
    }
    decryptString(stringToDecrypt) {
        let outputString = '';
        for (let i = 0; i < stringToDecrypt.length; i++) {
            outputString += String.fromCharCode(((stringToDecrypt.charCodeAt(i) ^ (((this.wzKey.at(i * 2 + 1) << 8) >>> 0) + this.wzKey.at(i * 2))) >>> 0) & 0xFFFF);
        }
        return outputString;
    }
    decryptNonUnicodeString(stringToDecrypt) {
        let outputString = '';
        for (let i = 0; i < stringToDecrypt.length; i++) {
            outputString += String.fromCharCode(((stringToDecrypt.charCodeAt(i) ^ this.wzKey.at(i)) >>> 0) & 0xFFFF);
        }
        return outputString;
    }
}


/***/ }),
/* 20 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return getIvFromZlz; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return generateLuaWzKey; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return generateWzKey; });
/* harmony import */ var _MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(11);
/* harmony import */ var _WzMutableKey_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(22);


function getIvFromZlz(zlzStream) {
    zlzStream.seek(0x10040);
    const iv = zlzStream.read(4);
    return iv;
}
/* function getAesKeyFromZlz (zlzStream: BinaryReader): Uint8Array {
  const aes = new Uint8Array(32)

  zlzStream.seek(0x10060)
  for (let i = 0; i < 8; i++) {
    zlzStream.readToBuffer(aes, i * 4, 4)
    zlzStream.pos += 12
  }
  return aes
} */
function generateLuaWzKey() {
    return new _WzMutableKey_js__WEBPACK_IMPORTED_MODULE_1__[/* WzMutableKey */ "a"](_MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_0__[/* WZ_MSEAIV */ "c"], Object(_MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_0__[/* getTrimmedUserKey */ "e"])(_MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_0__[/* MAPLESTORY_USERKEY_DEFAULT */ "a"]));
}
function generateWzKey(wzIv) {
    return new _WzMutableKey_js__WEBPACK_IMPORTED_MODULE_1__[/* WzMutableKey */ "a"](wzIv, Object(_MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_0__[/* getTrimmedUserKey */ "e"])(_MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_0__[/* userKeyWzLib */ "f"]));
}


/***/ }),
/* 21 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return rotateLeft; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return getIvByMapleVersion; });
/* harmony import */ var _WzMapleVersion_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(10);
/* harmony import */ var _MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(11);


function rotateLeft(x, n) {
    return ((((x) << (n)) >>> 0) | ((x) >>> (32 - (n)))) >>> 0;
}
function getIvByMapleVersion(ver) {
    switch (ver) {
        case _WzMapleVersion_js__WEBPACK_IMPORTED_MODULE_0__[/* WzMapleVersion */ "a"].EMS:
            return _MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_1__[/* WZ_MSEAIV */ "c"];
        case _WzMapleVersion_js__WEBPACK_IMPORTED_MODULE_0__[/* WzMapleVersion */ "a"].GMS:
            return _MapleCryptoConstants_js__WEBPACK_IMPORTED_MODULE_1__[/* WZ_GMSIV */ "b"];
        case _WzMapleVersion_js__WEBPACK_IMPORTED_MODULE_0__[/* WzMapleVersion */ "a"].CUSTOM: {
            return new Uint8Array(4);
        }
        case _WzMapleVersion_js__WEBPACK_IMPORTED_MODULE_0__[/* WzMapleVersion */ "a"].GENERATE:
            return new Uint8Array(4);
        case _WzMapleVersion_js__WEBPACK_IMPORTED_MODULE_0__[/* WzMapleVersion */ "a"].BMS:
        case _WzMapleVersion_js__WEBPACK_IMPORTED_MODULE_0__[/* WzMapleVersion */ "a"].CLASSIC:
        default:
            return new Uint8Array(4);
    }
}


/***/ }),
/* 22 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return WzMutableKey; });
/* harmony import */ var _node_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(0);
/* harmony import */ var _tybys_binreader__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(9);


/**
 * @public
 */
class WzMutableKey {
    constructor(_iv, _aesUserKey) {
        this._iv = _iv;
        this._aesUserKey = _aesUserKey;
        this._keys = null;
    }
    at(index) {
        if (this._keys == null || this._keys.length <= index) {
            this.ensureKeySize(index + 1);
        }
        return this._keys[index];
    }
    ensureKeySize(size) {
        if (this._keys != null && this._keys.length >= size) {
            return;
        }
        size = Math.ceil(1.0 * size / WzMutableKey._batchSize) * WzMutableKey._batchSize;
        const newKeys = new Uint8Array(size);
        const r = new _tybys_binreader__WEBPACK_IMPORTED_MODULE_1__[/* BinaryReader */ "b"](this._iv);
        // const tmp = this._iv.readInt32LE(0)
        const tmp = r.readInt32LE();
        r.dispose();
        if (tmp === 0) {
            this._keys = newKeys;
            return;
        }
        let startIndex = 0;
        if (this._keys != null) {
            // this._keys.copy(newKeys, 0, 0)
            newKeys.set(this._keys, 0);
            startIndex = this._keys.length;
        }
        const aes = _node_js__WEBPACK_IMPORTED_MODULE_0__[/* crypto */ "d"].createCipheriv('aes-256-ecb', this._aesUserKey, null);
        aes.setAutoPadding(true);
        // const ms = newKeys.slice(startIndex, newKeys.length - startIndex)
        const ms = newKeys.subarray(startIndex, newKeys.length);
        for (let i = startIndex; i < size; i += 16) {
            if (i === 0) {
                const block = new Uint8Array(16);
                for (let j = 0; j < block.length; j++) {
                    block[j] = this._iv[j % 4];
                }
                const buf = aes.update(block);
                ms.set(buf, i - startIndex);
                // buf.copy(ms, i, 0)
            }
            else {
                // const buf = aes.update(newKeys.slice(i - 16, i))
                const buf = aes.update(newKeys.subarray(i - 16, i));
                ms.set(buf, i - startIndex);
                // buf.copy(ms, i, 0)
            }
        }
        this._keys = newKeys;
        aes.destroy();
    }
}
WzMutableKey._batchSize = 4096;


/***/ }),
/* 23 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return tryGetRequireFunction; });
function tryGetRequireFunction (parentModule) {
  var nativeRequire;

  if (true) {
    nativeRequire =  true ? (function () {
  return typeof __webpack_public_path__ !== 'undefined' ? (typeof __non_webpack_require__ !== 'undefined' ? __non_webpack_require__ : undefined) : (typeof require !== 'undefined' ? require : undefined);
})() : undefined;
  } else {}

  if (typeof nativeRequire === 'function') {
    var g = (function (defaultValue) {
      var g;
      g = (function () { return this; })(); // non-strict mode
    
      try {
        g = g || new Function("return this")(); // allow eval
      } catch (_) {
        if (typeof globalThis !== "undefined") return globalThis;
        if (false) {}
        if (typeof window !== "undefined") return window;
        if (typeof self !== "undefined") return self;
      }
    
      return g || defaultValue;
    })();
    if (!(g && g.process && g.process.versions && typeof g.process.versions.node === 'string')) {
      return nativeRequire;
    }

    if (nativeRequire === g.require) {
      return nativeRequire;
    }

    var Module;
    try {
      Module = nativeRequire('module');
    } catch (_) {
      return nativeRequire;
    }

    Module = typeof Module === 'function' ? Module : ((typeof Module === 'object' && Module !== null) ? Module.Module : null);
    if (!Module || !(parentModule instanceof Module)) {
      return nativeRequire;
    }

    return (function makeRequireFunction (mod, main) {
      var Module = mod.constructor;
      function require (path) {
        return mod.require(path);
      };

      function validateString (value, name) {
        if (typeof value !== 'string') throw new TypeError('The "' + name + '" argument must be of type string. Received type ' + typeof value);
      }

      function resolve (request, opts) {
        validateString(request, 'request');
        return Module._resolveFilename(request, mod, false, opts);
      }

      require.resolve = resolve;

      function paths (request) {
        validateString(request, 'request');
        return Module._resolveLookupPaths(request, mod);
      }

      resolve.paths = paths;

      require.main = main;
      require.extensions = Module._extensions;
      require.cache = Module._cache;

      return require;
    })(parentModule, (g.process.mainModule instanceof Module) ? g.process.mainModule : undefined);
  }

  return nativeRequire;
}


/***/ }),
/* 24 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzLuaProperty", function() { return WzLuaProperty; });
/* harmony import */ var _util_WzKeyGenerator_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(20);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(2);
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(1);
/* harmony import */ var _util_node_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(0);




/**
 * @public
 */
class WzLuaProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImageProperty */ "a"] {
    constructor(name, encryptedBytes) {
        super();
        this.name = name;
        this.encryptedBytes = encryptedBytes;
        this.parent = null;
        this.wzKey = Object(_util_WzKeyGenerator_js__WEBPACK_IMPORTED_MODULE_0__[/* generateLuaWzKey */ "a"])();
    }
    setValue(value) {
        this.encryptedBytes = value;
    }
    get wzValue() {
        return this.value;
    }
    get value() {
        return this.encryptedBytes;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_2__[/* WzPropertyType */ "a"].Lua;
    }
    toString() {
        if (this.encryptedBytes == null)
            return '';
        return _util_node_js__WEBPACK_IMPORTED_MODULE_3__[/* asciiTextDecoder */ "c"].decode(this.encodeDecode(this.encryptedBytes));
    }
    encodeDecode(input) {
        const newArray = new Uint8Array(input.length);
        for (let i = 0; i < input.length; i++) {
            const encryptedChar = ((input[i] ^ this.wzKey.at(i)) & 0xFF);
            newArray[i] = encryptedChar;
        }
        return newArray;
    }
    dispose() {
        if (this._disposed)
            return;
        this.encryptedBytes = null;
        this._disposed = true;
    }
}


/***/ }),
/* 25 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzNullProperty", function() { return WzNullProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(5);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(2);



/**
 * @public
 */
class WzNullProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__[/* WzImageProperty */ "a"] {
    constructor(name = '') {
        super();
        this.name = name;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Null;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    setValue(_value) {
        throw new _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_1__[/* NotImplementedError */ "a"]('[WzNullProperty#setValue]');
    }
}


/***/ }),
/* 26 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzShortProperty", function() { return WzShortProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(2);


/**
 * @public
 */
class WzShortProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImageProperty */ "a"] {
    constructor(name = '', val = 0) {
        super();
        this.name = name;
        this.val = val;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Short;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    get value() {
        return this.val;
    }
    get wzValue() {
        return this.val;
    }
    setValue(value) {
        this.val = value;
    }
    toString() {
        return this.val.toString();
    }
}


/***/ }),
/* 27 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzLongProperty", function() { return WzLongProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(2);


/**
 * @public
 */
class WzLongProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImageProperty */ "a"] {
    constructor(name = '', val = BigInt(0)) {
        super();
        this.name = name;
        this.val = val;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Long;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    get value() {
        return this.val;
    }
    get wzValue() {
        return this.val;
    }
    setValue(value) {
        this.val = BigInt(value);
    }
    toString() {
        return this.val.toString();
    }
}


/***/ }),
/* 28 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzFloatProperty", function() { return WzFloatProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(2);


/**
 * @public
 */
class WzFloatProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImageProperty */ "a"] {
    constructor(name = '', val = 0) {
        super();
        this.name = name;
        this.val = val;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Float;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    get value() {
        return this.val;
    }
    get wzValue() {
        return this.val;
    }
    setValue(value) {
        this.val = value;
    }
    toString() {
        return this.val.toString();
    }
}


/***/ }),
/* 29 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzDoubleProperty", function() { return WzDoubleProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(2);


/**
 * @public
 */
class WzDoubleProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImageProperty */ "a"] {
    constructor(name = '', val = 0) {
        super();
        this.name = name;
        this.val = val;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Double;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    get value() {
        return this.val;
    }
    get wzValue() {
        return this.val;
    }
    setValue(value) {
        this.val = value;
    }
    toString() {
        return this.val.toString();
    }
}


/***/ }),
/* 30 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzStringProperty", function() { return WzStringProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(2);


/**
 * @public
 */
class WzStringProperty extends _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_1__[/* WzImageProperty */ "a"] {
    constructor(name = '', val = '') {
        super();
        this.name = name;
        this.val = val;
        this.parent = null;
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].String;
    }
    dispose() {
        if (this._disposed)
            return;
        this._disposed = true;
    }
    get value() {
        return this.val;
    }
    get wzValue() {
        return this.val;
    }
    setValue(value) {
        this.val = value;
    }
    toString() {
        return this.val;
    }
}


/***/ }),
/* 31 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzSubProperty", function() { return WzSubProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(6);
/* harmony import */ var _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(5);



/**
 * @public
 */
class WzSubProperty extends _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__[/* WzExtended */ "a"] {
    constructor(name = '') {
        super();
        this.name = name;
        this.properties = new Set();
        this.parent = null;
    }
    get wzProperties() {
        return this.properties;
    }
    addProperty(prop) {
        prop.parent = this;
        this.properties.add(prop);
    }
    removeProperty(prop) {
        prop.parent = null;
        this.properties.delete(prop);
    }
    addProperties(props) {
        for (const prop of props) {
            this.addProperty(prop);
        }
    }
    clearProperties() {
        for (const prop of this.properties) {
            prop.parent = null;
        }
        this.properties.clear();
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].SubProperty;
    }
    dispose() {
        if (this._disposed)
            return;
        for (const prop of this.properties) {
            prop.dispose();
        }
        this.properties.clear();
        this._disposed = true;
    }
    setValue(_value) {
        throw new _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_2__[/* NotImplementedError */ "a"]('[WzSubProperty#setValue]');
    }
    set(name, value) {
        if (value != null) {
            value.name = name;
            this.addProperty(value);
        }
    }
    at(name) {
        const nameLower = name.toLowerCase();
        for (const prop of this.properties) {
            if (prop.name.toLowerCase() === nameLower)
                return prop;
        }
        return null;
    }
    getFromPath(path) {
        const segments = path.split(/[\\/]/);
        if (segments[0] === '..') {
            return this.parent.at(segments.slice(1).join('/'));
        }
        let ret = this;
        for (let x = 0; x < segments.length; x++) {
            let foundChild = false;
            const list = ret.wzProperties;
            if (list != null) {
                const l = list;
                for (const iwp of l) {
                    if (iwp.name === segments[x]) {
                        ret = iwp;
                        foundChild = true;
                        break;
                    }
                }
            }
            if (!foundChild) {
                return null;
            }
        }
        return ret;
    }
}


/***/ }),
/* 32 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzCanvasProperty", function() { return WzCanvasProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(6);
/* harmony import */ var _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(2);
/* harmony import */ var _WzImage_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(4);
/* harmony import */ var _WzDirectory_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(13);





/**
 * @public
 */
class WzCanvasProperty extends _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__[/* WzExtended */ "a"] {
    constructor(name = '') {
        super();
        this.name = name;
        this.properties = new Set();
        this.parent = null;
        this.pngProperty = null;
    }
    get inlinkPropertyName() { return '_inlink'; }
    get outlinkPropertyName() { return '_outlink'; }
    get originPropertyName() { return 'origin'; }
    get headPropertyName() { return 'head'; }
    get ltPropertyName() { return 'lt'; }
    get animationDelayPropertyName() { return 'delay'; }
    get wzProperties() {
        return this.properties;
    }
    addProperty(prop) {
        prop.parent = this;
        this.properties.add(prop);
    }
    removeProperty(prop) {
        prop.parent = null;
        this.properties.delete(prop);
    }
    addProperties(props) {
        for (const prop of props) {
            this.addProperty(prop);
        }
    }
    clearProperties() {
        for (const prop of this.properties) {
            prop.parent = null;
        }
        this.properties.clear();
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Canvas;
    }
    dispose() {
        if (this._disposed)
            return;
        for (const prop of this.properties) {
            prop.dispose();
        }
        this.properties.clear();
        if (this.pngProperty != null) {
            this.pngProperty.dispose();
            this.pngProperty = null;
        }
        this._disposed = true;
    }
    get wzValue() {
        return this.pngProperty;
    }
    getProperty(name) {
        const nameLower = name.toLowerCase();
        for (const iwp of this.properties) {
            if (iwp.name.toLowerCase() === nameLower)
                return iwp;
        }
        return null;
    }
    at(name) {
        if (name === 'PNG')
            return this.pngProperty;
        const nameLower = name.toLowerCase();
        for (const iwp of this.properties) {
            if (iwp.name.toLowerCase() === nameLower)
                return iwp;
        }
        return null;
    }
    set(name, value) {
        if (value != null) {
            if (name === 'PNG') {
                this.pngProperty = value;
                return;
            }
            value.name = name;
            this.addProperty(value);
        }
    }
    setValue(value) {
        if (this.pngProperty != null) {
            this.pngProperty.setValue(value);
        }
    }
    getFromPath(path) {
        const segments = path.split(/[\\/]/);
        if (segments[0] === '..') {
            return this.parent.at(segments.slice(1).join('/'));
        }
        let ret = this;
        for (let x = 0; x < segments.length; x++) {
            let foundChild = false;
            if (segments[x] === 'PNG') {
                return this.pngProperty;
            }
            const list = ret.wzProperties;
            if (list != null) {
                const l = list;
                for (const iwp of l) {
                    if (iwp.name === segments[x]) {
                        ret = iwp;
                        foundChild = true;
                        break;
                    }
                }
            }
            if (!foundChild) {
                return null;
            }
        }
        return ret;
    }
    haveInlinkProperty() {
        return this.at(this.inlinkPropertyName) != null;
    }
    haveOutlinkProperty() {
        return this.at(this.outlinkPropertyName) != null;
    }
    async getLinkedWzCanvasBitmap() {
        var _a, _b;
        const _inlink = (_a = this.at(this.inlinkPropertyName)) === null || _a === void 0 ? void 0 : _a.value; // could get nexon'd here. In case they place an _inlink that's not WzStringProperty
        const _outlink = (_b = this.at(this.outlinkPropertyName)) === null || _b === void 0 ? void 0 : _b.value; // could get nexon'd here. In case they place an _outlink that's not WzStringProperty
        if (_inlink != null) {
            let currentWzObj = this; // first object to work with
            while ((currentWzObj = currentWzObj.parent) != null) {
                if (!(currentWzObj instanceof _WzImage_js__WEBPACK_IMPORTED_MODULE_3__[/* WzImage */ "a"]))
                    continue; // keep looping if its not a WzImage
                const wzImageParent = currentWzObj;
                const foundProperty = wzImageParent.getFromPath(_inlink);
                if (foundProperty != null && foundProperty instanceof _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__[/* WzImageProperty */ "a"]) {
                    return await foundProperty.getBitmap();
                }
            }
        }
        else if (_outlink != null) {
            let currentWzObj = this; // first object to work with
            while ((currentWzObj = currentWzObj.parent) != null) {
                if (!(currentWzObj instanceof _WzDirectory_js__WEBPACK_IMPORTED_MODULE_4__[/* WzDirectory */ "a"]))
                    continue; // keep looping if its not a WzImage
                const wzFileParent = (currentWzObj).wzFile;
                const match = wzFileParent.name.match(/^([A-Za-z]+)([0-9]*)\.wz/);
                const prefixWz = match[1] + '/';
                let foundProperty;
                if (_outlink.indexOf(prefixWz) === 0) {
                    // fixed root path
                    const realpath = _outlink.replace(prefixWz, wzFileParent.name.replace('.wz', '') + '/');
                    foundProperty = wzFileParent.getObjectFromPath(realpath);
                }
                else {
                    foundProperty = wzFileParent.getObjectFromPath(_outlink);
                }
                if (foundProperty != null && foundProperty instanceof _WzImageProperty_js__WEBPACK_IMPORTED_MODULE_2__[/* WzImageProperty */ "a"]) {
                    return await foundProperty.getBitmap();
                }
            }
        }
        return await this.getBitmap();
    }
    async getBitmap() {
        if (this.pngProperty != null) {
            return await this.pngProperty.getBitmap();
        }
        return null;
    }
}


/***/ }),
/* 33 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzVectorProperty", function() { return WzVectorProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(6);
/* harmony import */ var _WzIntProperty_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(16);



/**
 * @public
 */
class WzVectorProperty extends _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__[/* WzExtended */ "a"] {
    constructor(name = '', x, y) {
        super();
        this.name = '';
        this.parent = null;
        this.x = null;
        this.y = null;
        this.name = name;
        if (x != null)
            this.x = new _WzIntProperty_js__WEBPACK_IMPORTED_MODULE_2__["WzIntProperty"]('', x);
        if (y != null)
            this.y = new _WzIntProperty_js__WEBPACK_IMPORTED_MODULE_2__["WzIntProperty"]('', y);
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Vector;
    }
    dispose() {
        if (this._disposed)
            return;
        if (this.x != null) {
            this.x.dispose();
            this.x = null;
        }
        if (this.y != null) {
            this.y.dispose();
            this.y = null;
        }
        this._disposed = true;
    }
    get wzValue() {
        return {
            x: this.x != null ? this.x.value : 0,
            y: this.y != null ? this.y.value : 0
        };
    }
    setValue(value) {
        if (this.x != null)
            this.x.setValue(value.x);
        if (this.y != null)
            this.y.setValue(value.y);
    }
    toString() {
        if (this.x != null && this.y != null) {
            return 'X: ' + this.x.value.toString() + ', Y: ' + this.y.value.toString();
        }
        return '';
    }
}


/***/ }),
/* 34 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzConvexProperty", function() { return WzConvexProperty; });
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);
/* harmony import */ var _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(6);
/* harmony import */ var _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(5);



/**
 * @public
 */
class WzConvexProperty extends _WzExtended_js__WEBPACK_IMPORTED_MODULE_1__[/* WzExtended */ "a"] {
    constructor(name = '') {
        super();
        this.name = name;
        this.properties = new Set();
        this.parent = null;
    }
    get wzProperties() {
        return this.properties;
    }
    addProperty(prop) {
        prop.parent = this;
        this.properties.add(prop);
    }
    removeProperty(prop) {
        prop.parent = null;
        this.properties.delete(prop);
    }
    addProperties(props) {
        for (const prop of props) {
            this.addProperty(prop);
        }
    }
    clearProperties() {
        for (const prop of this.properties) {
            prop.parent = null;
        }
        this.properties.clear();
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_0__[/* WzPropertyType */ "a"].Convex;
    }
    dispose() {
        if (this._disposed)
            return;
        for (const prop of this.properties) {
            prop.dispose();
        }
        this.properties.clear();
        this._disposed = true;
    }
    setValue(_value) {
        throw new _util_NotImplementedError_js__WEBPACK_IMPORTED_MODULE_2__[/* NotImplementedError */ "a"]('[WzConvexProperty#setValue]');
    }
    at(name) {
        const nameLower = name.toLowerCase();
        for (const prop of this.properties) {
            if (prop.name.toLowerCase() === nameLower)
                return prop;
        }
        return null;
    }
    getProperty(name) {
        const nameLower = name.toLowerCase();
        for (const prop of this.properties) {
            if (prop.name.toLowerCase() === nameLower)
                return prop;
        }
        return null;
    }
    getFromPath(path) {
        const segments = path.split(/[\\/]/);
        if (segments[0] === '..') {
            return this.parent.at(segments.slice(1).join('/'));
        }
        let ret = this;
        for (let x = 0; x < segments.length; x++) {
            let foundChild = false;
            const list = ret.wzProperties;
            if (list != null) {
                const l = list;
                for (const iwp of l) {
                    if (iwp.name === segments[x]) {
                        ret = iwp;
                        foundChild = true;
                        break;
                    }
                }
            }
            if (!foundChild) {
                return null;
            }
        }
        return ret;
    }
}


/***/ }),
/* 35 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "WzBinaryProperty", function() { return WzBinaryProperty; });
/* harmony import */ var _WzExtended_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(6);
/* harmony import */ var _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(1);
/* harmony import */ var _util_node_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(0);



/**
 * @public
 */
class WzBinaryProperty extends _WzExtended_js__WEBPACK_IMPORTED_MODULE_0__[/* WzExtended */ "a"] {
    constructor(name, reader) {
        super();
        this.parent = null;
        this.mp3bytes = null;
        this.soundDataLen = 0;
        this.length = 0;
        this.offs = 0;
        this.name = name;
        this.wzReader = reader;
        this.offs = 0;
    }
    static async create(name, reader, parseNow) {
        const self = new WzBinaryProperty(name, reader);
        self.wzReader.pos++;
        // note - soundDataLen does NOT include the length of the header.
        self.soundDataLen = await self.wzReader.readWzInt();
        self.length = await self.wzReader.readWzInt();
        const headerOff = self.wzReader.pos;
        self.wzReader.pos += WzBinaryProperty.soundHeader.length;
        const wavFormatLen = await self.wzReader.readUInt8();
        self.wzReader.pos = headerOff;
        self.header = await self.wzReader.read(WzBinaryProperty.soundHeader.length + 1 + wavFormatLen);
        // this.parseWzSoundPropertyHeader()
        // sound file offs
        self.offs = self.wzReader.pos;
        if (parseNow) {
            self.mp3bytes = await self.wzReader.read(self.soundDataLen);
        }
        else {
            self.wzReader.pos += self.soundDataLen;
        }
        return self;
    }
    setValue(_value) { }
    get wzValue() {
        return this.getBytes(false);
    }
    async getBytes(saveInMemory = false) {
        if (this.mp3bytes != null) {
            return this.mp3bytes;
        }
        // if (this.wzReader == null) return null
        const currentPos = this.wzReader.pos;
        this.wzReader.pos = this.offs;
        this.mp3bytes = await this.wzReader.read(this.soundDataLen);
        this.wzReader.pos = currentPos;
        if (saveInMemory) {
            return this.mp3bytes;
        }
        const result = this.mp3bytes;
        this.mp3bytes = null;
        return result;
    }
    async saveToFile(file) {
        if (typeof window !== 'undefined') {
            const a = window.document.createElement('a');
            a.download = file;
            a.href = URL.createObjectURL(new Blob([await this.getBytes(false)], { type: 'audio/mp3' }));
            const event = new MouseEvent('click', {
                view: window,
                bubbles: true,
                cancelable: true
            });
            a.dispatchEvent(event);
            a.remove();
            return;
        }
        try {
            _util_node_js__WEBPACK_IMPORTED_MODULE_2__[/* fs */ "e"].mkdirSync(_util_node_js__WEBPACK_IMPORTED_MODULE_2__[/* path */ "g"].dirname(file), { recursive: true });
        }
        catch (_) { }
        _util_node_js__WEBPACK_IMPORTED_MODULE_2__[/* fs */ "e"].writeFileSync(file, await this.getBytes(false));
    }
    get propertyType() {
        return _WzPropertyType_js__WEBPACK_IMPORTED_MODULE_1__[/* WzPropertyType */ "a"].Sound;
    }
    dispose() {
        if (this._disposed)
            return;
        this.mp3bytes = null;
        this._disposed = true;
    }
}
WzBinaryProperty.soundHeader = [
    0x02,
    0x83, 0xEB, 0x36, 0xE4, 0x4F, 0x52, 0xCE, 0x11, 0x9F, 0x53, 0x00, 0x20, 0xAF, 0x0B, 0xA7, 0x70,
    0x8B, 0xEB, 0x36, 0xE4, 0x4F, 0x52, 0xCE, 0x11, 0x9F, 0x53, 0x00, 0x20, 0xAF, 0x0B, 0xA7, 0x70,
    0x00,
    0x01,
    0x81, 0x9F, 0x58, 0x05, 0x56, 0xC3, 0xCE, 0x11, 0xBF, 0x01, 0x00, 0xAA, 0x00, 0x55, 0x59, 0x5A
];


/***/ }),
/* 36 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, "WzPngProperty", function() { return /* binding */ WzPngProperty_WzPngProperty; });

// EXTERNAL MODULE: ./lib/esm-modern/WzPropertyType.js
var WzPropertyType = __webpack_require__(1);

// EXTERNAL MODULE: ./lib/esm-modern/WzExtended.js
var WzExtended = __webpack_require__(6);

// EXTERNAL MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/index.js + 7 modules
var esm_modern = __webpack_require__(9);

// CONCATENATED MODULE: ./lib/esm-modern/util/Color.js
class Color {
    constructor(r, g, b, a) {
        Object.defineProperties(this, {
            r: createGetSetDescriptor(r),
            g: createGetSetDescriptor(g),
            b: createGetSetDescriptor(b),
            a: createGetSetDescriptor(a)
        });
    }
    static fromRgb(r, g, b) {
        assertByteRange(r);
        assertByteRange(g);
        assertByteRange(b);
        return new Color(r, g, b, 255);
    }
    static fromArgb(a, r, g, b) {
        assertByteRange(a);
        assertByteRange(r);
        assertByteRange(g);
        assertByteRange(b);
        return new Color(r, g, b, a);
    }
    static fromAc(a, color) {
        assertByteRange(a);
        return new Color(color.r, color.g, color.b, a);
    }
}
Color.black = new Color(0, 0, 0, 255);
function createGetSetDescriptor(val) {
    return {
        configurable: true,
        enumerable: true,
        get: () => val,
        set: (v) => {
            assertByteRange(v);
            val = v;
        }
    };
}
function assertByteRange(n) {
    if (n >>> 8 !== 0 || Number.isNaN(n))
        throw new RangeError('RGB out of range');
}

// EXTERNAL MODULE: ./lib/esm-modern/util/ErrorLogger.js
var ErrorLogger = __webpack_require__(3);

// EXTERNAL MODULE: ./lib/esm-modern/util/NotImplementedError.js
var NotImplementedError = __webpack_require__(5);

// EXTERNAL MODULE: ./lib/esm-modern/util/node.js
var node = __webpack_require__(0);

// EXTERNAL MODULE: ./lib/esm-modern/util/Canvas.js
var Canvas = __webpack_require__(7);

// CONCATENATED MODULE: ./lib/esm-modern/properties/WzPngProperty.js








let wasmInflate = null;
/**
 * @public
 */
class WzPngProperty_WzPngProperty extends WzExtended["a" /* WzExtended */] {
    constructor(reader /* , parseNow: boolean = false */) {
        super();
        this.parent = null;
        this.compressedImageBytes = null;
        this.png = null;
        this.listWzUsed = false;
        this.width = 0;
        this.height = 0;
        this.format1 = 0;
        this.format2 = 0;
        this.offs = 0;
        this.wzReader = reader;
        Object.defineProperty(this, 'format', {
            configurable: true,
            enumerable: true,
            get: () => this.format1 + this.format2,
            set: (value) => {
                this.format1 = value;
                this.format2 = 0;
            }
        });
    }
    get propertyType() {
        return WzPropertyType["a" /* WzPropertyType */].PNG;
    }
    dispose() {
        if (this._disposed)
            return;
        this.compressedImageBytes = null;
        if (this.png != null) {
            this.png.dispose();
            this.png = null;
        }
        this.wzReader = null;
        this._disposed = true;
    }
    get wzValue() {
        return this.getImage(false);
    }
    get name() {
        return 'PNG';
    }
    static async create(reader) {
        const self = new WzPngProperty_WzPngProperty(reader);
        self.width = await reader.readWzInt();
        self.height = await reader.readWzInt();
        self.format1 = await reader.readWzInt();
        self.format2 = await reader.readUInt8();
        reader.pos += 4;
        self.offs = reader.pos;
        const len = await reader.readInt32LE() - 1;
        reader.pos += 1;
        if (len > 0) {
            /* if (parseNow) {
              if (this.wzReader == null) {
                this.compressedImageBytes = reader.read(len)
              } else {
                this.compressedImageBytes = this.wzReader.read(len)
              }
      
              // this.parsePng()
            } else { */
            reader.pos += len;
            /* } */
        }
        return self;
    }
    setValue(_value) {
        throw new NotImplementedError["a" /* NotImplementedError */]('[WzPngProperty#setValue]');
    }
    async getImage(saveInMemory = false) {
        if (this.png == null) {
            await this.parsePng(saveInMemory);
        }
        return this.png;
    }
    async getCompressedBytes(saveInMemory = false) {
        if (this.compressedImageBytes == null) {
            const pos = this.wzReader.pos;
            this.wzReader.pos = this.offs;
            const len = await this.wzReader.readInt32LE() - 1;
            if (len <= 0) {
                // possibility an image written with the wrong wzIv
                throw new Error('The length of the image is negative. WzPngProperty. Wrong WzIV?');
            }
            this.wzReader.pos += 1;
            /* if (len > 0) */ this.compressedImageBytes = await this.wzReader.read(len);
            this.wzReader.pos = pos;
        }
        if (!saveInMemory) {
            // were removing the referance to compressedBytes, so a backup for the ret value is needed
            const returnBytes = this.compressedImageBytes;
            this.compressedImageBytes = null;
            return returnBytes;
        }
        return this.compressedImageBytes;
    }
    async parsePng(saveInMemory = false) {
        const rawBytes = await this._getRawImage(saveInMemory);
        if (rawBytes === null) {
            this.png = null;
            return;
        }
        const format = this.format1 + this.format2;
        let bmp = null;
        switch (format) {
            case 1: {
                const decoded = WzPngProperty_WzPngProperty.getPixelDataBgra4444(rawBytes, this.width, this.height);
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                // img.colorType(6)
                bgra8888(img, decoded, decoded.length);
                bmp = img;
                break;
            }
            case 2: {
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                bgra8888(img, rawBytes, rawBytes.length);
                bmp = img;
                break;
            }
            case 3: {
                const decoded = WzPngProperty_WzPngProperty.getPixelDataDXT3(rawBytes, this.width, this.height);
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                bgra8888(img, decoded, this.width * this.height);
                bmp = img;
                break;
            }
            case 257: { // http://forum.ragezone.com/f702/wz-png-format-decode-code-1114978/index2.html#post9053713
                // "Npc.wz\\2570101.img\\info\\illustration2\\face\\0"
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                argb1555(img, rawBytes, rawBytes.length);
                bmp = img;
                break;
            }
            case 513: {
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                rgb565(img, rawBytes, rawBytes.length);
                bmp = img;
                break;
            }
            case 517: {
                const decoded = WzPngProperty_WzPngProperty.getPixelDataForm517(rawBytes, this.width, this.height);
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                rgb565(img, decoded, decoded.length);
                bmp = img;
                break;
            }
            case 1026: {
                const decoded = WzPngProperty_WzPngProperty.getPixelDataDXT3(rawBytes, this.width, this.height);
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                bgra8888(img, decoded, decoded.length);
                bmp = img;
                break;
            }
            case 2050: {
                const decoded = WzPngProperty_WzPngProperty.getPixelDataDXT5(rawBytes, this.width, this.height);
                const img = new Canvas["a" /* Canvas */](this.width, this.height);
                bgra8888(img, decoded, decoded.length);
                bmp = img;
                break;
            }
            default: {
                ErrorLogger["b" /* ErrorLogger */].log(ErrorLogger["a" /* ErrorLevel */].MissingFeature, `Unknown PNG format ${this.format1} ${this.format2}`);
                break;
            }
        }
        this.png = bmp;
    }
    async _getRawImage(saveInMemory = false) {
        const rawImageBytes = await this.getCompressedBytes(saveInMemory);
        const reader = new esm_modern["b" /* BinaryReader */](rawImageBytes);
        const header = reader.readUInt16LE();
        this.listWzUsed = header !== 0x9C78 && header !== 0xDA78 && header !== 0x0178 && header !== 0x5E78;
        let data;
        if (!this.listWzUsed) {
            data = rawImageBytes;
        }
        else {
            reader.pos -= 2;
            const dataStream = [];
            let blocksize = 0;
            const endOfPng = rawImageBytes.length;
            while (reader.pos < endOfPng) {
                blocksize = reader.readInt32LE();
                for (let i = 0; i < blocksize; i++) {
                    dataStream.push(((reader.readUInt8() ^ this.parentImage.reader.wzKey.at(i)) >>> 0) & 0xFF);
                }
            }
            data = new Uint8Array(dataStream);
        }
        reader.dispose();
        const format = this.format1 + this.format2;
        let uncompressedSize = 0;
        let decBuf = null;
        switch (format) {
            case 1: {
                uncompressedSize = this.width * this.height * 2;
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            case 2: {
                uncompressedSize = this.width * this.height * 4;
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            case 3: {
                uncompressedSize = this.width * this.height * 4;
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            case 257: { // http://forum.ragezone.com/f702/wz-png-format-decode-code-1114978/index2.html#post9053713
                // "Npc.wz\\2570101.img\\info\\illustration2\\face\\0"
                uncompressedSize = this.width * this.height * 2;
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            case 513: {
                uncompressedSize = this.width * this.height * 2;
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            case 517: {
                uncompressedSize = parseInt(this.width * this.height / 128);
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            case 1026: {
                uncompressedSize = this.width * this.height * 4;
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            case 2050: {
                uncompressedSize = this.width * this.height;
                decBuf = await inflate(data, uncompressedSize);
                break;
            }
            default: {
                ErrorLogger["b" /* ErrorLogger */].log(ErrorLogger["a" /* ErrorLevel */].MissingFeature, `Unknown PNG format ${this.format1} ${this.format2}`);
                break;
            }
        }
        return decBuf;
    }
    static getPixelDataBgra4444(rawData, width, height) {
        let b = 0;
        let g = 0;
        let byteAtPosition = 0;
        const uncompressedSize = width * height * 2;
        const argb = new Uint8Array(uncompressedSize * 2);
        for (let i = 0; i < uncompressedSize; i++) {
            byteAtPosition = rawData[i];
            b = byteAtPosition & 0x0F;
            b |= (b << 4);
            argb[i * 2] = (b >>> 0) & 0xff;
            g = byteAtPosition & 0xF0;
            g |= (g >> 4);
            argb[i * 2 + 1] = (g >>> 0) & 0xff;
        }
        return argb;
    }
    static getPixelDataForm517(rawData, width, height) {
        const pixel = new Uint8Array(width * height * 2);
        let lineIndex = 0;
        for (let j0 = 0, j1 = parseInt((height / 16)); j0 < j1; j0++) {
            let dstIndex = lineIndex;
            for (let i0 = 0, i1 = parseInt((width / 16)); i0 < i1; i0++) {
                const idx = (i0 + j0 * i1) * 2;
                const b0 = rawData[idx];
                const b1 = rawData[idx + 1];
                for (let k = 0; k < 16; k++) {
                    pixel[dstIndex++] = b0;
                    pixel[dstIndex++] = b1;
                }
            }
            for (let k = 1; k < 16; k++) {
                const src = new Uint8Array(pixel.buffer, pixel.byteOffset + lineIndex, width * 2);
                pixel.set(src, dstIndex);
                // pixel.copy(pixel, dstIndex, lineIndex, lineIndex + (width * 2))
                dstIndex += width * 2;
            }
            lineIndex += width * 32;
        }
        return pixel;
    }
    static getPixelDataDXT5(rawData, width, height) {
        const pixel = new Uint8Array(width * height * 4);
        const colorTable = [
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0)
        ];
        const colorIdxTable = Array(16).fill(0);
        const alphaTable = new Uint8Array(8);
        const alphaIdxTable = Array(16).fill(0);
        for (let y = 0; y < height; y += 4) {
            for (let x = 0; x < width; x += 4) {
                const off = x * 4 + y * width;
                WzPngProperty_WzPngProperty.expandAlphaTableDXT5(alphaTable, rawData[off + 0], rawData[off + 1]);
                WzPngProperty_WzPngProperty.expandAlphaIndexTableDXT5(alphaIdxTable, rawData, off + 2);
                const r = new esm_modern["b" /* BinaryReader */](rawData);
                r.seek(off + 8);
                const u0 = r.readUInt16LE(); // rawData.readUInt16LE(off + 8)
                const u1 = r.readUInt16LE(); // rawData.readUInt16LE(off + 10)
                r.dispose();
                WzPngProperty_WzPngProperty.expandColorTable(colorTable, u0, u1);
                WzPngProperty_WzPngProperty.expandColorIndexTable(colorIdxTable, rawData, off + 12);
                for (let j = 0; j < 4; j++) {
                    for (let i = 0; i < 4; i++) {
                        WzPngProperty_WzPngProperty.setPixel(pixel, x + i, y + j, width, colorTable[colorIdxTable[j * 4 + i]], alphaTable[alphaIdxTable[j * 4 + i]]);
                    }
                }
            }
        }
        return pixel;
    }
    static expandAlphaTableDXT5(alpha, a0, a1) {
        alpha[0] = a0;
        alpha[1] = a1;
        if (a0 > a1) {
            for (let i = 2; i < 8; i++) {
                alpha[i] = (parseInt(((8 - i) * a0 + (i - 1) * a1 + 3) / 7) >>> 0) & 0xff;
            }
        }
        else {
            for (let i = 2; i < 6; i++) {
                alpha[i] = (parseInt(((6 - i) * a0 + (i - 1) * a1 + 2) / 5) >>> 0) & 0xff;
            }
            alpha[6] = 0;
            alpha[7] = 255;
        }
    }
    static expandAlphaIndexTableDXT5(alphaIndex, rawData, offset) {
        for (let i = 0; i < 16; i += 8, offset += 3) {
            const flags = rawData[offset] |
                (rawData[offset + 1] << 8) |
                (rawData[offset + 2] << 16);
            for (let j = 0; j < 8; j++) {
                const mask = 0x07 << (3 * j);
                alphaIndex[i + j] = (flags & mask) >> (3 * j);
            }
        }
    }
    static getPixelDataDXT3(rawData, width, height) {
        const pixel = new Uint8Array(width * height * 4);
        const colorTable = [
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0)
        ];
        const colorIdxTable = Array(16).fill(0);
        const alphaTable = new Uint8Array(16);
        for (let y = 0; y < height; y += 4) {
            for (let x = 0; x < width; x += 4) {
                const off = x * 4 + y * width;
                WzPngProperty_WzPngProperty.expandAlphaTableDXT3(alphaTable, rawData, off);
                const r = new esm_modern["b" /* BinaryReader */](rawData);
                r.seek(off + 8);
                const u0 = r.readUInt16LE(); // rawData.readUInt16LE(off + 8)
                const u1 = r.readUInt16LE(); // rawData.readUInt16LE(off + 10)
                r.dispose();
                WzPngProperty_WzPngProperty.expandColorTable(colorTable, u0, u1);
                WzPngProperty_WzPngProperty.expandColorIndexTable(colorIdxTable, rawData, off + 12);
                for (let j = 0; j < 4; j++) {
                    for (let i = 0; i < 4; i++) {
                        WzPngProperty_WzPngProperty.setPixel(pixel, x + i, y + j, width, colorTable[colorIdxTable[j * 4 + i]], alphaTable[j * 4 + i]);
                    }
                }
            }
        }
        return pixel;
    }
    static expandAlphaTableDXT3(alpha, rawData, offset) {
        for (let i = 0; i < 16; i += 2, offset++) {
            alpha[i + 0] = (rawData[offset] & 0x0f) >>> 0;
            alpha[i + 1] = ((rawData[offset] & 0xf0) >> 4) >>> 0;
        }
        for (let i = 0; i < 16; i++) {
            alpha[i] = ((alpha[i] | (alpha[i] << 4)) >>> 0) & 0xff;
        }
    }
    static setPixel(pixelData, x, y, width, color, alpha) {
        const offset = (y * width + x) * 4;
        pixelData[offset + 0] = color.b;
        pixelData[offset + 1] = color.g;
        pixelData[offset + 2] = color.r;
        pixelData[offset + 3] = (alpha >>> 0) & 0xff;
    }
    static expandColorTable(color, c0, c1) {
        color[0] = rgb565ToColor(c0);
        color[1] = rgb565ToColor(c1);
        if (c0 > c1) {
            color[2] = Color.fromArgb(0xff, parseInt((color[0].r * 2 + color[1].r + 1) / 3), parseInt((color[0].g * 2 + color[1].g + 1) / 3), parseInt((color[0].b * 2 + color[1].b + 1) / 3));
            color[3] = Color.fromArgb(0xff, parseInt((color[0].r + color[1].r * 2 + 1) / 3), parseInt((color[0].g + color[1].g * 2 + 1) / 3), parseInt((color[0].b + color[1].b * 2 + 1) / 3));
        }
        else {
            color[2] = Color.fromArgb(0xff, parseInt((color[0].r + color[1].r) / 2), parseInt((color[0].g + color[1].g) / 2), parseInt((color[0].b + color[1].b) / 2));
            color[3] = Color.fromAc(0xff, Color.black);
        }
    }
    static expandColorIndexTable(colorIndex, rawData, offset) {
        for (let i = 0; i < 16; i += 4, offset++) {
            colorIndex[i + 0] = (rawData[offset] & 0x03);
            colorIndex[i + 1] = (rawData[offset] & 0x0c) >> 2;
            colorIndex[i + 2] = (rawData[offset] & 0x30) >> 4;
            colorIndex[i + 3] = (rawData[offset] & 0xc0) >> 6;
        }
    }
    getBitmap() {
        return this.getImage(false);
    }
    async saveToFile(file) {
        if (this.png != null) {
            await this.png.writeAsync(file);
            return true;
        }
        const png = await this.getImage(false);
        if (png != null) {
            await png.writeAsync(file);
            return true;
        }
        return false;
    }
}
function inflate(data, len) {
    if (typeof window !== 'undefined') {
        return inflateWasm(data, len);
    }
    return new Promise((resolve, reject) => {
        const inflateStream = node["j" /* zlib */].createInflate();
        const buf = node["b" /* _Buffer */].alloc(len);
        const chunks = [];
        inflateStream.once('error', reject);
        inflateStream.on('data', (chunk) => {
            chunks.push(chunk);
        });
        inflateStream.on('finish', () => {
            const chunk = node["b" /* _Buffer */].concat(chunks);
            chunk.copy(buf, 0, 0, Math.min(chunk.length, len));
            resolve(buf);
            inflateStream.close();
        });
        inflateStream.end(data);
    });
}
async function inflateWasm(data, len) {
    if (wasmInflate === null) {
        const wzWasm = await Promise.resolve(/* import() */).then(__webpack_require__.t.bind(null, 37, 7));
        /* const { Module } =  */ await wzWasm.default();
        wasmInflate = wzWasm.inflate;
    }
    const buf = wasmInflate(data, len);
    return buf;
}
function rgb565ToColor(val) {
    const rgb565maskR = 0xf800;
    const rgb565maskG = 0x07e0;
    const rgb565maskB = 0x001f;
    const r = (val & rgb565maskR) >> 11;
    const g = (val & rgb565maskG) >> 5;
    const b = (val & rgb565maskB);
    const c = Color.fromRgb((r << 3) | (r >> 2), (g << 2) | (g >> 4), (b << 3) | (b >> 2));
    return c;
}
function bgra8888(img, data, length) {
    let x = 0;
    let y = 0;
    const width = img.getWidth();
    for (let i = 0; i < length; i += 4) {
        img.setPixelColor(Canvas["a" /* Canvas */].rgbaToInt(data[i + 2], data[i + 1], data[i + 0], data[i + 3]), x, y);
        x++;
        if (x >= width) {
            x = 0;
            y++;
        }
    }
}
function rgb565(img, data, length) {
    // rrrrrggg gggbbbbb
    let x = 0;
    let y = 0;
    const width = img.getWidth();
    const r = new esm_modern["b" /* BinaryReader */](data);
    for (let i = 0; i < length; i += 2) {
        const ushort = r.readUInt16LE(); // data.readUInt16LE(i)
        const c = rgb565ToColor(ushort);
        img.setPixelColor(Canvas["a" /* Canvas */].rgbaToInt(c.r, c.g, c.b, c.a), x, y);
        x++;
        if (x >= width) {
            x = 0;
            y++;
        }
    }
    r.dispose();
}
function argb1555(img, data, length) {
    // arrrrrgg gggbbbbb
    let x = 0;
    let y = 0;
    const width = img.getWidth();
    const r = new esm_modern["b" /* BinaryReader */](data);
    for (let i = 0; i < length; i += 2) {
        const ushort = r.readUInt16LE(); // data.readUInt16LE(i)
        const c = argb1555ToColor(ushort);
        img.setPixelColor(Canvas["a" /* Canvas */].rgbaToInt(c.r, c.g, c.b, c.a), x, y);
        x++;
        if (x >= width) {
            x = 0;
            y++;
        }
    }
    r.dispose();
}
function argb1555ToColor(val) {
    const maskA = 0x8000;
    const maskR = 0x7c00;
    const maskG = 0x03e0;
    const maskB = 0x001f;
    const a = ((val & maskA) !== 0) ? 255 : 0;
    const r = (val & maskR) >> 10;
    const g = (val & maskG) >> 5;
    const b = (val & maskB);
    const c = Color.fromArgb(a, (r << 3) | (r >> 2), (g << 3) | (g >> 2), (b << 3) | (b >> 2));
    return c;
}


/***/ }),
/* 37 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

!function (e, n) { var t; t =  true ? "undefined" != typeof require ? require : void 0 : undefined; var r = e && e.process, i = "wz";  true ? module.exports = n(t, r, e) : undefined; }(function (e) { if ("undefined" != typeof globalThis)
    return globalThis; var n = function () { return this; }(); if (!n && function () { var e; try {
    e = new Function;
}
catch (e) {
    return !1;
} return "function" == typeof e; }() && (n = new Function("return this")()), !n) {
    if (false)
        {}
    if ("undefined" != typeof window)
        return window;
    if ("undefined" != typeof self)
        return self;
} return n || e; }(this), (function (e, n, t, r) { var i, o = ""; try {
    o = document.currentScript.src;
}
catch (e) { } function a(t) { t = void 0 !== t ? t : {}; var i, a, u, f = Object.assign({}, t), l = "object" == typeof window, s = "function" == typeof importScripts, c = "object" == typeof n && "object" == typeof n.versions && "string" == typeof n.versions.node, d = ""; if (c) {
    var p = e("fs"), h = e("path");
    d = s ? h.dirname(d) + "/" : __dirname + "/", i = (e, n) => (e = M(e) ? new URL(e) : h.normalize(e), p.readFileSync(e, n ? void 0 : "utf8")), u = e => { var n = i(e, !0); return n.buffer || (n = new Uint8Array(n)), n; }, a = (e, n, t, r = !0) => { e = M(e) ? new URL(e) : h.normalize(e), p.readFile(e, r ? void 0 : "utf8", ((e, i) => { e ? t(e) : n(r ? i.buffer : i); })); }, !t.thisProgram && n.argv.length > 1 && n.argv[1].replace(/\\/g, "/"), n.argv.slice(2), void 0 !== r && (r.exports = t), n.on("uncaughtException", (e => { if (!("unwind" === e || e instanceof W || e.context instanceof W))
        throw e; })), (e, t) => { throw n.exitCode = e, t; }, t.inspect = () => "[Emscripten Module object]";
}
else
    (l || s) && (d = 0 !== (d = s ? self.location.href : o).indexOf("blob:") ? d.substr(0, d.replace(/[?#].*/, "").lastIndexOf("/") + 1) : "", i = e => { var n = new XMLHttpRequest; return n.open("GET", e, !1), n.send(null), n.responseText; }, s && (u = e => { var n = new XMLHttpRequest; return n.open("GET", e, !1), n.responseType = "arraybuffer", n.send(null), new Uint8Array(n.response); }), a = (e, n, t) => { var r = new XMLHttpRequest; r.open("GET", e, !0), r.responseType = "arraybuffer", r.onload = () => { 200 == r.status || 0 == r.status && r.response ? n(r.response) : t(); }, r.onerror = t, r.send(null); }, e => document.title = e); t.print || console.log.bind(console); var _, m = t.printErr || console.error.bind(console); Object.assign(t, f), f = null, t.arguments && t.arguments, t.thisProgram && t.thisProgram, t.quit && t.quit, t.wasmBinary && (_ = t.wasmBinary); var y, w; t.noExitRuntime; "object" != typeof WebAssembly && I("no native wasm support detected"); var b, v = !1; function g() { var e = y.buffer; t.HEAP8 = new Int8Array(e), t.HEAP16 = new Int16Array(e), t.HEAP32 = new Int32Array(e), t.HEAPU8 = b = new Uint8Array(e), t.HEAPU16 = new Uint16Array(e), t.HEAPU32 = new Uint32Array(e), t.HEAPF32 = new Float32Array(e), t.HEAPF64 = new Float64Array(e); } var A = [], E = [], R = []; var P = 0, z = null, U = null; function I(e) { throw t.onAbort && t.onAbort(e), m(e = "Aborted(" + e + ")"), v = !0, 1, e += ". Build with -sASSERTIONS for more info.", new WebAssembly.RuntimeError(e); } var k, x, H = "data:application/octet-stream;base64,"; function S(e) { return e.startsWith(H); } function M(e) { return e.startsWith("file://"); } function j(e) { if (e == k && _)
    return new Uint8Array(_); if (u)
    return u(e); throw "both async and sync fetching of the wasm failed"; } function T(e, n, t) { return function (e) { if (!_ && (l || s)) {
    if ("function" == typeof fetch && !M(e))
        return fetch(e, { credentials: "same-origin" }).then((n => { if (!n.ok)
            throw "failed to load wasm binary file at '" + e + "'"; return n.arrayBuffer(); })).catch((() => j(e)));
    if (a)
        return new Promise(((n, t) => { a(e, (e => n(new Uint8Array(e))), t); }));
} return Promise.resolve().then((() => j(e))); }(e).then((e => WebAssembly.instantiate(e, n))).then((e => e)).then(t, (e => { m("failed to asynchronously prepare wasm: " + e), I(e); })); } function W(e) { this.name = "ExitStatus", this.message = `Program terminated with exit(${e})`, this.status = e; } S(k = "wz.wasm") || (x = k, k = t.locateFile ? t.locateFile(x, d) : d + x); var F, q = e => { for (; e.length > 0;)
    e.shift()(t); }, O = e => { var n = e - y.buffer.byteLength + 65535 >>> 16; try {
    return y.grow(n), g(), 1;
}
catch (e) { } }, L = { b: (e, n, t) => b.copyWithin(e, n, n + t), a: e => { var n = b.length, t = 2147483648; if ((e >>>= 0) > t)
        return !1; for (var r, i, o = 1; o <= 4; o *= 2) {
        var a = n * (1 + .2 / o);
        a = Math.min(a, e + 100663296);
        var u = Math.min(t, (r = Math.max(e, a)) + ((i = 65536) - r % i) % i);
        if (O(u))
            return !0;
    } return !1; } }; (function () { var e, n, r, i = { a: L }; function o(e, n) { var r, i = e.exports; return y = (w = i).c, g(), w.k, r = w.d, E.unshift(r), function (e) { if (P--, t.monitorRunDependencies && t.monitorRunDependencies(P), 0 == P && (null !== z && (clearInterval(z), z = null), U)) {
    var n = U;
    U = null, n();
} }(), i; } if (P++, t.monitorRunDependencies && t.monitorRunDependencies(P), t.instantiateWasm)
    try {
        return t.instantiateWasm(i, o);
    }
    catch (e) {
        return m("Module.instantiateWasm callback failed with error: " + e), !1;
    } e = k, n = i, r = function (e) { o(e.instance); }, _ || "function" != typeof WebAssembly.instantiateStreaming || S(e) || M(e) || c || "function" != typeof fetch ? T(e, n, r) : fetch(e, { credentials: "same-origin" }).then((t => WebAssembly.instantiateStreaming(t, n).then(r, (function (t) { return m("wasm streaming compile failed: " + t), m("falling back to ArrayBuffer instantiation"), T(e, n, r); })))); })(), t._wz_zlib_inflate = (e, n, r, i) => (t._wz_zlib_inflate = w.e)(e, n, r, i), t._wz_aes_ecb_create_key = e => (t._wz_aes_ecb_create_key = w.f)(e), t._malloc = e => (t._malloc = w.g)(e), t._free = e => (t._free = w.h)(e), t._wz_aes_ecb_destroy_key = e => (t._wz_aes_ecb_destroy_key = w.i)(e), t._wz_aes_ecb_encrypt = (e, n, r) => (t._wz_aes_ecb_encrypt = w.j)(e, n, r); function B() { function e() { F || (F = !0, t.calledRun = !0, v || (!0, q(E), t.onRuntimeInitialized && t.onRuntimeInitialized(), function () { if (t.postRun)
    for ("function" == typeof t.postRun && (t.postRun = [t.postRun]); t.postRun.length;)
        e = t.postRun.shift(), R.unshift(e); var e; q(R); }())); } P > 0 || (!function () { if (t.preRun)
    for ("function" == typeof t.preRun && (t.preRun = [t.preRun]); t.preRun.length;)
        e = t.preRun.shift(), A.unshift(e); var e; q(A); }(), P > 0 || (t.setStatus ? (t.setStatus("Running..."), setTimeout((function () { setTimeout((function () { t.setStatus(""); }), 1), e(); }), 1)) : e())); } if (U = function e() { F || B(), F || (U = e); }, t.preInit)
    for ("function" == typeof t.preInit && (t.preInit = [t.preInit]); t.preInit.length > 0;)
        t.preInit.pop()(); return B(), { Module: t }; } var u = {}; try {
    Object.defineProperty(u, "__esModule", { value: !0 });
}
catch (e) {
    u.__esModule = !0;
} return function (e, t, r) { var o = { Module: null }, u = null; function f(e) { return o.Module ? Promise.resolve(o) : u || (u = new Promise((function (t, r) { i = e = e || {}; var f, l = function (e) { void 0 !== n && n.off("uncaughtException", e); }, s = function (e) { r(e), l(s); }, c = "onAbort" in e, d = e.onAbort, p = "onRuntimeInitialized" in e, h = e.onRuntimeInitialized, _ = function () { c ? e.onAbort = d : delete e.onAbort, p ? e.onRuntimeInitialized = h : delete e.onRuntimeInitialized; }; e.onAbort = function (n) { s(new Error(n)), _(), "function" == typeof e.onAbort && e.onAbort(n); }, e.onRuntimeInitialized = function () { if (_(), "function" == typeof e.onRuntimeInitialized)
    try {
        e.onRuntimeInitialized();
    }
    catch (e) {
        return void s(e);
    } o.Module = e, u = null, t(o), l(s); }, f = s, void 0 !== n && n.on("uncaughtException", f), a(e); })).catch((function (e) { return i = void 0, o.Module = null, u = null, Promise.reject(e); }))); } try {
    Object.defineProperty(e, "default", { enumerable: !0, value: f });
}
catch (n) {
    e.default = f;
} if ("undefined" != typeof Symbol && Symbol.toStringTag)
    try {
        Object.defineProperty(e, Symbol.toStringTag, { value: "Module" });
    }
    catch (e) { } return function (e) { function n(e) { this.k = e, this.remain = new Uint8Array(0), this.autoPadding = !0; } e.inflate = function (e, n) { var t = e.length >>> 0, r = i._malloc(t); if (!r)
    throw new Error("malloc failed"); i.HEAPU8.set(e, r); var o = n >>> 0, a = i._malloc(o); if (!a)
    throw i._free(r), new Error("malloc failed"); var u = i._wz_zlib_inflate(r, t, a, o); if (i._free(r), 0 === u) {
    var f = i.HEAPU8.slice(a, a + o);
    return i._free(a), f;
} throw i._free(a), new Error("Inflate failed"); }, n.prototype.setAutoPadding = function (e) { return this.autoPadding = e, this; }, n.prototype.destroy = function () { return i._wz_aes_ecb_destroy_key(this.k), this; }, n.prototype.update = function (e) { var n = new Uint8Array(this.remain.length + e.length); n.set(this.remain, 0), n.set(e, this.remain.length); var t = n.length, r = i._malloc(t); if (!r)
    throw new Error("malloc failed"); i.HEAPU8.set(n, r); var o = i._malloc(16); if (!o)
    throw i._free(r), new Error("malloc failed"); for (var a = [], u = 0; u < n.length; u += 16) {
    if (n.length - u < 16) {
        this.remain = n.slice(u);
        break;
    }
    i._wz_aes_ecb_encrypt(r + u, o, this.k), a.push(i.HEAPU8.slice(o, o + 16));
} var f = new Uint8Array(16 * a.length); for (u = 0; u < a.length; ++u)
    f.set(a[u], 16 * u); return i._free(o), i._free(r), f; }, n.prototype.final = function () { var e; if (this.autoPadding) {
    (e = new Uint8Array(16)).set(this.remain);
    for (var n = 16 - this.remain.length, t = this.remain.length; t < 16; ++t)
        e[t] = n;
}
else {
    if (this.remain.length % 16 != 0)
        throw new Error("wrong final block length");
    if (0 === this.remain.length)
        return new Uint8Array(0);
    e = this.remain;
} var r = i._malloc(16); if (!r)
    throw new Error("malloc failed"); i.HEAPU8.set(e, r); var o = i._malloc(16); if (!o)
    throw i._free(r), new Error("malloc failed"); i._wz_aes_ecb_encrypt(r, o, this.k); var a = i.HEAPU8.slice(o, o + 16); return i._free(o), i._free(r), a; }, e.aesCreate = function (e) { var t = e.length; if (32 !== t)
    throw new Error("Invalid key"); var r = i._malloc(t); if (!r)
    throw new Error("malloc failed"); i.HEAPU8.set(e, r); var o = i._wz_aes_ecb_create_key(r); return i._free(r), new n(o); }; }(e), e; }(u); }));


/***/ }),
/* 38 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(39);


/***/ }),
/* 39 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, "BaseError", function() { return /* reexport */ BaseError["a" /* BaseError */]; });
__webpack_require__.d(__webpack_exports__, "ErrorLevel", function() { return /* reexport */ ErrorLogger["a" /* ErrorLevel */]; });
__webpack_require__.d(__webpack_exports__, "ErrorLogger", function() { return /* reexport */ ErrorLogger["b" /* ErrorLogger */]; });
__webpack_require__.d(__webpack_exports__, "WzError", function() { return /* reexport */ ErrorLogger["c" /* WzError */]; });
__webpack_require__.d(__webpack_exports__, "WzMutableKey", function() { return /* reexport */ WzMutableKey["a" /* WzMutableKey */]; });
__webpack_require__.d(__webpack_exports__, "WzBinaryReader", function() { return /* reexport */ WzBinaryReader["a" /* WzBinaryReader */]; });
__webpack_require__.d(__webpack_exports__, "NotImplementedError", function() { return /* reexport */ NotImplementedError["a" /* NotImplementedError */]; });
__webpack_require__.d(__webpack_exports__, "Canvas", function() { return /* reexport */ Canvas["a" /* Canvas */]; });
__webpack_require__.d(__webpack_exports__, "WzLuaProperty", function() { return /* reexport */ WzLuaProperty["WzLuaProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzBinaryPropertyType", function() { return /* reexport */ WzBinaryPropertyType; });
__webpack_require__.d(__webpack_exports__, "WzBinaryProperty", function() { return /* reexport */ WzBinaryProperty["WzBinaryProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzCanvasProperty", function() { return /* reexport */ WzCanvasProperty["WzCanvasProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzConvexProperty", function() { return /* reexport */ WzConvexProperty["WzConvexProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzDoubleProperty", function() { return /* reexport */ WzDoubleProperty["WzDoubleProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzFloatProperty", function() { return /* reexport */ WzFloatProperty["WzFloatProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzIntProperty", function() { return /* reexport */ WzIntProperty["WzIntProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzLongProperty", function() { return /* reexport */ WzLongProperty["WzLongProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzNullProperty", function() { return /* reexport */ WzNullProperty["WzNullProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzPngProperty", function() { return /* reexport */ WzPngProperty["WzPngProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzShortProperty", function() { return /* reexport */ WzShortProperty["WzShortProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzStringProperty", function() { return /* reexport */ WzStringProperty["WzStringProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzSubProperty", function() { return /* reexport */ WzSubProperty["WzSubProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzUOLProperty", function() { return /* reexport */ WzUOLProperty["WzUOLProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzVectorProperty", function() { return /* reexport */ WzVectorProperty["WzVectorProperty"]; });
__webpack_require__.d(__webpack_exports__, "WzObjectType", function() { return /* reexport */ WzObjectType["a" /* WzObjectType */]; });
__webpack_require__.d(__webpack_exports__, "WzMapleVersion", function() { return /* reexport */ WzMapleVersion["a" /* WzMapleVersion */]; });
__webpack_require__.d(__webpack_exports__, "WzPropertyType", function() { return /* reexport */ WzPropertyType["a" /* WzPropertyType */]; });
__webpack_require__.d(__webpack_exports__, "WzHeader", function() { return /* reexport */ WzHeader["a" /* WzHeader */]; });
__webpack_require__.d(__webpack_exports__, "WzObject", function() { return /* reexport */ WzObject["a" /* WzObject */]; });
__webpack_require__.d(__webpack_exports__, "WzImageProperty", function() { return /* reexport */ WzImageProperty["a" /* WzImageProperty */]; });
__webpack_require__.d(__webpack_exports__, "WzExtended", function() { return /* reexport */ WzExtended["a" /* WzExtended */]; });
__webpack_require__.d(__webpack_exports__, "WzImage", function() { return /* reexport */ WzImage["a" /* WzImage */]; });
__webpack_require__.d(__webpack_exports__, "WzDirectory", function() { return /* reexport */ WzDirectory["a" /* WzDirectory */]; });
__webpack_require__.d(__webpack_exports__, "WzDirectoryType", function() { return /* reexport */ WzDirectoryType["a" /* WzDirectoryType */]; });
__webpack_require__.d(__webpack_exports__, "WzFileParseStatus", function() { return /* reexport */ WzFileParseStatus; });
__webpack_require__.d(__webpack_exports__, "getErrorDescription", function() { return /* reexport */ getErrorDescription; });
__webpack_require__.d(__webpack_exports__, "WzFile", function() { return /* reexport */ WzFile_WzFile; });
__webpack_require__.d(__webpack_exports__, "MapleStoryLocalisation", function() { return /* reexport */ MapleStoryLocalisation; });
__webpack_require__.d(__webpack_exports__, "WzMainDirectory", function() { return /* reexport */ WzMainDirectory; });
__webpack_require__.d(__webpack_exports__, "walkWzFileAsync", function() { return /* reexport */ walkWzFileAsync; });
__webpack_require__.d(__webpack_exports__, "walkDirectory", function() { return /* reexport */ walkDirectory; });
__webpack_require__.d(__webpack_exports__, "walkPropertyContainer", function() { return /* reexport */ walkPropertyContainer; });
__webpack_require__.d(__webpack_exports__, "init", function() { return /* reexport */ init; });

// EXTERNAL MODULE: ./lib/esm-modern/util/BaseError.js
var BaseError = __webpack_require__(18);

// EXTERNAL MODULE: ./lib/esm-modern/util/ErrorLogger.js
var ErrorLogger = __webpack_require__(3);

// EXTERNAL MODULE: ./lib/esm-modern/util/WzMutableKey.js
var WzMutableKey = __webpack_require__(22);

// EXTERNAL MODULE: ./lib/esm-modern/util/WzBinaryReader.js
var WzBinaryReader = __webpack_require__(19);

// EXTERNAL MODULE: ./lib/esm-modern/util/NotImplementedError.js
var NotImplementedError = __webpack_require__(5);

// EXTERNAL MODULE: ./lib/esm-modern/util/Canvas.js
var Canvas = __webpack_require__(7);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzLuaProperty.js
var WzLuaProperty = __webpack_require__(24);

// CONCATENATED MODULE: ./lib/esm-modern/properties/WzBinaryPropertyType.js
/**
 * @public
 */
var WzBinaryPropertyType;
(function (WzBinaryPropertyType) {
    WzBinaryPropertyType[WzBinaryPropertyType["Raw"] = 0] = "Raw";
    WzBinaryPropertyType[WzBinaryPropertyType["MP3"] = 1] = "MP3";
    WzBinaryPropertyType[WzBinaryPropertyType["WAV"] = 2] = "WAV";
})(WzBinaryPropertyType || (WzBinaryPropertyType = {}));

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzBinaryProperty.js
var WzBinaryProperty = __webpack_require__(35);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzCanvasProperty.js
var WzCanvasProperty = __webpack_require__(32);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzConvexProperty.js
var WzConvexProperty = __webpack_require__(34);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzDoubleProperty.js
var WzDoubleProperty = __webpack_require__(29);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzFloatProperty.js
var WzFloatProperty = __webpack_require__(28);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzIntProperty.js
var WzIntProperty = __webpack_require__(16);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzLongProperty.js
var WzLongProperty = __webpack_require__(27);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzNullProperty.js
var WzNullProperty = __webpack_require__(25);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzPngProperty.js + 1 modules
var WzPngProperty = __webpack_require__(36);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzShortProperty.js
var WzShortProperty = __webpack_require__(26);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzStringProperty.js
var WzStringProperty = __webpack_require__(30);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzSubProperty.js
var WzSubProperty = __webpack_require__(31);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzUOLProperty.js
var WzUOLProperty = __webpack_require__(17);

// EXTERNAL MODULE: ./lib/esm-modern/properties/WzVectorProperty.js
var WzVectorProperty = __webpack_require__(33);

// EXTERNAL MODULE: ./lib/esm-modern/WzObjectType.js
var WzObjectType = __webpack_require__(8);

// EXTERNAL MODULE: ./lib/esm-modern/WzMapleVersion.js
var WzMapleVersion = __webpack_require__(10);

// EXTERNAL MODULE: ./lib/esm-modern/WzPropertyType.js
var WzPropertyType = __webpack_require__(1);

// EXTERNAL MODULE: ./lib/esm-modern/WzHeader.js
var WzHeader = __webpack_require__(14);

// EXTERNAL MODULE: ./lib/esm-modern/WzObject.js
var WzObject = __webpack_require__(12);

// EXTERNAL MODULE: ./lib/esm-modern/WzImageProperty.js
var WzImageProperty = __webpack_require__(2);

// EXTERNAL MODULE: ./lib/esm-modern/WzExtended.js
var WzExtended = __webpack_require__(6);

// EXTERNAL MODULE: ./lib/esm-modern/WzImage.js
var WzImage = __webpack_require__(4);

// EXTERNAL MODULE: ./lib/esm-modern/WzDirectory.js
var WzDirectory = __webpack_require__(13);

// EXTERNAL MODULE: ./lib/esm-modern/WzDirectoryType.js
var WzDirectoryType = __webpack_require__(15);

// CONCATENATED MODULE: ./lib/esm-modern/WzFileParseStatus.js
/** @public */
var WzFileParseStatus;
(function (WzFileParseStatus) {
    WzFileParseStatus[WzFileParseStatus["PATH_IS_NULL"] = -1] = "PATH_IS_NULL";
    WzFileParseStatus[WzFileParseStatus["ERROR_GAME_VER_HASH"] = -2] = "ERROR_GAME_VER_HASH";
    WzFileParseStatus[WzFileParseStatus["FAILED_UNKNOWN"] = 0] = "FAILED_UNKNOWN";
    WzFileParseStatus[WzFileParseStatus["SUCCESS"] = 1] = "SUCCESS";
})(WzFileParseStatus || (WzFileParseStatus = {}));
/** @public */
function getErrorDescription(status) {
    switch (status) {
        case WzFileParseStatus.SUCCESS:
            return 'Success';
        case WzFileParseStatus.FAILED_UNKNOWN:
            return 'Failed, in this case the causes are undetermined.';
        case WzFileParseStatus.PATH_IS_NULL:
            return 'Path is null';
        case WzFileParseStatus.ERROR_GAME_VER_HASH:
            return 'Error with game version hash : The specified game version is incorrect and WzLib was unable to determine the version itself';
        default: return '';
    }
}

// EXTERNAL MODULE: ./lib/esm-modern/util/node.js
var node = __webpack_require__(0);

// EXTERNAL MODULE: ./node_modules/@tybys/binreader/lib/esm-modern/index.js + 7 modules
var esm_modern = __webpack_require__(9);

// EXTERNAL MODULE: ./lib/esm-modern/util/WzKeyGenerator.js
var WzKeyGenerator = __webpack_require__(20);

// EXTERNAL MODULE: ./lib/esm-modern/util/WzTool.js
var WzTool = __webpack_require__(21);

// CONCATENATED MODULE: ./lib/esm-modern/WzFile.js













/** @public */
var MapleStoryLocalisation;
(function (MapleStoryLocalisation) {
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryKorea"] = 1] = "MapleStoryKorea";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryKoreaTespia"] = 2] = "MapleStoryKoreaTespia";
    MapleStoryLocalisation[MapleStoryLocalisation["Unknown3"] = 3] = "Unknown3";
    MapleStoryLocalisation[MapleStoryLocalisation["Unknown4"] = 4] = "Unknown4";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryTespia"] = 5] = "MapleStoryTespia";
    MapleStoryLocalisation[MapleStoryLocalisation["Unknown6"] = 6] = "Unknown6";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStorySEA"] = 7] = "MapleStorySEA";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryGlobal"] = 8] = "MapleStoryGlobal";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryEurope"] = 9] = "MapleStoryEurope";
    MapleStoryLocalisation[MapleStoryLocalisation["Not_Known"] = 999] = "Not_Known";
    // TODO: other values
})(MapleStoryLocalisation || (MapleStoryLocalisation = {}));
/**
 * @public
 */
class WzFile_WzFile extends WzObject["a" /* WzObject */] {
    constructor(filepath, version, gameVersion = -1) {
        super();
        this.name = '';
        this.parent = null;
        this.header = WzHeader["a" /* WzHeader */].getDefault();
        this._wzVersionHeader = 0;
        this._wzVersionHeader64bit_start = 770;
        this._versionHash = 0;
        this.mapleStoryPatchVersion = 0;
        this._mapleLocaleVersion = MapleStoryLocalisation.Not_Known;
        this._wz_withEncryptVersionHeader = true;
        this._wzDir = null;
        this._isUnloaded = false;
        this.filepath = filepath;
        this.name = typeof filepath === 'string' ? filepath : filepath.name;
        this.mapleStoryPatchVersion = gameVersion;
        this.maplepLocalVersion = version;
        if (version === WzMapleVersion["a" /* WzMapleVersion */].GETFROMZLZ) {
            if (typeof window !== 'undefined')
                throw new Error('Not supported in browser');
            const r = new esm_modern["b" /* BinaryReader */](node["g" /* path */].join(node["g" /* path */].dirname(filepath), 'ZLZ.dll'));
            this._wzIv = Object(WzKeyGenerator["c" /* getIvFromZlz */])(r);
            r.close();
        }
        else {
            this._wzIv = Object(WzTool["a" /* getIvByMapleVersion */])(version);
        }
    }
    get isUnloaded() {
        return this._isUnloaded;
    }
    get mapleLocaleVersion() {
        return this._mapleLocaleVersion;
    }
    get is64BitWzFile() {
        return !this._wz_withEncryptVersionHeader;
    }
    get wzDirectory() {
        return this._wzDir;
    }
    dispose() {
        if (this._disposed)
            return;
        this._isUnloaded = true;
        if (this._wzDir != null) {
            this._wzDir.reader.dispose();
            this._wzDir.reader = null;
            this._wzDir.dispose();
            this._wzDir = null;
        }
        this.header = WzHeader["a" /* WzHeader */].getDefault();
        this._disposed = true;
    }
    get objectType() {
        return WzObjectType["a" /* WzObjectType */].File;
    }
    get wzFileParent() {
        return this;
    }
    at(name) {
        return this._wzDir != null ? this._wzDir.at(name) : null;
    }
    get fullPath() {
        return this.wzDirectory != null ? this.wzDirectory.name : '';
    }
    async parseWzFile(wzIv = null) {
        if (this._wzDir != null) {
            return WzFileParseStatus.SUCCESS;
        }
        if (wzIv != null) {
            this._wzIv = wzIv;
        }
        return await this._parseMainWzDirectory(false);
    }
    /* public lazyParseWzFile (out: IWzParseResult): boolean {
      return this.parseMainWzDirectory(out, true)
    } */
    async _parseMainWzDirectory(lazyParse = false) {
        if (this.filepath === '') {
            ErrorLogger["b" /* ErrorLogger */].log(ErrorLogger["a" /* ErrorLevel */].Critical, '[Error] Path is null');
            return WzFileParseStatus.PATH_IS_NULL;
        }
        const reader = new WzBinaryReader["a" /* WzBinaryReader */](this.filepath, this._wzIv);
        this.header = new WzHeader["a" /* WzHeader */]();
        this.header.ident = await reader.readString('ascii', 4);
        this.header.fsize = await reader.readBigUInt64LE();
        this.header.fstart = await reader.readUInt32LE();
        this.header.copyright = await reader.readString('ascii', this.header.fstart - 17);
        /* const unk1 =  */ await reader.read();
        /* const unk2 =  */ await reader.read(this.header.fstart - reader.pos);
        reader.header = this.header;
        await this._check64BitClient(reader); // update b64BitClient flag
        // the value of wzVersionHeader is less important. It is used for reading/writing from/to WzFile Header, and calculating the versionHash.
        // it can be any number if the client is 64-bit. Assigning 777 is just for convenience when calculating the versionHash.
        this._wzVersionHeader = this._wz_withEncryptVersionHeader ? await reader.readUInt16LE() : this._wzVersionHeader64bit_start;
        if (this.mapleStoryPatchVersion === -1) {
            // for 64-bit client, return immediately if version 777 works correctly.
            // -- the latest KMS update seems to have changed it to 778? 779?
            if (!this._wz_withEncryptVersionHeader) {
                for (let maplestoryVerToDecode = this._wzVersionHeader64bit_start; maplestoryVerToDecode < this._wzVersionHeader64bit_start + 10; maplestoryVerToDecode++) {
                    if (await this._tryDecodeWithWZVersionNumber(reader, this._wzVersionHeader, maplestoryVerToDecode, lazyParse)) {
                        return WzFileParseStatus.SUCCESS;
                    }
                }
            }
            // Attempt to get version from MapleStory.exe first
            let maplestoryVerDetectedFromClient;
            if (typeof window !== 'undefined') {
                maplestoryVerDetectedFromClient = 0;
            }
            else {
                const _this = this;
                const out = {
                    get value() {
                        return _this._mapleLocaleVersion;
                    },
                    set value(value) {
                        _this._mapleLocaleVersion = value;
                    }
                };
                maplestoryVerDetectedFromClient = WzFile_WzFile._getMapleStoryVerFromExe(this.filepath, out);
            }
            // this step is actually not needed if we know the maplestory patch version (the client .exe), but since we dont..
            // we'll need a bruteforce way around it.
            const MAX_PATCH_VERSION = 1000; // wont be reached for the forseeable future.
            for (let j = maplestoryVerDetectedFromClient; j < MAX_PATCH_VERSION; j++) {
                if (await this._tryDecodeWithWZVersionNumber(reader, this._wzVersionHeader, j, lazyParse)) {
                    return WzFileParseStatus.SUCCESS;
                }
            }
            // parseErrorMessage = "Error with game version hash : The specified game version is incorrect and WzLib was unable to determine the version itself";
            return WzFileParseStatus.ERROR_GAME_VER_HASH;
        }
        else {
            this._versionHash = this._checkAndGetVersionHash(this._wzVersionHeader, this.mapleStoryPatchVersion);
            reader.hash = this._versionHash;
            const directory = new WzDirectory["a" /* WzDirectory */](reader, this.name, this._versionHash, this._wzIv, this);
            directory.offset = reader.pos;
            await directory.parseDirectory(lazyParse);
            this._wzDir = directory;
        }
        return WzFileParseStatus.SUCCESS;
    }
    async _tryDecodeWithWZVersionNumber(reader, useWzVersionHeader, useMapleStoryPatchVersion, lazyParse) {
        this.mapleStoryPatchVersion = useMapleStoryPatchVersion;
        this._versionHash = this._checkAndGetVersionHash(useWzVersionHeader, this.mapleStoryPatchVersion);
        if (this._versionHash === 0) {
            return false;
        }
        reader.hash = this._versionHash;
        const fallbackOffsetPosition = reader.pos; // save position to rollback to, if should parsing fail from here
        let testDirectory;
        try {
            testDirectory = new WzDirectory["a" /* WzDirectory */](reader, this.name, this._versionHash, this._wzIv, this);
            testDirectory.offset = reader.pos;
            await testDirectory.parseDirectory(lazyParse);
        }
        catch (_) {
            reader.pos = fallbackOffsetPosition;
            return false;
        }
        let bCloseTestDirectory = true;
        try {
            const testImage = testDirectory.wzImages.values().next().value;
            if (testImage != null) {
                try {
                    reader.pos = testImage.offset;
                    const checkByte = await reader.readUInt8();
                    reader.pos = fallbackOffsetPosition;
                    switch (checkByte) {
                        case 0x73:
                        case 0x1b: {
                            const directory = new WzDirectory["a" /* WzDirectory */](reader, this.name, this._versionHash, this._wzIv, this);
                            directory.offset = reader.pos;
                            await directory.parseDirectory(lazyParse);
                            this._wzDir = directory;
                            return true;
                        }
                        case 0x30:
                        case 0x6C: // idk
                        case 0xBC: // Map002.wz? KMST?
                        default: {
                            ErrorLogger["b" /* ErrorLogger */].log(ErrorLogger["a" /* ErrorLevel */].MissingFeature, `[WzFile.ts] New Wz image header found. checkByte = ${checkByte}. File Name = ${this.name}`);
                            break;
                        }
                    }
                    reader.pos = fallbackOffsetPosition; // reset
                    return false;
                }
                catch (_) {
                    reader.pos = fallbackOffsetPosition; // reset
                    return false;
                }
            }
            else { // if there's no image in the WZ file (new KMST Base.wz), test the directory instead
                // coincidentally in msea v194 Map001.wz, the hash matches exactly using mapleStoryPatchVersion of 113, and it fails to decrypt later on (probably 1 in a million chance? o_O).
                // damn, technical debt accumulating here
                if (this.is64BitWzFile && this.mapleStoryPatchVersion === 113) {
                    // hack for now
                    reader.pos = fallbackOffsetPosition; // reset
                    return false;
                }
                else {
                    this._wzDir = testDirectory;
                    bCloseTestDirectory = false;
                    return true;
                }
            }
        }
        finally {
            if (bCloseTestDirectory) {
                try {
                    testDirectory.dispose();
                }
                catch (_) { }
            }
        }
    }
    static _getMapleStoryVerFromExe(wzFilePath, mapleLocaleVersion) {
        if (typeof window !== 'undefined' || node["f" /* os */].platform() !== 'win32' || !node["e" /* fs */].existsSync(wzFilePath)) {
            mapleLocaleVersion.value = MapleStoryLocalisation.Not_Known;
            return 0;
        }
        const MAPLESTORY_EXE_NAME = 'MapleStory.exe';
        const MAPLESTORYT_EXE_NAME = 'MapleStoryT.exe';
        const MAPLESTORYADMIN_EXE_NAME = 'MapleStoryA.exe';
        let currentDirectory = node["g" /* path */].dirname(wzFilePath);
        for (let i = 0; i < 4; i++) {
            const exeFileInfo = [];
            const msexe = node["g" /* path */].join(currentDirectory, MAPLESTORY_EXE_NAME);
            const mstexe = node["g" /* path */].join(currentDirectory, MAPLESTORYT_EXE_NAME);
            const msaexe = node["g" /* path */].join(currentDirectory, MAPLESTORYADMIN_EXE_NAME);
            if (node["e" /* fs */].existsSync(msexe)) {
                exeFileInfo.push(msexe);
            }
            if (node["e" /* fs */].existsSync(mstexe)) {
                exeFileInfo.push(mstexe);
            }
            if (node["e" /* fs */].existsSync(msaexe)) {
                exeFileInfo.push(msaexe);
            }
            for (let j = 0; j < exeFileInfo.length; ++j) {
                const versionInfo = node["h" /* tybysWindowsFileVersionInfo */].FileVersionInfo.getVersionInfo(exeFileInfo[i]);
                if ((versionInfo.fileMajorPart === 1 && versionInfo.fileMinorPart === 0 && versionInfo.fileBuildPart === 0) ||
                    (versionInfo.fileMajorPart === 0 && versionInfo.fileMinorPart === 0 && versionInfo.fileBuildPart === 0)) { // older client uses 1.0.0.1
                    continue;
                }
                const locale = versionInfo.fileMajorPart;
                let localeVersion = MapleStoryLocalisation.Not_Known;
                if (MapleStoryLocalisation[locale] !== undefined) {
                    localeVersion = locale;
                }
                const msVersion = versionInfo.fileMinorPart;
                // const msMinorPatchVersion = versionInfo.fileBuildPart
                mapleLocaleVersion.value = localeVersion;
                return msVersion;
            }
            const oldCurrentDirectory = currentDirectory;
            currentDirectory = node["g" /* path */].dirname(oldCurrentDirectory);
            if (currentDirectory === oldCurrentDirectory) {
                break;
            }
        }
        mapleLocaleVersion.value = MapleStoryLocalisation.Not_Known;
        return 0;
    }
    async _check64BitClient(reader) {
        if (this.header.fsize >= 2) {
            reader.seek(this.header.fstart); // go back to 0x3C
            const encver = await reader.readUInt16LE();
            if (encver > 0xff) { // encver always less than 256
                this._wz_withEncryptVersionHeader = false;
            }
            else if (encver === 0x80) {
                // there's an exceptional case that the first field of data part is a compressed int which determined property count,
                // if the value greater than 127 and also to be a multiple of 256, the first 5 bytes will become to
                //   80 00 xx xx xx
                // so we additional check the int value, at most time the child node count in a wz won't greater than 65536.
                if (this.header.fsize >= 5) {
                    reader.seek(this.header.fstart); // go back to 0x3C
                    const propCount = await reader.readInt32LE();
                    if (propCount > 0 && (propCount & 0xff) === 0 && propCount <= 0xffff) {
                        this._wz_withEncryptVersionHeader = false;
                    }
                }
            }
            else {
                // old wz file with header version
            }
        }
        else {
            this._wz_withEncryptVersionHeader = false;
        }
        // reset position
        reader.seek(this.header.fstart);
    }
    _checkAndGetVersionHash(wzVersionHeader, maplestoryPatchVersion) {
        /* const VersionNumber = maplestoryPatchVersion
        let VersionHash = 0
        const VersionNumberStr = VersionNumber.toString()
    
        const l = VersionNumberStr.length
        for (let i = 0; i < l; i++) {
          VersionHash = (32 * VersionHash) + VersionNumberStr.charCodeAt(i) + 1
        }
    
        if (wzVersionHeader === this._wzVersionHeader64bit_start) {
          return VersionHash >>> 0 // always 59192
        }
    
        const a = (VersionHash >> 24) & 0xFF
        const b = (VersionHash >> 16) & 0xFF
        const c = (VersionHash >> 8) & 0xFF
        const d = VersionHash & 0xFF
        const DecryptedVersionNumber = (0xff ^ a ^ b ^ c ^ d)
    
        if (wzVersionHeader === DecryptedVersionNumber) return (VersionHash >>> 0) */
        let versionHash = 0;
        const versionNumberStr = maplestoryPatchVersion.toString();
        for (let i = 0; i < versionNumberStr.length; i++) {
            versionHash = (versionHash * 32) + versionNumberStr.charCodeAt(i) + 1;
        }
        if (wzVersionHeader === this._wzVersionHeader64bit_start) {
            return versionHash >>> 0; // always 59192
        }
        const decryptedVersionNumber = ((~((versionHash >> 24) & 0xFF ^ (versionHash >> 16) & 0xFF ^ (versionHash >> 8) & 0xFF ^ versionHash & 0xFF)) & 0xFF) >>> 0;
        if (wzVersionHeader === decryptedVersionNumber) {
            return versionHash >>> 0;
        }
        return 0;
    }
    getObjectFromPath(path, checkFirstDirectoryName = true) {
        if (this._wzDir == null)
            return null;
        const seperatedPath = path.split(/[\\/]/);
        if (seperatedPath.length === 1) {
            return this.wzDirectory;
        }
        const checkObjInOtherWzFile = null;
        if (checkFirstDirectoryName) {
            if (seperatedPath[0].toLowerCase() !== this._wzDir.name.toLowerCase() && seperatedPath[0].toLowerCase() !== this._wzDir.name.substring(0, this._wzDir.name.length - 3).toLowerCase()) {
                return null;
            }
        }
        let curObj;
        if (checkObjInOtherWzFile != null) {
            curObj = checkObjInOtherWzFile;
        }
        else {
            curObj = this.wzDirectory;
        }
        for (let i = 1; i < seperatedPath.length; i++) {
            if (curObj == null) {
                return null;
            }
            switch (curObj.objectType) {
                case WzObjectType["a" /* WzObjectType */].Directory:
                    curObj = curObj.at(seperatedPath[i]);
                    continue;
                case WzObjectType["a" /* WzObjectType */].Image:
                    curObj = curObj.at(seperatedPath[i]);
                    continue;
                case WzObjectType["a" /* WzObjectType */].Property:
                    switch (curObj.propertyType) {
                        case WzPropertyType["a" /* WzPropertyType */].Canvas:
                            curObj = curObj.at(seperatedPath[i]);
                            continue;
                        case WzPropertyType["a" /* WzPropertyType */].Convex:
                            curObj = curObj.at(seperatedPath[i]);
                            continue;
                        case WzPropertyType["a" /* WzPropertyType */].SubProperty:
                            curObj = curObj.at(seperatedPath[i]);
                            continue;
                        case WzPropertyType["a" /* WzPropertyType */].Vector:
                            if (seperatedPath[i] === 'x')
                                return curObj.x;
                            else if (seperatedPath[i] === 'y')
                                return curObj.y;
                            else
                                return null;
                        default: // Wut?
                            return null;
                    }
            }
        }
        if (curObj == null) {
            return null;
        }
        return curObj;
    }
}

// CONCATENATED MODULE: ./lib/esm-modern/WzMainDirectory.js
/** @public */
class WzMainDirectory {
    constructor(file, directory) {
        this.file = file;
        this._directory = directory;
    }
    get directory() {
        var _a;
        return (_a = this._directory) !== null && _a !== void 0 ? _a : this.file.wzDirectory;
    }
}

// CONCATENATED MODULE: ./lib/esm-modern/init.js
// eslint-disable-next-line @typescript-eslint/triple-slash-reference
/// <reference types="emscripten" />
/**
 * @public
 */
async function init(moduleOverrides) {
    if (typeof window !== 'undefined') {
        const wzWasm = await Promise.resolve(/* import() */).then(__webpack_require__.t.bind(null, 37, 7));
        await wzWasm.default(moduleOverrides);
    }
    else {
        await Promise.resolve();
    }
}

// CONCATENATED MODULE: ./lib/esm-modern/walk.js
/* eslint-disable @typescript-eslint/strict-boolean-expressions */





/**
 * @public
 * @returns `true` if stop manually
 */
async function walkWzFileAsync(filepath, mapleVersion, callback) {
    await init();
    const wz = new WzFile_WzFile(filepath, mapleVersion);
    const r = await wz.parseWzFile();
    if (r !== WzFileParseStatus.SUCCESS) {
        wz.dispose();
        throw new Error(getErrorDescription(r));
    }
    const stop = await walkDirectory(wz.wzDirectory, callback);
    wz.dispose();
    return stop;
}
/**
 * @public
 * @returns `true` if stop manually
 */
async function walkDirectory(dir, callback) {
    let stop = !!(await Promise.resolve(callback(dir)));
    if (stop) {
        dir.dispose();
        return stop;
    }
    for (const subdir of dir.wzDirectories) {
        stop = await walkDirectory(subdir, callback);
        if (stop) {
            dir.dispose();
            return stop;
        }
    }
    for (const img of dir.wzImages) {
        stop = await walkPropertyContainer(img, callback);
        if (stop) {
            dir.dispose();
            return stop;
        }
    }
    dir.dispose();
    return stop;
}
/**
 * @public
 * @returns `true` if stop manually
 */
async function walkPropertyContainer(container, callback) {
    if (container instanceof WzImage["a" /* WzImage */]) {
        await container.parseImage();
    }
    let stop = !!(await Promise.resolve(callback(container)));
    if (!stop && !(container instanceof WzUOLProperty["WzUOLProperty"]) && container.wzProperties != null) {
        for (const prop of container.wzProperties) {
            stop = await walkPropertyContainer(prop, callback);
            if (stop)
                break;
        }
    }
    if (container instanceof WzImage["a" /* WzImage */]) {
        container.dispose();
    }
    return stop;
}

// CONCATENATED MODULE: ./lib/esm-modern/index.js
/**
 * MapleStory WZ reader.
 *
 * @packageDocumentation
 */







































/***/ })
/******/ ]);
});