export declare function checkRange(pos: number, size: number): void;
/** @public */
export declare abstract class Reader {
    /**
     * Size of binary.
     */
    protected readonly _size: number;
    /**
     * Current position.
     */
    pos: number;
    /**
     * Throws RangeError if position is out of range when set this to `true`
     */
    strictRangeChecking: boolean;
    protected constructor(size?: number);
    get size(): number;
    /**
     * Set current position.
     * @param pos - Target position
     */
    seek(pos: number): void;
    /**
     * Get current position
     */
    tell(): number;
    abstract close(): void;
    /**
     * Destructor.
     * @virtual
     */
    dispose(): void;
}
