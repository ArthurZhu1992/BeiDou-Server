import { __awaiter, __extends, __generator } from "tslib";
import { os, path, fs, tybysWindowsFileVersionInfo } from "./util/node.js";
import { BinaryReader } from '@tybys/binreader';
import { ErrorLevel, ErrorLogger } from "./util/ErrorLogger.js";
import { WzBinaryReader } from "./util/WzBinaryReader.js";
import { getIvFromZlz } from "./util/WzKeyGenerator.js";
import { getIvByMapleVersion } from "./util/WzTool.js";
import { WzDirectory } from "./WzDirectory.js";
import { WzHeader } from "./WzHeader.js";
import { WzMapleVersion } from "./WzMapleVersion.js";
import { WzObject } from "./WzObject.js";
import { WzObjectType } from "./WzObjectType.js";
import { WzPropertyType } from "./WzPropertyType.js";
import { WzFileParseStatus } from "./WzFileParseStatus.js";
/** @public */
export var MapleStoryLocalisation;
(function (MapleStoryLocalisation) {
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryKorea"] = 1] = "MapleStoryKorea";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryKoreaTespia"] = 2] = "MapleStoryKoreaTespia";
    MapleStoryLocalisation[MapleStoryLocalisation["Unknown3"] = 3] = "Unknown3";
    MapleStoryLocalisation[MapleStoryLocalisation["Unknown4"] = 4] = "Unknown4";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryTespia"] = 5] = "MapleStoryTespia";
    MapleStoryLocalisation[MapleStoryLocalisation["Unknown6"] = 6] = "Unknown6";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStorySEA"] = 7] = "MapleStorySEA";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryGlobal"] = 8] = "MapleStoryGlobal";
    MapleStoryLocalisation[MapleStoryLocalisation["MapleStoryEurope"] = 9] = "MapleStoryEurope";
    MapleStoryLocalisation[MapleStoryLocalisation["Not_Known"] = 999] = "Not_Known";
    // TODO: other values
})(MapleStoryLocalisation || (MapleStoryLocalisation = {}));
/**
 * @public
 */
var WzFile = /*#__PURE__*/ (function (_super) {
    __extends(WzFile, _super);
    function WzFile(filepath, version, gameVersion) {
        if (gameVersion === void 0) { gameVersion = -1; }
        var _this_1 = _super.call(this) || this;
        _this_1.name = '';
        _this_1.parent = null;
        _this_1.header = WzHeader.getDefault();
        _this_1._wzVersionHeader = 0;
        _this_1._wzVersionHeader64bit_start = 770;
        _this_1._versionHash = 0;
        _this_1.mapleStoryPatchVersion = 0;
        _this_1._mapleLocaleVersion = MapleStoryLocalisation.Not_Known;
        _this_1._wz_withEncryptVersionHeader = true;
        _this_1._wzDir = null;
        _this_1._isUnloaded = false;
        _this_1.filepath = filepath;
        _this_1.name = typeof filepath === 'string' ? filepath : filepath.name;
        _this_1.mapleStoryPatchVersion = gameVersion;
        _this_1.maplepLocalVersion = version;
        if (version === WzMapleVersion.GETFROMZLZ) {
            if (typeof window !== 'undefined')
                throw new Error('Not supported in browser');
            var r = new BinaryReader(path.join(path.dirname(filepath), 'ZLZ.dll'));
            _this_1._wzIv = getIvFromZlz(r);
            r.close();
        }
        else {
            _this_1._wzIv = getIvByMapleVersion(version);
        }
        return _this_1;
    }
    Object.defineProperty(WzFile.prototype, "isUnloaded", {
        get: function () {
            return this._isUnloaded;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(WzFile.prototype, "mapleLocaleVersion", {
        get: function () {
            return this._mapleLocaleVersion;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(WzFile.prototype, "is64BitWzFile", {
        get: function () {
            return !this._wz_withEncryptVersionHeader;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(WzFile.prototype, "wzDirectory", {
        get: function () {
            return this._wzDir;
        },
        enumerable: false,
        configurable: true
    });
    WzFile.prototype.dispose = function () {
        if (this._disposed)
            return;
        this._isUnloaded = true;
        if (this._wzDir != null) {
            this._wzDir.reader.dispose();
            this._wzDir.reader = null;
            this._wzDir.dispose();
            this._wzDir = null;
        }
        this.header = WzHeader.getDefault();
        this._disposed = true;
    };
    Object.defineProperty(WzFile.prototype, "objectType", {
        get: function () {
            return WzObjectType.File;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(WzFile.prototype, "wzFileParent", {
        get: function () {
            return this;
        },
        enumerable: false,
        configurable: true
    });
    WzFile.prototype.at = function (name) {
        return this._wzDir != null ? this._wzDir.at(name) : null;
    };
    Object.defineProperty(WzFile.prototype, "fullPath", {
        get: function () {
            return this.wzDirectory != null ? this.wzDirectory.name : '';
        },
        enumerable: false,
        configurable: true
    });
    WzFile.prototype.parseWzFile = function (wzIv) {
        if (wzIv === void 0) { wzIv = null; }
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (this._wzDir != null) {
                            return [2 /*return*/, WzFileParseStatus.SUCCESS];
                        }
                        if (wzIv != null) {
                            this._wzIv = wzIv;
                        }
                        return [4 /*yield*/, this._parseMainWzDirectory(false)];
                    case 1: return [2 /*return*/, _a.sent()];
                }
            });
        });
    };
    /* public lazyParseWzFile (out: IWzParseResult): boolean {
      return this.parseMainWzDirectory(out, true)
    } */
    WzFile.prototype._parseMainWzDirectory = function (lazyParse) {
        if (lazyParse === void 0) { lazyParse = false; }
        return __awaiter(this, void 0, void 0, function () {
            var reader, _a, _b, _c, _d, _e, _f, maplestoryVerToDecode, maplestoryVerDetectedFromClient, _this_2, out, MAX_PATCH_VERSION, j, directory;
            return __generator(this, function (_g) {
                switch (_g.label) {
                    case 0:
                        if (this.filepath === '') {
                            ErrorLogger.log(ErrorLevel.Critical, '[Error] Path is null');
                            return [2 /*return*/, WzFileParseStatus.PATH_IS_NULL];
                        }
                        reader = new WzBinaryReader(this.filepath, this._wzIv);
                        this.header = new WzHeader();
                        _a = this.header;
                        return [4 /*yield*/, reader.readString('ascii', 4)];
                    case 1:
                        _a.ident = _g.sent();
                        _b = this.header;
                        return [4 /*yield*/, reader.readBigUInt64LE()];
                    case 2:
                        _b.fsize = _g.sent();
                        _c = this.header;
                        return [4 /*yield*/, reader.readUInt32LE()];
                    case 3:
                        _c.fstart = _g.sent();
                        _d = this.header;
                        return [4 /*yield*/, reader.readString('ascii', this.header.fstart - 17)
                            /* const unk1 =  */ ];
                    case 4:
                        _d.copyright = _g.sent();
                        /* const unk1 =  */ return [4 /*yield*/, reader.read()
                            /* const unk2 =  */ ];
                    case 5:
                        /* const unk1 =  */ _g.sent();
                        /* const unk2 =  */ return [4 /*yield*/, reader.read(this.header.fstart - reader.pos)];
                    case 6:
                        /* const unk2 =  */ _g.sent();
                        reader.header = this.header;
                        return [4 /*yield*/, this._check64BitClient(reader)
                            // the value of wzVersionHeader is less important. It is used for reading/writing from/to WzFile Header, and calculating the versionHash.
                            // it can be any number if the client is 64-bit. Assigning 777 is just for convenience when calculating the versionHash.
                        ]; // update b64BitClient flag
                    case 7:
                        _g.sent(); // update b64BitClient flag
                        // the value of wzVersionHeader is less important. It is used for reading/writing from/to WzFile Header, and calculating the versionHash.
                        // it can be any number if the client is 64-bit. Assigning 777 is just for convenience when calculating the versionHash.
                        _e = this;
                        if (!this._wz_withEncryptVersionHeader) return [3 /*break*/, 9];
                        return [4 /*yield*/, reader.readUInt16LE()];
                    case 8:
                        _f = _g.sent();
                        return [3 /*break*/, 10];
                    case 9:
                        _f = this._wzVersionHeader64bit_start;
                        _g.label = 10;
                    case 10:
                        // the value of wzVersionHeader is less important. It is used for reading/writing from/to WzFile Header, and calculating the versionHash.
                        // it can be any number if the client is 64-bit. Assigning 777 is just for convenience when calculating the versionHash.
                        _e._wzVersionHeader = _f;
                        if (!(this.mapleStoryPatchVersion === -1)) return [3 /*break*/, 19];
                        if (!!this._wz_withEncryptVersionHeader) return [3 /*break*/, 14];
                        maplestoryVerToDecode = this._wzVersionHeader64bit_start;
                        _g.label = 11;
                    case 11:
                        if (!(maplestoryVerToDecode < this._wzVersionHeader64bit_start + 10)) return [3 /*break*/, 14];
                        return [4 /*yield*/, this._tryDecodeWithWZVersionNumber(reader, this._wzVersionHeader, maplestoryVerToDecode, lazyParse)];
                    case 12:
                        if (_g.sent()) {
                            return [2 /*return*/, WzFileParseStatus.SUCCESS];
                        }
                        _g.label = 13;
                    case 13:
                        maplestoryVerToDecode++;
                        return [3 /*break*/, 11];
                    case 14:
                        maplestoryVerDetectedFromClient = void 0;
                        if (typeof window !== 'undefined') {
                            maplestoryVerDetectedFromClient = 0;
                        }
                        else {
                            _this_2 = this;
                            out = {
                                get value() {
                                    return _this_2._mapleLocaleVersion;
                                },
                                set value(value) {
                                    _this_2._mapleLocaleVersion = value;
                                }
                            };
                            maplestoryVerDetectedFromClient = WzFile._getMapleStoryVerFromExe(this.filepath, out);
                        }
                        MAX_PATCH_VERSION = 1000 // wont be reached for the forseeable future.
                        ;
                        j = maplestoryVerDetectedFromClient;
                        _g.label = 15;
                    case 15:
                        if (!(j < MAX_PATCH_VERSION)) return [3 /*break*/, 18];
                        return [4 /*yield*/, this._tryDecodeWithWZVersionNumber(reader, this._wzVersionHeader, j, lazyParse)];
                    case 16:
                        if (_g.sent()) {
                            return [2 /*return*/, WzFileParseStatus.SUCCESS];
                        }
                        _g.label = 17;
                    case 17:
                        j++;
                        return [3 /*break*/, 15];
                    case 18: 
                    // parseErrorMessage = "Error with game version hash : The specified game version is incorrect and WzLib was unable to determine the version itself";
                    return [2 /*return*/, WzFileParseStatus.ERROR_GAME_VER_HASH];
                    case 19:
                        this._versionHash = this._checkAndGetVersionHash(this._wzVersionHeader, this.mapleStoryPatchVersion);
                        reader.hash = this._versionHash;
                        directory = new WzDirectory(reader, this.name, this._versionHash, this._wzIv, this);
                        directory.offset = reader.pos;
                        return [4 /*yield*/, directory.parseDirectory(lazyParse)];
                    case 20:
                        _g.sent();
                        this._wzDir = directory;
                        _g.label = 21;
                    case 21: return [2 /*return*/, WzFileParseStatus.SUCCESS];
                }
            });
        });
    };
    WzFile.prototype._tryDecodeWithWZVersionNumber = function (reader, useWzVersionHeader, useMapleStoryPatchVersion, lazyParse) {
        return __awaiter(this, void 0, void 0, function () {
            var fallbackOffsetPosition, testDirectory, _1, bCloseTestDirectory, testImage, checkByte, _a, directory, _2;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        this.mapleStoryPatchVersion = useMapleStoryPatchVersion;
                        this._versionHash = this._checkAndGetVersionHash(useWzVersionHeader, this.mapleStoryPatchVersion);
                        if (this._versionHash === 0) {
                            return [2 /*return*/, false];
                        }
                        reader.hash = this._versionHash;
                        fallbackOffsetPosition = reader.pos // save position to rollback to, if should parsing fail from here
                        ;
                        _b.label = 1;
                    case 1:
                        _b.trys.push([1, 3, , 4]);
                        testDirectory = new WzDirectory(reader, this.name, this._versionHash, this._wzIv, this);
                        testDirectory.offset = reader.pos;
                        return [4 /*yield*/, testDirectory.parseDirectory(lazyParse)];
                    case 2:
                        _b.sent();
                        return [3 /*break*/, 4];
                    case 3:
                        _1 = _b.sent();
                        reader.pos = fallbackOffsetPosition;
                        return [2 /*return*/, false];
                    case 4:
                        bCloseTestDirectory = true;
                        _b.label = 5;
                    case 5:
                        _b.trys.push([5, , 16, 17]);
                        testImage = testDirectory.wzImages.values().next().value;
                        if (!(testImage != null)) return [3 /*break*/, 14];
                        _b.label = 6;
                    case 6:
                        _b.trys.push([6, 12, , 13]);
                        reader.pos = testImage.offset;
                        return [4 /*yield*/, reader.readUInt8()];
                    case 7:
                        checkByte = _b.sent();
                        reader.pos = fallbackOffsetPosition;
                        _a = checkByte;
                        switch (_a) {
                            case 0x73: return [3 /*break*/, 8];
                            case 0x1b: return [3 /*break*/, 8];
                            case 0x30: return [3 /*break*/, 10];
                            case 0x6C: return [3 /*break*/, 10];
                            case 0xBC: return [3 /*break*/, 10];
                        }
                        return [3 /*break*/, 10];
                    case 8:
                        directory = new WzDirectory(reader, this.name, this._versionHash, this._wzIv, this);
                        directory.offset = reader.pos;
                        return [4 /*yield*/, directory.parseDirectory(lazyParse)];
                    case 9:
                        _b.sent();
                        this._wzDir = directory;
                        return [2 /*return*/, true];
                    case 10:
                        {
                            ErrorLogger.log(ErrorLevel.MissingFeature, "[WzFile.ts] New Wz image header found. checkByte = ".concat(checkByte, ". File Name = ").concat(this.name));
                            return [3 /*break*/, 11];
                        }
                        _b.label = 11;
                    case 11:
                        reader.pos = fallbackOffsetPosition; // reset
                        return [2 /*return*/, false];
                    case 12:
                        _2 = _b.sent();
                        reader.pos = fallbackOffsetPosition; // reset
                        return [2 /*return*/, false];
                    case 13: return [3 /*break*/, 15];
                    case 14:
                        // coincidentally in msea v194 Map001.wz, the hash matches exactly using mapleStoryPatchVersion of 113, and it fails to decrypt later on (probably 1 in a million chance? o_O).
                        // damn, technical debt accumulating here
                        if (this.is64BitWzFile && this.mapleStoryPatchVersion === 113) {
                            // hack for now
                            reader.pos = fallbackOffsetPosition; // reset
                            return [2 /*return*/, false];
                        }
                        else {
                            this._wzDir = testDirectory;
                            bCloseTestDirectory = false;
                            return [2 /*return*/, true];
                        }
                        _b.label = 15;
                    case 15: return [3 /*break*/, 17];
                    case 16:
                        if (bCloseTestDirectory) {
                            try {
                                testDirectory.dispose();
                            }
                            catch (_) { }
                        }
                        return [7 /*endfinally*/];
                    case 17: return [2 /*return*/];
                }
            });
        });
    };
    WzFile._getMapleStoryVerFromExe = function (wzFilePath, mapleLocaleVersion) {
        if (typeof window !== 'undefined' || os.platform() !== 'win32' || !fs.existsSync(wzFilePath)) {
            mapleLocaleVersion.value = MapleStoryLocalisation.Not_Known;
            return 0;
        }
        var MAPLESTORY_EXE_NAME = 'MapleStory.exe';
        var MAPLESTORYT_EXE_NAME = 'MapleStoryT.exe';
        var MAPLESTORYADMIN_EXE_NAME = 'MapleStoryA.exe';
        var currentDirectory = path.dirname(wzFilePath);
        for (var i = 0; i < 4; i++) {
            var exeFileInfo = [];
            var msexe = path.join(currentDirectory, MAPLESTORY_EXE_NAME);
            var mstexe = path.join(currentDirectory, MAPLESTORYT_EXE_NAME);
            var msaexe = path.join(currentDirectory, MAPLESTORYADMIN_EXE_NAME);
            if (fs.existsSync(msexe)) {
                exeFileInfo.push(msexe);
            }
            if (fs.existsSync(mstexe)) {
                exeFileInfo.push(mstexe);
            }
            if (fs.existsSync(msaexe)) {
                exeFileInfo.push(msaexe);
            }
            for (var j = 0; j < exeFileInfo.length; ++j) {
                var versionInfo = tybysWindowsFileVersionInfo.FileVersionInfo.getVersionInfo(exeFileInfo[i]);
                if ((versionInfo.fileMajorPart === 1 && versionInfo.fileMinorPart === 0 && versionInfo.fileBuildPart === 0) ||
                    (versionInfo.fileMajorPart === 0 && versionInfo.fileMinorPart === 0 && versionInfo.fileBuildPart === 0)) { // older client uses 1.0.0.1
                    continue;
                }
                var locale = versionInfo.fileMajorPart;
                var localeVersion = MapleStoryLocalisation.Not_Known;
                if (MapleStoryLocalisation[locale] !== undefined) {
                    localeVersion = locale;
                }
                var msVersion = versionInfo.fileMinorPart;
                // const msMinorPatchVersion = versionInfo.fileBuildPart
                mapleLocaleVersion.value = localeVersion;
                return msVersion;
            }
            var oldCurrentDirectory = currentDirectory;
            currentDirectory = path.dirname(oldCurrentDirectory);
            if (currentDirectory === oldCurrentDirectory) {
                break;
            }
        }
        mapleLocaleVersion.value = MapleStoryLocalisation.Not_Known;
        return 0;
    };
    WzFile.prototype._check64BitClient = function (reader) {
        return __awaiter(this, void 0, void 0, function () {
            var encver, propCount;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!(this.header.fsize >= 2)) return [3 /*break*/, 6];
                        reader.seek(this.header.fstart); // go back to 0x3C
                        return [4 /*yield*/, reader.readUInt16LE()];
                    case 1:
                        encver = _a.sent();
                        if (!(encver > 0xff)) return [3 /*break*/, 2];
                        this._wz_withEncryptVersionHeader = false;
                        return [3 /*break*/, 5];
                    case 2:
                        if (!(encver === 0x80)) return [3 /*break*/, 5];
                        if (!(this.header.fsize >= 5)) return [3 /*break*/, 4];
                        reader.seek(this.header.fstart); // go back to 0x3C
                        return [4 /*yield*/, reader.readInt32LE()];
                    case 3:
                        propCount = _a.sent();
                        if (propCount > 0 && (propCount & 0xff) === 0 && propCount <= 0xffff) {
                            this._wz_withEncryptVersionHeader = false;
                        }
                        _a.label = 4;
                    case 4: return [3 /*break*/, 5];
                    case 5: return [3 /*break*/, 7];
                    case 6:
                        this._wz_withEncryptVersionHeader = false;
                        _a.label = 7;
                    case 7:
                        // reset position
                        reader.seek(this.header.fstart);
                        return [2 /*return*/];
                }
            });
        });
    };
    WzFile.prototype._checkAndGetVersionHash = function (wzVersionHeader, maplestoryPatchVersion) {
        /* const VersionNumber = maplestoryPatchVersion
        let VersionHash = 0
        const VersionNumberStr = VersionNumber.toString()
    
        const l = VersionNumberStr.length
        for (let i = 0; i < l; i++) {
          VersionHash = (32 * VersionHash) + VersionNumberStr.charCodeAt(i) + 1
        }
    
        if (wzVersionHeader === this._wzVersionHeader64bit_start) {
          return VersionHash >>> 0 // always 59192
        }
    
        const a = (VersionHash >> 24) & 0xFF
        const b = (VersionHash >> 16) & 0xFF
        const c = (VersionHash >> 8) & 0xFF
        const d = VersionHash & 0xFF
        const DecryptedVersionNumber = (0xff ^ a ^ b ^ c ^ d)
    
        if (wzVersionHeader === DecryptedVersionNumber) return (VersionHash >>> 0) */
        var versionHash = 0;
        var versionNumberStr = maplestoryPatchVersion.toString();
        for (var i = 0; i < versionNumberStr.length; i++) {
            versionHash = (versionHash * 32) + versionNumberStr.charCodeAt(i) + 1;
        }
        if (wzVersionHeader === this._wzVersionHeader64bit_start) {
            return versionHash >>> 0; // always 59192
        }
        var decryptedVersionNumber = ((~((versionHash >> 24) & 0xFF ^ (versionHash >> 16) & 0xFF ^ (versionHash >> 8) & 0xFF ^ versionHash & 0xFF)) & 0xFF) >>> 0;
        if (wzVersionHeader === decryptedVersionNumber) {
            return versionHash >>> 0;
        }
        return 0;
    };
    WzFile.prototype.getObjectFromPath = function (path, checkFirstDirectoryName) {
        if (checkFirstDirectoryName === void 0) { checkFirstDirectoryName = true; }
        if (this._wzDir == null)
            return null;
        var seperatedPath = path.split(/[\\/]/);
        if (seperatedPath.length === 1) {
            return this.wzDirectory;
        }
        var checkObjInOtherWzFile = null;
        if (checkFirstDirectoryName) {
            if (seperatedPath[0].toLowerCase() !== this._wzDir.name.toLowerCase() && seperatedPath[0].toLowerCase() !== this._wzDir.name.substring(0, this._wzDir.name.length - 3).toLowerCase()) {
                return null;
            }
        }
        var curObj;
        if (checkObjInOtherWzFile != null) {
            curObj = checkObjInOtherWzFile;
        }
        else {
            curObj = this.wzDirectory;
        }
        for (var i = 1; i < seperatedPath.length; i++) {
            if (curObj == null) {
                return null;
            }
            switch (curObj.objectType) {
                case WzObjectType.Directory:
                    curObj = curObj.at(seperatedPath[i]);
                    continue;
                case WzObjectType.Image:
                    curObj = curObj.at(seperatedPath[i]);
                    continue;
                case WzObjectType.Property:
                    switch (curObj.propertyType) {
                        case WzPropertyType.Canvas:
                            curObj = curObj.at(seperatedPath[i]);
                            continue;
                        case WzPropertyType.Convex:
                            curObj = curObj.at(seperatedPath[i]);
                            continue;
                        case WzPropertyType.SubProperty:
                            curObj = curObj.at(seperatedPath[i]);
                            continue;
                        case WzPropertyType.Vector:
                            if (seperatedPath[i] === 'x')
                                return curObj.x;
                            else if (seperatedPath[i] === 'y')
                                return curObj.y;
                            else
                                return null;
                        default: // Wut?
                            return null;
                    }
            }
        }
        if (curObj == null) {
            return null;
        }
        return curObj;
    };
    return WzFile;
}(WzObject));
export { WzFile };
