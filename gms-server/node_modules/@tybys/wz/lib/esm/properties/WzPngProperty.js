import { __awaiter, __extends, __generator } from "tslib";
import { WzPropertyType } from "../WzPropertyType.js";
import { WzExtended } from "../WzExtended.js";
import { BinaryReader } from '@tybys/binreader';
import { Color } from "../util/Color.js";
import { ErrorLevel, ErrorLogger } from "../util/ErrorLogger.js";
import { NotImplementedError } from "../util/NotImplementedError.js";
import { _Buffer, zlib } from "../util/node.js";
import { Canvas } from "../util/Canvas.js";
var wasmInflate = null;
/**
 * @public
 */
var WzPngProperty = /*#__PURE__*/ (function (_super) {
    __extends(WzPngProperty, _super);
    function WzPngProperty(reader /* , parseNow: boolean = false */) {
        var _this = _super.call(this) || this;
        _this.parent = null;
        _this.compressedImageBytes = null;
        _this.png = null;
        _this.listWzUsed = false;
        _this.width = 0;
        _this.height = 0;
        _this.format1 = 0;
        _this.format2 = 0;
        _this.offs = 0;
        _this.wzReader = reader;
        Object.defineProperty(_this, 'format', {
            configurable: true,
            enumerable: true,
            get: function () { return _this.format1 + _this.format2; },
            set: function (value) {
                _this.format1 = value;
                _this.format2 = 0;
            }
        });
        return _this;
    }
    Object.defineProperty(WzPngProperty.prototype, "propertyType", {
        get: function () {
            return WzPropertyType.PNG;
        },
        enumerable: false,
        configurable: true
    });
    WzPngProperty.prototype.dispose = function () {
        if (this._disposed)
            return;
        this.compressedImageBytes = null;
        if (this.png != null) {
            this.png.dispose();
            this.png = null;
        }
        this.wzReader = null;
        this._disposed = true;
    };
    Object.defineProperty(WzPngProperty.prototype, "wzValue", {
        get: function () {
            return this.getImage(false);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(WzPngProperty.prototype, "name", {
        get: function () {
            return 'PNG';
        },
        enumerable: false,
        configurable: true
    });
    WzPngProperty.create = function (reader) {
        return __awaiter(this, void 0, void 0, function () {
            var self, _a, _b, _c, _d, len;
            return __generator(this, function (_e) {
                switch (_e.label) {
                    case 0:
                        self = new WzPngProperty(reader);
                        _a = self;
                        return [4 /*yield*/, reader.readWzInt()];
                    case 1:
                        _a.width = _e.sent();
                        _b = self;
                        return [4 /*yield*/, reader.readWzInt()];
                    case 2:
                        _b.height = _e.sent();
                        _c = self;
                        return [4 /*yield*/, reader.readWzInt()];
                    case 3:
                        _c.format1 = _e.sent();
                        _d = self;
                        return [4 /*yield*/, reader.readUInt8()];
                    case 4:
                        _d.format2 = _e.sent();
                        reader.pos += 4;
                        self.offs = reader.pos;
                        return [4 /*yield*/, reader.readInt32LE()];
                    case 5:
                        len = (_e.sent()) - 1;
                        reader.pos += 1;
                        if (len > 0) {
                            /* if (parseNow) {
                              if (this.wzReader == null) {
                                this.compressedImageBytes = reader.read(len)
                              } else {
                                this.compressedImageBytes = this.wzReader.read(len)
                              }
                      
                              // this.parsePng()
                            } else { */
                            reader.pos += len;
                            /* } */
                        }
                        return [2 /*return*/, self];
                }
            });
        });
    };
    WzPngProperty.prototype.setValue = function (_value) {
        throw new NotImplementedError('[WzPngProperty#setValue]');
    };
    WzPngProperty.prototype.getImage = function (saveInMemory) {
        if (saveInMemory === void 0) { saveInMemory = false; }
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!(this.png == null)) return [3 /*break*/, 2];
                        return [4 /*yield*/, this.parsePng(saveInMemory)];
                    case 1:
                        _a.sent();
                        _a.label = 2;
                    case 2: return [2 /*return*/, this.png];
                }
            });
        });
    };
    WzPngProperty.prototype.getCompressedBytes = function (saveInMemory) {
        if (saveInMemory === void 0) { saveInMemory = false; }
        return __awaiter(this, void 0, void 0, function () {
            var pos, len, _a, returnBytes;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        if (!(this.compressedImageBytes == null)) return [3 /*break*/, 3];
                        pos = this.wzReader.pos;
                        this.wzReader.pos = this.offs;
                        return [4 /*yield*/, this.wzReader.readInt32LE()];
                    case 1:
                        len = (_b.sent()) - 1;
                        if (len <= 0) {
                            // possibility an image written with the wrong wzIv
                            throw new Error('The length of the image is negative. WzPngProperty. Wrong WzIV?');
                        }
                        this.wzReader.pos += 1;
                        /* if (len > 0) */ _a = this;
                        return [4 /*yield*/, this.wzReader.read(len)];
                    case 2:
                        /* if (len > 0) */ _a.compressedImageBytes = _b.sent();
                        this.wzReader.pos = pos;
                        _b.label = 3;
                    case 3:
                        if (!saveInMemory) {
                            returnBytes = this.compressedImageBytes;
                            this.compressedImageBytes = null;
                            return [2 /*return*/, returnBytes];
                        }
                        return [2 /*return*/, this.compressedImageBytes];
                }
            });
        });
    };
    WzPngProperty.prototype.parsePng = function (saveInMemory) {
        if (saveInMemory === void 0) { saveInMemory = false; }
        return __awaiter(this, void 0, void 0, function () {
            var rawBytes, format, bmp, decoded, img, img, decoded, img, img, img, decoded, img, decoded, img, decoded, img;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this._getRawImage(saveInMemory)];
                    case 1:
                        rawBytes = _a.sent();
                        if (rawBytes === null) {
                            this.png = null;
                            return [2 /*return*/];
                        }
                        format = this.format1 + this.format2;
                        bmp = null;
                        switch (format) {
                            case 1: {
                                decoded = WzPngProperty.getPixelDataBgra4444(rawBytes, this.width, this.height);
                                img = new Canvas(this.width, this.height);
                                // img.colorType(6)
                                bgra8888(img, decoded, decoded.length);
                                bmp = img;
                                break;
                            }
                            case 2: {
                                img = new Canvas(this.width, this.height);
                                bgra8888(img, rawBytes, rawBytes.length);
                                bmp = img;
                                break;
                            }
                            case 3: {
                                decoded = WzPngProperty.getPixelDataDXT3(rawBytes, this.width, this.height);
                                img = new Canvas(this.width, this.height);
                                bgra8888(img, decoded, this.width * this.height);
                                bmp = img;
                                break;
                            }
                            case 257: { // http://forum.ragezone.com/f702/wz-png-format-decode-code-1114978/index2.html#post9053713
                                img = new Canvas(this.width, this.height);
                                argb1555(img, rawBytes, rawBytes.length);
                                bmp = img;
                                break;
                            }
                            case 513: {
                                img = new Canvas(this.width, this.height);
                                rgb565(img, rawBytes, rawBytes.length);
                                bmp = img;
                                break;
                            }
                            case 517: {
                                decoded = WzPngProperty.getPixelDataForm517(rawBytes, this.width, this.height);
                                img = new Canvas(this.width, this.height);
                                rgb565(img, decoded, decoded.length);
                                bmp = img;
                                break;
                            }
                            case 1026: {
                                decoded = WzPngProperty.getPixelDataDXT3(rawBytes, this.width, this.height);
                                img = new Canvas(this.width, this.height);
                                bgra8888(img, decoded, decoded.length);
                                bmp = img;
                                break;
                            }
                            case 2050: {
                                decoded = WzPngProperty.getPixelDataDXT5(rawBytes, this.width, this.height);
                                img = new Canvas(this.width, this.height);
                                bgra8888(img, decoded, decoded.length);
                                bmp = img;
                                break;
                            }
                            default: {
                                ErrorLogger.log(ErrorLevel.MissingFeature, "Unknown PNG format ".concat(this.format1, " ").concat(this.format2));
                                break;
                            }
                        }
                        this.png = bmp;
                        return [2 /*return*/];
                }
            });
        });
    };
    WzPngProperty.prototype._getRawImage = function (saveInMemory) {
        if (saveInMemory === void 0) { saveInMemory = false; }
        return __awaiter(this, void 0, void 0, function () {
            var rawImageBytes, reader, header, data, dataStream, blocksize, endOfPng, i, format, uncompressedSize, decBuf, _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0: return [4 /*yield*/, this.getCompressedBytes(saveInMemory)];
                    case 1:
                        rawImageBytes = _b.sent();
                        reader = new BinaryReader(rawImageBytes);
                        header = reader.readUInt16LE();
                        this.listWzUsed = header !== 0x9C78 && header !== 0xDA78 && header !== 0x0178 && header !== 0x5E78;
                        if (!this.listWzUsed) {
                            data = rawImageBytes;
                        }
                        else {
                            reader.pos -= 2;
                            dataStream = [];
                            blocksize = 0;
                            endOfPng = rawImageBytes.length;
                            while (reader.pos < endOfPng) {
                                blocksize = reader.readInt32LE();
                                for (i = 0; i < blocksize; i++) {
                                    dataStream.push(((reader.readUInt8() ^ this.parentImage.reader.wzKey.at(i)) >>> 0) & 0xFF);
                                }
                            }
                            data = new Uint8Array(dataStream);
                        }
                        reader.dispose();
                        format = this.format1 + this.format2;
                        uncompressedSize = 0;
                        decBuf = null;
                        _a = format;
                        switch (_a) {
                            case 1: return [3 /*break*/, 2];
                            case 2: return [3 /*break*/, 4];
                            case 3: return [3 /*break*/, 6];
                            case 257: return [3 /*break*/, 8];
                            case 513: return [3 /*break*/, 10];
                            case 517: return [3 /*break*/, 12];
                            case 1026: return [3 /*break*/, 14];
                            case 2050: return [3 /*break*/, 16];
                        }
                        return [3 /*break*/, 18];
                    case 2:
                        uncompressedSize = this.width * this.height * 2;
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 3:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 4:
                        uncompressedSize = this.width * this.height * 4;
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 5:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 6:
                        uncompressedSize = this.width * this.height * 4;
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 7:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 8:
                        // "Npc.wz\\2570101.img\\info\\illustration2\\face\\0"
                        uncompressedSize = this.width * this.height * 2;
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 9:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 10:
                        uncompressedSize = this.width * this.height * 2;
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 11:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 12:
                        uncompressedSize = parseInt(this.width * this.height / 128);
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 13:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 14:
                        uncompressedSize = this.width * this.height * 4;
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 15:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 16:
                        uncompressedSize = this.width * this.height;
                        return [4 /*yield*/, inflate(data, uncompressedSize)];
                    case 17:
                        decBuf = _b.sent();
                        return [3 /*break*/, 19];
                    case 18:
                        {
                            ErrorLogger.log(ErrorLevel.MissingFeature, "Unknown PNG format ".concat(this.format1, " ").concat(this.format2));
                            return [3 /*break*/, 19];
                        }
                        _b.label = 19;
                    case 19: return [2 /*return*/, decBuf];
                }
            });
        });
    };
    WzPngProperty.getPixelDataBgra4444 = function (rawData, width, height) {
        var b = 0;
        var g = 0;
        var byteAtPosition = 0;
        var uncompressedSize = width * height * 2;
        var argb = new Uint8Array(uncompressedSize * 2);
        for (var i = 0; i < uncompressedSize; i++) {
            byteAtPosition = rawData[i];
            b = byteAtPosition & 0x0F;
            b |= (b << 4);
            argb[i * 2] = (b >>> 0) & 0xff;
            g = byteAtPosition & 0xF0;
            g |= (g >> 4);
            argb[i * 2 + 1] = (g >>> 0) & 0xff;
        }
        return argb;
    };
    WzPngProperty.getPixelDataForm517 = function (rawData, width, height) {
        var pixel = new Uint8Array(width * height * 2);
        var lineIndex = 0;
        for (var j0 = 0, j1 = parseInt((height / 16)); j0 < j1; j0++) {
            var dstIndex = lineIndex;
            for (var i0 = 0, i1 = parseInt((width / 16)); i0 < i1; i0++) {
                var idx = (i0 + j0 * i1) * 2;
                var b0 = rawData[idx];
                var b1 = rawData[idx + 1];
                for (var k = 0; k < 16; k++) {
                    pixel[dstIndex++] = b0;
                    pixel[dstIndex++] = b1;
                }
            }
            for (var k = 1; k < 16; k++) {
                var src = new Uint8Array(pixel.buffer, pixel.byteOffset + lineIndex, width * 2);
                pixel.set(src, dstIndex);
                // pixel.copy(pixel, dstIndex, lineIndex, lineIndex + (width * 2))
                dstIndex += width * 2;
            }
            lineIndex += width * 32;
        }
        return pixel;
    };
    WzPngProperty.getPixelDataDXT5 = function (rawData, width, height) {
        var pixel = new Uint8Array(width * height * 4);
        var colorTable = [
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0)
        ];
        var colorIdxTable = Array(16).fill(0);
        var alphaTable = new Uint8Array(8);
        var alphaIdxTable = Array(16).fill(0);
        for (var y = 0; y < height; y += 4) {
            for (var x = 0; x < width; x += 4) {
                var off = x * 4 + y * width;
                WzPngProperty.expandAlphaTableDXT5(alphaTable, rawData[off + 0], rawData[off + 1]);
                WzPngProperty.expandAlphaIndexTableDXT5(alphaIdxTable, rawData, off + 2);
                var r = new BinaryReader(rawData);
                r.seek(off + 8);
                var u0 = r.readUInt16LE(); // rawData.readUInt16LE(off + 8)
                var u1 = r.readUInt16LE(); // rawData.readUInt16LE(off + 10)
                r.dispose();
                WzPngProperty.expandColorTable(colorTable, u0, u1);
                WzPngProperty.expandColorIndexTable(colorIdxTable, rawData, off + 12);
                for (var j = 0; j < 4; j++) {
                    for (var i = 0; i < 4; i++) {
                        WzPngProperty.setPixel(pixel, x + i, y + j, width, colorTable[colorIdxTable[j * 4 + i]], alphaTable[alphaIdxTable[j * 4 + i]]);
                    }
                }
            }
        }
        return pixel;
    };
    WzPngProperty.expandAlphaTableDXT5 = function (alpha, a0, a1) {
        alpha[0] = a0;
        alpha[1] = a1;
        if (a0 > a1) {
            for (var i = 2; i < 8; i++) {
                alpha[i] = (parseInt(((8 - i) * a0 + (i - 1) * a1 + 3) / 7) >>> 0) & 0xff;
            }
        }
        else {
            for (var i = 2; i < 6; i++) {
                alpha[i] = (parseInt(((6 - i) * a0 + (i - 1) * a1 + 2) / 5) >>> 0) & 0xff;
            }
            alpha[6] = 0;
            alpha[7] = 255;
        }
    };
    WzPngProperty.expandAlphaIndexTableDXT5 = function (alphaIndex, rawData, offset) {
        for (var i = 0; i < 16; i += 8, offset += 3) {
            var flags = rawData[offset] |
                (rawData[offset + 1] << 8) |
                (rawData[offset + 2] << 16);
            for (var j = 0; j < 8; j++) {
                var mask = 0x07 << (3 * j);
                alphaIndex[i + j] = (flags & mask) >> (3 * j);
            }
        }
    };
    WzPngProperty.getPixelDataDXT3 = function (rawData, width, height) {
        var pixel = new Uint8Array(width * height * 4);
        var colorTable = [
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0),
            Color.fromRgb(0, 0, 0)
        ];
        var colorIdxTable = Array(16).fill(0);
        var alphaTable = new Uint8Array(16);
        for (var y = 0; y < height; y += 4) {
            for (var x = 0; x < width; x += 4) {
                var off = x * 4 + y * width;
                WzPngProperty.expandAlphaTableDXT3(alphaTable, rawData, off);
                var r = new BinaryReader(rawData);
                r.seek(off + 8);
                var u0 = r.readUInt16LE(); // rawData.readUInt16LE(off + 8)
                var u1 = r.readUInt16LE(); // rawData.readUInt16LE(off + 10)
                r.dispose();
                WzPngProperty.expandColorTable(colorTable, u0, u1);
                WzPngProperty.expandColorIndexTable(colorIdxTable, rawData, off + 12);
                for (var j = 0; j < 4; j++) {
                    for (var i = 0; i < 4; i++) {
                        WzPngProperty.setPixel(pixel, x + i, y + j, width, colorTable[colorIdxTable[j * 4 + i]], alphaTable[j * 4 + i]);
                    }
                }
            }
        }
        return pixel;
    };
    WzPngProperty.expandAlphaTableDXT3 = function (alpha, rawData, offset) {
        for (var i = 0; i < 16; i += 2, offset++) {
            alpha[i + 0] = (rawData[offset] & 0x0f) >>> 0;
            alpha[i + 1] = ((rawData[offset] & 0xf0) >> 4) >>> 0;
        }
        for (var i = 0; i < 16; i++) {
            alpha[i] = ((alpha[i] | (alpha[i] << 4)) >>> 0) & 0xff;
        }
    };
    WzPngProperty.setPixel = function (pixelData, x, y, width, color, alpha) {
        var offset = (y * width + x) * 4;
        pixelData[offset + 0] = color.b;
        pixelData[offset + 1] = color.g;
        pixelData[offset + 2] = color.r;
        pixelData[offset + 3] = (alpha >>> 0) & 0xff;
    };
    WzPngProperty.expandColorTable = function (color, c0, c1) {
        color[0] = rgb565ToColor(c0);
        color[1] = rgb565ToColor(c1);
        if (c0 > c1) {
            color[2] = Color.fromArgb(0xff, parseInt((color[0].r * 2 + color[1].r + 1) / 3), parseInt((color[0].g * 2 + color[1].g + 1) / 3), parseInt((color[0].b * 2 + color[1].b + 1) / 3));
            color[3] = Color.fromArgb(0xff, parseInt((color[0].r + color[1].r * 2 + 1) / 3), parseInt((color[0].g + color[1].g * 2 + 1) / 3), parseInt((color[0].b + color[1].b * 2 + 1) / 3));
        }
        else {
            color[2] = Color.fromArgb(0xff, parseInt((color[0].r + color[1].r) / 2), parseInt((color[0].g + color[1].g) / 2), parseInt((color[0].b + color[1].b) / 2));
            color[3] = Color.fromAc(0xff, Color.black);
        }
    };
    WzPngProperty.expandColorIndexTable = function (colorIndex, rawData, offset) {
        for (var i = 0; i < 16; i += 4, offset++) {
            colorIndex[i + 0] = (rawData[offset] & 0x03);
            colorIndex[i + 1] = (rawData[offset] & 0x0c) >> 2;
            colorIndex[i + 2] = (rawData[offset] & 0x30) >> 4;
            colorIndex[i + 3] = (rawData[offset] & 0xc0) >> 6;
        }
    };
    WzPngProperty.prototype.getBitmap = function () {
        return this.getImage(false);
    };
    WzPngProperty.prototype.saveToFile = function (file) {
        return __awaiter(this, void 0, void 0, function () {
            var png;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!(this.png != null)) return [3 /*break*/, 2];
                        return [4 /*yield*/, this.png.writeAsync(file)];
                    case 1:
                        _a.sent();
                        return [2 /*return*/, true];
                    case 2: return [4 /*yield*/, this.getImage(false)];
                    case 3:
                        png = _a.sent();
                        if (!(png != null)) return [3 /*break*/, 5];
                        return [4 /*yield*/, png.writeAsync(file)];
                    case 4:
                        _a.sent();
                        return [2 /*return*/, true];
                    case 5: return [2 /*return*/, false];
                }
            });
        });
    };
    return WzPngProperty;
}(WzExtended));
export { WzPngProperty };
function inflate(data, len) {
    if (typeof window !== 'undefined') {
        return inflateWasm(data, len);
    }
    return new Promise(function (resolve, reject) {
        var inflateStream = zlib.createInflate();
        var buf = _Buffer.alloc(len);
        var chunks = [];
        inflateStream.once('error', reject);
        inflateStream.on('data', function (chunk) {
            chunks.push(chunk);
        });
        inflateStream.on('finish', function () {
            var chunk = _Buffer.concat(chunks);
            chunk.copy(buf, 0, 0, Math.min(chunk.length, len));
            resolve(buf);
            inflateStream.close();
        });
        inflateStream.end(data);
    });
}
function inflateWasm(data, len) {
    return __awaiter(this, void 0, void 0, function () {
        var wzWasm, buf;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    if (!(wasmInflate === null)) return [3 /*break*/, 3];
                    return [4 /*yield*/, import("../util/wz.js")];
                case 1:
                    wzWasm = _a.sent();
                    /* const { Module } =  */ return [4 /*yield*/, wzWasm.default()];
                case 2:
                    /* const { Module } =  */ _a.sent();
                    wasmInflate = wzWasm.inflate;
                    _a.label = 3;
                case 3:
                    buf = wasmInflate(data, len);
                    return [2 /*return*/, buf];
            }
        });
    });
}
function rgb565ToColor(val) {
    var rgb565maskR = 0xf800;
    var rgb565maskG = 0x07e0;
    var rgb565maskB = 0x001f;
    var r = (val & rgb565maskR) >> 11;
    var g = (val & rgb565maskG) >> 5;
    var b = (val & rgb565maskB);
    var c = Color.fromRgb((r << 3) | (r >> 2), (g << 2) | (g >> 4), (b << 3) | (b >> 2));
    return c;
}
function bgra8888(img, data, length) {
    var x = 0;
    var y = 0;
    var width = img.getWidth();
    for (var i = 0; i < length; i += 4) {
        img.setPixelColor(Canvas.rgbaToInt(data[i + 2], data[i + 1], data[i + 0], data[i + 3]), x, y);
        x++;
        if (x >= width) {
            x = 0;
            y++;
        }
    }
}
function rgb565(img, data, length) {
    // rrrrrggg gggbbbbb
    var x = 0;
    var y = 0;
    var width = img.getWidth();
    var r = new BinaryReader(data);
    for (var i = 0; i < length; i += 2) {
        var ushort = r.readUInt16LE(); // data.readUInt16LE(i)
        var c = rgb565ToColor(ushort);
        img.setPixelColor(Canvas.rgbaToInt(c.r, c.g, c.b, c.a), x, y);
        x++;
        if (x >= width) {
            x = 0;
            y++;
        }
    }
    r.dispose();
}
function argb1555(img, data, length) {
    // arrrrrgg gggbbbbb
    var x = 0;
    var y = 0;
    var width = img.getWidth();
    var r = new BinaryReader(data);
    for (var i = 0; i < length; i += 2) {
        var ushort = r.readUInt16LE(); // data.readUInt16LE(i)
        var c = argb1555ToColor(ushort);
        img.setPixelColor(Canvas.rgbaToInt(c.r, c.g, c.b, c.a), x, y);
        x++;
        if (x >= width) {
            x = 0;
            y++;
        }
    }
    r.dispose();
}
function argb1555ToColor(val) {
    var maskA = 0x8000;
    var maskR = 0x7c00;
    var maskG = 0x03e0;
    var maskB = 0x001f;
    var a = ((val & maskA) !== 0) ? 255 : 0;
    var r = (val & maskR) >> 10;
    var g = (val & maskG) >> 5;
    var b = (val & maskB);
    var c = Color.fromArgb(a, (r << 3) | (r >> 2), (g << 3) | (g >> 2), (b << 3) | (b >> 2));
    return c;
}
